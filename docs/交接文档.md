# 项目交接文档 — 我的电子辞典（zidiankaifa）

> **交接时间**：2026-09-13　**HEAD**：`aada652`　**最新版本**：v0.7.1　**工作区**：干净，与 `origin/main` 同步
>
> **接手须知**：先读本文 → 再读 **`AGENTS.md`**（环境事实与防循环铁律）→ 需要细节时查 `docs/需求总结.md`（20 章编年史）。
> **不要重新勘察环境**——`AGENTS.md` 第 3 节已固化全部踩坑经验。

---

## 0. 三十秒概览

自研电子辞典。**英语 77 万词 + 俄语 10.1 万词**，带词源演变链、词根词缀拆解、词根表/词缀表、同根词族；React + TypeScript + SQLite 一套代码，Electron 出 Windows 桌面版，PWA 覆盖手机端，配套 Node 云同步服务。

- **已发布 12 个版本**（v0.2.0 → v0.7.1），最新版安装包 166.92MB
- **测试 226 项全绿**（core 204 + desktop 22）
- **功能需求 F1–F13 全部完成**；用户实测反馈 4 项全部解决
- **剩余工作集中在数据覆盖率与产品增强**（见 §6），无阻塞性缺陷

---

## 1. 项目结构与技术栈

| 部分 | 路径 | 说明 |
| --- | --- | --- |
| 共享数据层 | `packages/core/src/` | `types.ts` 契约 / `lang.ts` 标签映射 / `db/index.ts` 查词·拆解·词源 / `db/lexicon.ts` 词根词缀表 / `db/schema.ts` / `graph.ts` |
| 数据管线 | `packages/data-pipeline/` | Python：`build_db.py` / `build_wiktextract.py` / `build_ru_etym.py` / `build_roots_tables.mjs` / `roots.json` / `roots_ru.json` |
| Web 前端 | `apps/web/src/` | React 18 + TS + Vite，**手写 CSS**（无 UI 库）；`api.ts` 双后端（rest / electron） |
| 桌面端 | `apps/desktop/src/` | `main.mjs` 主进程+IPC / `preload.mjs` / `updater.mjs` / `dbmigrate.mjs` |
| 同步服务 | `apps/sync-server/src/index.ts` | Node 内置 http，端口 4570 |
| 词库 | `data/db/dict.db` | **494.23 MB**，不入 git |

**技术栈**：React 18 + TypeScript + Vite / Electron 37（`node:sqlite`，无原生编译依赖）/ Node 内置 http / SQLite。

---

## 2. 最小运行路径

```powershell
# 依赖（首次）
pnpm install

# 构建（改过 core 必须重跑；改过 web 要重跑 web）
pnpm --filter @zidiankaifa/core build
pnpm --filter @zidiankaifa/web build

# 桌面开发
pnpm --filter @zidiankaifa/desktop dev

# 测试（226 项）
pnpm --filter @zidiankaifa/core test      # 204 项，需 data/db/dict.db
pnpm --filter @zidiankaifa/desktop test   # 22 项，词库升级逻辑

# UI 验证（免 dev server，规避沙箱 spawn EPERM）
node _serve_static.mjs 5180                                     # 静态托管 apps/web/dist
$env:ZIDIANKAIFA_DB='D:\lzk17\Documents\zidiankaifa\data\db\dict.db'; node apps/sync-server/dist/index.js

# 打包（环境变量三件套见 AGENTS.md 第 3 节）
pnpm --filter @zidiankaifa/desktop build
```

**发布流程**见 `AGENTS.md` 第 6 节（含 gh 认证模板）。

---

## 3. 需求清单与完成状态

### 3.1 功能需求 F1–F13（全部完成 ✅）

| 编号 | 需求 | 状态 | 说明 |
| --- | --- | --- | --- |
| F1 | 查词 | ✅ | 音标/释义/词性/柯林斯·牛津星级/考试标签/词频 |
| F2 | 发音 | ✅ | 系统 TTS（真人音频未做，见 §6） |
| F3 | 词源分类 | ✅ | 英语词源 154,984 条 + 俄语 31,256 条（中文 14,224）；词级演变链 + 语言判定 |
| F4 | 词形演变 | ✅ | `word_forms` 196,396 条，点词形即查 |
| F5 | 词根词缀拆解 | ✅ | 词素库 909 条（英 468 / 俄 441），全局最优分段 DP |
| F6 | 生词本 | ✅ | 四态 + 标签 + 按词根分组视图 + 桌面端知识图谱 |
| F7 | 剪贴板取词 | ✅ | 桌面端轮询 |
| F8 | 桌面应用 | ✅ | Electron，自动更新（blockmap 差分） |
| F9 | 云同步 | ✅ | last-write-wins，含删除墓碑 |
| F10 | 多语言（俄语） | ✅ | 101,512 主词条 + 变格变位表格 + 词形反查 633,514 行 |
| F11 | 浏览器/PWA | ✅ | 同一套前端，静态托管即可 |
| F12 | 数据管线 | ✅ | 10 张业务表 + 2 个视图 |
| F13 | 自动更新 | ✅ | 静默检查 → 条幅 → 差分下载 → 重启安装 |

### 3.2 用户实测反馈（v0.2 四项，全部解决 ✅）

| 问题 | 状态 | 解决方式 |
| --- | --- | --- |
| P0-1 英俄混用混乱 | ✅ | 语言切换器（自动/英语/俄语）+ suggest 语言隔离 |
| P0-2 构词路径不体现、词根不明确 | ✅ | 词源卡加「构词成分」行，把词素级与词级演变串成路径 |
| P1-3 知识图谱联系生词 | ✅ | 词根/词缀表 + 生词本按词根分组 + SVG 图谱 |
| P1-4 俄语变格变位不明确 | ✅ | 按语法标签成表（格×数 / 人称×时态） |

### 3.3 v0.7.1 修复（本轮）

**桌面端升级不换库**：`resolveDbPath()` 原先只在 userData 副本不存在时复制内置库，导致装过旧版后**永久读旧库**（用户实测 v0.7.0 仍在读 v0.2.x 的 509MB 旧库，俄语词源/词素均为 0）。已由 `apps/desktop/src/dbmigrate.mjs` 的 `ensureUserDb()` 修复（指纹比对换库 + 生词本迁移 + 备份）。详见 `docs/release-v0.7.1.md` 与 `docs/需求总结.md` §20。

---

## 4. 版本时间线

| 版本 | 主题 | 关键数字 |
| --- | --- | --- |
| v0.2.0 | 首个发布版（安装包 + 便携版） | exe 148MB |
| v0.2.1 | 数据管线补全 | 俄语 forms 48.8%→62.8%；中文词源 9,378 行 |
| v0.3.0 | 俄语词源 · 词根词缀 | 俄语词源 0→30,767；俄语词素 145 条 |
| v0.4.0 | 自动更新系统 | blockmap 差分 |
| v0.4.1 | 查词正确性修复 | 专名大小写漏失、词形反查劫持；词素 601 |
| v0.4.2 | 更新行为核验与加固 | 防自动下载 |
| v0.4.3 | 俄语词素数据驱动扩充 | 词素 888（俄 432） |
| v0.4.4 | 屈折形污染清理 | 清除 58,195 条；俄语词条 159,127→101,512 |
| v0.5.0 | 数据质量总收 | 词源来源修正 19,865 条；VACUUM 515→491MB；覆盖率口径修正 30.8% |
| v0.6.0 | 词根表 / 词缀表 | roots 476 + affixes 403；拆解算法改 DP；lexicon 67 项测试 |
| v0.7.0 | 词源关联网络 · 同根词族 | 词源相关词可点；同根词族；词素 909 |
| **v0.7.1** | **修复升级不换库** | dbmigrate + 22 项测试 |

全部 Release：https://github.com/lzk101/zidiankaifa/releases

---

## 5. 数据与代码终态（实测）

### 5.1 词库 `data/db/dict.db`（494.23 MB）

| 表 / 视图 | 行数 | 说明 |
| --- | --- | --- |
| `words` | 770,611 | 英语词条（ECDICT） |
| `words_i18n` (ru) | 101,512 | 俄语主词条 |
| `word_etymology` (en) | 154,984 | 英语词源 |
| `word_etymology` (ru) | 31,256 | 俄语词源（中文 14,224） |
| `morphemes` (en / ru) | 468 / 441 | 词素库，共 909 |
| `roots` | 476（en 322 / ru 154） | 词根表（含关联词倒排） |
| `affixes` | 403（en 前缀 59 + 后缀 67；ru 前缀 185 + 后缀 92） | 词缀表 |
| `i18n_forms` | 633,514 | 俄语词形反查 |
| `word_forms` | 196,396 | 英语词形 |
| `words_en` / `words_ru`（视图） | 770,611 / 101,512 | 分语言单词表 |
| `book` | 3（首次装后为空） | 生词本 |

**另有 4 张备份/日志表**：`words_i18n_inflection_backup`、`word_etymology_origin_backup`、`i18n_forms_fix_backup`、`_inflection_cleanup_log`（可清理以减小安装包，见 §6-A5）。

### 5.2 覆盖率现状

| 指标 | 数值 | 备注 |
| --- | --- | --- |
| 俄语词源覆盖 | **30.8%** | 31,256 / 101,512；en/zh/ru 三转储已挖到头 |
| 俄语拆解覆盖 | **30.8%**（下限断言 ≥25%） | 词素库 441 条限制 |
| 俄语词族覆盖 | **30.6%**（≥20%） | 受拆解覆盖率限制 |
| 英语词族覆盖 | **23.2%**（≥15%） | — |
| 俄语音标 / 音频 | 64.2% / 10.4% | 受源数据限制 |

### 5.3 测试

```
pnpm --filter @zidiankaifa/core test     →  regress 99 + lexicon 67 + related 38 = 204 通过 / 0 失败
pnpm --filter @zidiankaifa/desktop test  →  dbmigrate 22 通过 / 0 失败
```

---

## 6. 未完成任务（按优先级）

### A. 数据类

| # | 任务 | 现状 / 入口 | 验收标准 |
| --- | --- | --- | --- |
| **A1** | **提高俄语词源覆盖率** | 30.8%；en/zh/ru 三个 kaikki 转储已确认挖到头（第三源 44.3 万词条仅可新增 128 条）。需**新数据源**（如 ru.wiktionary XML dump、词源词典） | 覆盖率 >40% 且抽样准确率 >95% |
| **A2** | **提高俄语拆解覆盖率** | 30.8%；入口 `packages/data-pipeline/roots_ru.json`（441 条）+ `analyze_ru_morphemes.py` 挖矿。**注意**：自动挖矿曾试过并被否（同前缀 ≠ 同源，TOP40 全是噪声），需**人工审定** | 覆盖率 >45%，且 20 词正反例判别集全绿 |
| **A3** | 拆解残余噪声 | `stateless` 被假命中 `tele`（位置 4-8）；`рас-` 词缀组含 `раса`/`Расти` | 误拆率下降且不伤既有正例 |
| **A4** | 英语词源链接覆盖面 | 仅覆盖 `+`/连字符构词表达，`unbelievable` 这类纯叙述型 `text_en` 抽不出词素（按全文实词提取已验证会引入 distance/apparatus 噪声） | — |
| **A5** | 清理备份表 + VACUUM | 4 张备份表仍在库内；上游清理脚本：`clean_i18n_inflections.mjs` / `fix_etym_origin.mjs` / `fix_inflection_forms.mjs` | 安装包进一步减小，且测试仍 204 通过 |
| **A6** | 单词根词无法拆解 | `луна`/`небо`/`река`/`гора` 等 50/106 常用基础词无拆解 → 无同根词；已用**词源链接**补足 | — |

### B. 产品功能类（Roadmap 未完成）

| # | 任务 | 说明 |
| --- | --- | --- |
| **B1** | 真人发音音频 | ECDICT `audio` 字段已有 10.4% 覆盖，可接有道/离线音频 |
| **B2** | 背单词 / 记忆曲线复习 | 生词本已有 `reviewCount`/`lastReviewedAt` 字段，数据层就绪 |
| **B3** | Capacitor 打包 Android APK | 已预留配置，前端是 PWA 可直接复用 |
| **B4** | 中文分词搜索（FTS5 trigram） | 目前中文反查是 `translation LIKE`，无索引 |
| **B5** | 全局划词取词（快捷键） | 桌面端已有剪贴板轮询，缺全局热键 |
| **B6** | exe 应用图标 | 打包日志 `default Electron icon is used`，当前是 Electron 默认图标 |
| **B7** | 俄语模式 suggest 细化 | 已接 `lang`，但 auto 模式下中文反查按设计英俄交错 |

### C. 工程类（低优先）

| # | 任务 | 说明 |
| --- | --- | --- |
| C1 | `sync-server` 需重新构建 | 本轮 bump 到 0.7.1 但没跑它的 build，`/health` 仍返回 `version: 0.7.0`（仅显示问题，功能正常）。下次改 `apps/sync-server/src` 后记得 `pnpm --filter @zidiankaifa/sync-server build` |
| C2 | 词根表关联词统计 | `roots`/`affixes` 表的关联词字段结构见 `packages/data-pipeline/build_roots_tables.mjs`；§5.1 未列关联词总数（约 11.4 万条） |

---

## 7. 已知约束与"非缺陷"清单（避免误判）

1. **截图里卡片空白 ≠ 数据为空**：`ZIDIANKAFA_SHOT` 的 `capturePage` **只截视口**。v0.7.1 排查中曾据此误判「词根词缀拆解」为空，实际拆解卡在词源卡**下方视口外**。用 `browser_snapshot` 或 `ZIDIANKAFA_DUMP` 验证。
2. **俄语模式查英语词显示「未收录」是设计**（v0.2「英俄分离」需求），不是 bug。
3. **中文反查英俄交错**是 auto 模式的刻意设计；用语言切换器可强制隔离。
4. **`COLLATE NOCASE` 对西里尔无效**，俄语专名查询必须走 `caseVariants()`。
5. **不要在 `SCHEMA_SQL` 里给新列写 `CREATE INDEX`**（老库会崩 `no such column`）。
6. **改算法前先建正反例判别集**——本项目在 `breakdownWord` 上连续 4 次调参回退的教训见 `AGENTS.md` 第 4.1 节。

---

## 8. 关键文档索引

| 文档 | 用途 |
| --- | --- |
| **`AGENTS.md`** | **首读**。环境事实（第 3 节）+ 防循环熔断规则（第 4 节）+ 发布流程（第 6 节） |
| **`docs/交接文档.md`** | 本文。项目进度、需求清单、未完成任务 |
| `README.md` | 面向用户的功能介绍、技术栈、快速开始、接口列表、Roadmap |
| `docs/需求总结.md` | **权威编年史**。20 章：每轮需求 → 勘察 → 实现 → 自测 → 遗留（90.8 KB） |
| `docs/release-v0.7.0.md` / `docs/release-v0.7.1.md` | 最近两次发布的详情 |
| ⚠️ 根目录 `需求总结.md`、`已完成需求.md`、`任务排序.md`、`运行日志.md` | **已废弃**。2026-08-31 后停更，由已停摆的 `check_requirements.py` 生成，内容严重过时（根目录那份 `需求总结.md` 仅 8 行、只提 R2）。**不要作为进度依据**，已加废弃横幅 |

> **注意**：根目录的 `需求总结.md` 与 `docs/需求总结.md` 是**两个完全不同的文件**，前者是僵尸产物。接手时若只按文件名找到根目录那份，会得到与事实相反的结论。

---

## 9. 交接检查清单

接手后建议按序执行，确认环境可用：

```powershell
# 1. 确认工作区干净、与远端同步
git status --short ; git rev-list --left-right --count origin/main...HEAD   # 期望 0  0

# 2. 跑全部测试（226 项），确认基线
pnpm --filter @zidiankaifa/core test
pnpm --filter @zidiankaifa/desktop test

# 3. 起服务做一次 UI 冒烟
node _serve_static.mjs 5180
$env:ZIDIANKAIFA_DB='D:\lzk17\Documents\zidiankaifa\data\db\dict.db'; node apps/sync-server/dist/index.js
# 浏览器打 http://localhost:5180/，切俄语查 тоска：
#   期望看到 变格表 + 词源（原始斯拉夫语 *tъska）+ 构词成分 тоск- + 拆解 + 同根词族 5 词

# 4. 重装 exe 验证升级不换库的修复
#    观察 %APPDATA%\@zidiankaifa\desktop\ 下是否生成 dict.db.stamp 与 dict.db.bak-*
```

**基线预期**：测试 226/226；`тоска` 渲染出拆解与词源；换库后 userData 的俄语词源 31,256、俄语词素 441、roots 476。
