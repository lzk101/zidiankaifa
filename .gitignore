node_modules/
dist/
build/
*.log
.DS_Store
.env
*.tsbuildinfo
__pycache__/
*.pyc
.pnpm-store/
.electron-cache/
.eb-cache/
.gh-config/
.pip-tmp/
data/py-deps/
dist-release/
data/raw/*
!data/raw/.gitkeep
data/db/*.db
data/db/*.db-*
data/sync-data/
data/*.png
data/*.txt
apps/sync-server/dist/

data/db/*.bak*
packages/data-pipeline/_ru_etym_stats.json
data/db/*_backup*.json
# ---- 落库备份（结构性防误提交）----
# 背景：v0.9.0 落库由 scripts/apply_v9_morphemes{,2}.mjs 执行，脚本会写
#   roots_ru.json.bak-<ISO 时间戳> / roots_ru.json.bak2-*
# 属一次性回滚快照，**不入 git**（原始内容由 git 历史保证）。
packages/data-pipeline/*.json.bak*
packages/data-pipeline/*.json.bak2*

_serve_static.mjs

# ---- 临时工作目录（结构性防误提交）----
# 背景：v0.8.0 发布时 `git add scripts/` 与 `git add .board/` 曾把临时探针一并暂存
# （.board/_tmp/ 下 11 个文件已误提交进 4c89272，后由 55f8745 移除）。
# 项目惯例是**工作区保留、不入 git**（与上面的 _serve_static.mjs 同模式）。
_tmp/
.tmp/
# agent 工具产生的随机临时目录，形如
#   scripts/.probe_t20_prefixfamily.mjs.82276.57378a70-aa46-....tmpdir/
*.tmpdir/
# 端到端冒烟用的临时目录（含库副本与临时 userData，单次可达数百 MB）
#   形如 .smoke_t44/ · .smoke_t44b/
.smoke_*/
# 主管写提交说明用的临时文件（pwsh 不支持 heredoc ⇒ 一贯写文件后 git commit -F）
#   形如 .board/_commit_v0100.txt · .board/_commit_t54.txt
#   背景：v0.10.0 的 T54 提交把 .board/_commit_t54.txt 一并 `git add .board` 进了索引，
#   并已随 04deb37 推送 ⇒ 结构性堵住（下划线前缀临时文件一律不入 git）。
_board_commit*
_commit*.txt

# ---- v0.11.0 手机端本地工具链（结构性防误提交）----
# 背景：Android 构建需要 JDK + Android SDK，但本机原先 JAVA_HOME / ANDROID_HOME 均为空
#   （java / gradle / adb 全未安装）。用户授权后就地安装到仓库内，**绝不入 git**：
#   .android-toolchain/jdk/jdk-21.0.12.1+1/  Temurin JDK 21（★实际用于 Android 构建）
#   .android-toolchain/jdk/jdk-17.0.20.1+1/   Temurin JDK 17（保留但**不再用于 Android 构建**）
#   .android-toolchain/sdk/     Android SDK（platform-tools + platforms + build-tools）
#   .android-toolchain/dl/      原始 zip 安装包（jdk21.zip 205 MB / jdk17.zip 190 MB / cmdline-tools.zip 155 MB）
#   .android-toolchain/gradle/  Gradle user home（android 工程首次构建会拉 Gradle 发行包与依赖，实测 461 MB）
# ⚠ **必须是 JDK 21，不是 17**：@capacitor/android@8.5.2 的 capacitor/build.gradle 写死
#   sourceCompatibility / targetCompatibility = JavaVersion.VERSION_21，用 17 构建会失败于
#   `:capacitor-android:compileDebugJavaWithJavac` → `无效的源发行版：21`。（Capacitor 7 才配 JDK 17。）
# 放在仓库内是为了让 JAVA_HOME / ANDROID_HOME 有确定落点、每次构建可复现
# （依 AGENTS.md 铁律 4：环境事实必须落盘，不能只留在上下文里）。
# ⚠ 单个文件上限 20 MB、已跟踪文件 ≤300 的预算都不该被这些二进制拖累 ⇒ 必须忽略。
.android-toolchain/
# Gradle 构建产物与缓存（若沿用默认 GRADLE_USER_HOME=%USERPROFILE%\.gradle 则不会出现在此，仍结构性堵住）
.gradle-cache/

# ---- v0.11.0 手机端 Android 工程（Capacitor）----
# 原则：**原生工程入 git**（保证 android/ 的 Gradle 配置、Manifest、图标、签名配置可追溯、可复现），
#   **构建产物与本机路径不入 git** —— 这是 Capacitor 官方模板自带的 .gitignore 划分。
# 实测（`cap add android` 后）：android/ 共 68 文件 / 520,321 B，其中绝大多数是需入库的工程文件。
apps/mobile/android/build/
apps/mobile/android/app/build/
apps/mobile/android/.gradle/
apps/mobile/android/capacitor-cordova-android-plugins/
apps/mobile/android/local.properties
# ★ web 产物副本**不入库**（apps/mobile/android/app/src/main/assets/public/**）
#   实际生效的规则在 Capacitor 官方模板自带文件 `apps/mobile/android/.gitignore:96`
#   （其注释为 "Copied web assets"）——本文件不再重复声明，避免两处口径打架。
#   理由：该目录由 `cap sync` 每次从 apps/web/dist 整体复制生成，**与 web 产物是同一份东西**；
#   入库会造出「同一个 index-<hash>.js 有两个来源」的口径分裂（本项目已因同类口径问题踩坑 11 次）。
#   ⇒ 完整构建链（clone 之后）：pnpm install → pnpm --filter @zidiankaifa/web build
#     → pnpm --filter @zidiankaifa/mobile sync → android 工程 gradlew assembleDebug

# ---- v0.11.0 release 签名凭证（★安全：绝不能入 git）----
# 存放：apps/mobile/android/.toolchain/
#   zidiankaifa-upload.jks     release 签名密钥（App 的**永久身份**）
#   keystore.properties        Gradle 读取的口令（storePassword / keyPassword）
#
# ⚠ **这条规则必须显式写、且必须是 .toolchain/ 而不能只写 .android-toolchain/**：
#   `git check-ignore -v` 实测确认，根 .gitignore:71 的 `.android-toolchain/` 是**带短横线的另一个目录**，
#   不覆盖带点的 `.toolchain/`；而 git 的目录忽略规则对**嵌套**路径不自动生效
#   （实测 `--untracked-files=all` 会把这两个文件报成 `??` ⇒ 一旦有人 `git add apps/mobile` 就会把私钥提交上去）。
# ⚠ 私钥入库 = App 身份泄露：任何人都能用同一密钥伪造出「可覆盖升级」的恶意版本分发给你已安装的用户。
# ⇒ 双保险：本规则忽略整个目录；`apps/mobile/KEYSTORE.md` 记录凭证位置与指纹，但**只记指纹不记口令**。
.toolchain/

