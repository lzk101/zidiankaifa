# 结构台账 — zidiankaifa

> **写者**：项目结构管理 agent（第 4 个常驻角色）。**其他角色只读。**
> 委任书：`.board/roles/structure-agent.md`　额度依据：`.board/BASELINE.md` §7
> 台账建立：2026-09-16（角色建立时 HEAD `d558aa0`，版本 v0.10.0）

---

## 使用说明（结构 agent 必读）

1. **每次版本发布 + push 之后**追加一节「快检」；每 5 个 release 或大版本后追加一节「全量审计」。
2. 每节必须含 **§九项实测表**（逐项：实测值 · 额度 · 达标/越线）+ **本轮清理**（路径 · 字节 · 递归数 · 权限依据）+ **门禁复核**（清理后复跑 core/desktop）。
3. **清理前先记录证据再执行**；**已跟踪文件一律只列清单提请核准**，不自行删。
4. 体量必带「是否递归」；文件数必带口径（`git ls-files` / 只读 / 只增）。
5. 回执按委任书 §12 格式发给主管；**需要裁决的事项同时追加到 `.board/BOARD.md` 的 `## [项目结构管理 agent]` 区**。

---

## 节次索引

| # | 日期 | 节奏 | 触发版本 | 结论摘要 |
| --- | --- | --- | --- | --- |
| — | 2026-09-16 | 建账 | v0.10.0 | 台账建立；首轮体检**待做**（见下「待办」） |

---

## 待办（首轮，已由主管派单）

| # | 事项 | 状态 |
| --- | --- | --- |
| 1 | 九项体检实测，回填 `.board/BASELINE.md` §7「冻结时实测」列如与实测不符者，**在本文件提请主管订正**（不直接改 BASELINE） | ⏳ |
| 2 | `dist-release/` 超线 **−3.99 GB**（实测 5.49 GB vs 额度 1.5 GB）的**具体清理清单**：逐个文件名 + 字节数 + 保留理由，**先报主管核准，不执行** | ⏳ |
| 3 | `scripts/` 分类 MECE 校验（A 活跃 / B 证据 / C 一次性 / D 已失效 / E 临时）—— 五档必须覆盖全部文件且两两不相交 | ⏳ |
| 4 | 「清了又长」根因诊断：`scripts/_tmp/booklang_tmp/run-*` 由 `packages/core/test/book_lang.mjs` 每次运行重建（清理分支报 `EPERM`）⇒ 报根因而非反复重删 | ⏳ |
| 5 | 引用完整性：`.board/*.md` 与 `docs/*.md` 引用了哪些 `scripts/*.mjs`；产出**死引用清单**（被引用但已不存在的文件） | ⏳ |

---

## 首轮快检（v0.10.0 发布后）— **待填**

（按委任书 §5 九项逐项实测后填入；金额一律给绝对值 + 额度 + 差值）
