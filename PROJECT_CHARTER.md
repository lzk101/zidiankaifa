# 项目主管章程 — zidiankaifa 电子辞典

> 本文件是 zidiankaifa 项目**多 agent 协作架构**的常驻章程。任何接手本项目的会话/主 agent 都应先读本文，再读 `AGENTS.md`（环境事实与防循环铁律）。
> 建立日期：2026-09-16　建立时的 HEAD：`055c753`　版本：v0.7.1
>
> 📁 **结构指针**：文件放哪儿、能不能改、哪些是死代码 ⇒ 见根目录 **`PROJECT_STRUCTURE.md`**（结构主文档，2026-09-16 建）。
> 🧹 **结构治理**：**每完成一次版本发布，由第 4 个常驻 agent（项目结构管理 agent）做一次结构体检与清理** —— 额度表见 `.board/BASELINE.md` §7，职责见 `.board/roles/structure-agent.md`，台账见 `.board/STRUCTURE.md`。
> 📊 **当前状态**（`PROJECT_STRUCTURE.md` §10 有变更记录）：v0.10.0 已发布（tag `v0.10.0` / commit `041c6e7`）· 门禁 **643 通过 / 0 失败** · 词素库 `morphemes` **918**（en 468 / ru 450）。
> ⚠ 本文 §9 记录的 goal 口径**已被取代** —— 旧 objective「覆盖率 30.8% → >45%」已由用户拍板取消（详见 §9）。

---

## 1. 架构总览

```
                    ┌──────────────────────────────┐
                    │  用户（最终审定人 / 拍板人）    │
                    └──────────────┬───────────────┘
                                   │ 需求 · 拍板 · 词素审定
                    ┌──────────────▼───────────────┐
                    │  项目主管 agent（主会话）      │
                    │  派单 · 裁决 · 验收闸门        │
                    │  commit/tag/release 唯一执行者 │
                    └───┬──────────┬───────────┬───┘
                        │          │           │
              .board/ 文件中枢（非实时对话，留痕可审计）
                        │          │           │
        ┌───────────────▼──┐ ┌─────▼────────┐ ┌▼────────────────┐
        │ 需求管理 agent    │ │ 开发编程 agent│ │ 功能测试 agent   │
        │ REQ/DECISIONS    │ │ src/ 实现     │ │ test/ 断言+证据  │
        └──────────────────┘ └──────────────┘ └─────────────────┘
                        │
              ┌─────────▼──────────────────────┐
              │ 项目结构管理 agent（第 4 个）    │
              │ 每次版本发布后：结构体检+清理    │
              │ 只删产物，不删代码；只出方案     │
              └────────────────────────────────┘
```

**架构的真正价值不在"有三个 agent"，而在验收链路的独立性**：测试 agent 与开发 agent **不共享上下文**，它只能依据"需求文档里写死的验收标准"与"自己跑出来的证据"说话，**不接受开发 agent 的任何口头解释**（"已修复""应该没问题"）。

## 2. 角色职责与独占产出

| 角色 | 谁 | 独占写 | 绝不碰 |
| --- | --- | --- | --- |
| **项目主管** | 主 agent（本会话） | `.board/TASKS.md`、`.board/BASELINE.md`、`.board/BOARD.md` | 不自己下场写产品实现（避免既当运动员又当裁判） |
| **需求管理 agent** | 常驻 subagent | `.board/REQ.md`、`.board/DECISIONS.md`、`.board/CHANGELOG.md` | 不写任何 `src/` 代码 |
| **开发编程 agent** | 常驻 subagent | `packages/*/src/`、`apps/*/src/`、`packages/data-pipeline/` | 不改 `test/*.mjs` 断言 |
| **功能测试 agent** | 常驻 subagent | `packages/core/test/*.mjs`、`apps/desktop/test/*.mjs`、`scripts/`、`.board/EVIDENCE.md` | **不改 `src/` 一行** |
| **项目结构管理 agent** | 常驻 subagent（2026-09-16 增） | `.board/STRUCTURE.md`、`.board/structure/`、`PROJECT_STRUCTURE.md`（仅事实性更新）、`.gitignore`（只增规则）、`scripts/audit_*`/`fix_*_corruption`/`structure_*` | `data/**`、`src/**`、`test/**`、已跟踪文件的删除、**任何 git 写操作** |

**独占写是硬约束**：一个文件只有一个角色能写，避免并发写入互相覆盖。跨角色需要改对方的文件，必须走项目主管转发。

## 3. 文件中枢 `.board/`

| 文件 | 写者 | 内容 |
| --- | --- | --- |
| `REQ.md` | 需求管理 agent | 需求台账：编号 / 优先级 / 可测验收标准 / DoD / 非目标 / 状态 |
| `DECISIONS.md` | 需求管理 agent | 决策日志：决定 + **理由** + 日期 + 提出方 |
| `CHANGELOG.md` | 需求管理 agent | 发布流水（按版本） |
| `TASKS.md` | 项目主管 | 在飞任务、负责人、轮次、状态 |
| `BASELINE.md` | 项目主管 | 冻结基线：HEAD / 版本 / 测试计数 / 词库指纹 / 覆盖率三把尺子 |
| `BOARD.md` | 三方追加 | 发现、阻塞、假设、异议（按角色分区） |
| `EVIDENCE.md` | 功能测试 agent | 断言结果 + 实测证据（含原始命令与输出摘要） |
| `STRUCTURE.md` | 项目结构管理 agent | 结构台账：九项体检实测、越线项、清理记录、根因诊断 |
| `roles/*.md` | 项目主管 | 各角色的常驻委任书（职责边界的事实来源），现 4 份 |

**投递机制**：项目主管 `read` 议题板 → `send_message` 派单给常驻 subagent；subagent 写盘 → 只回结构化回执。**主会话只看回执，不把冗长工具输出带回主上下文**——这是保住主管自身上下文预算的关键。**现为 4 个常驻 agent**（需求管理 / 开发编程 / 功能测试 / 项目结构管理）。

## 4. 标准工作流（功能类需求）

```
① 定需求   主管+需求agent → REQ.md【可测验收标准 + DoD + 非目标】
② 断言先行 测试agent → test/*.mjs 先写【会红的断言】（红绿开发）
             ↓ 主管审 diff（闸门：断言是否为"真验收"而非"迁就实现"）
③ 实现     开发agent → 改 src/ → 小样本自测 → 回执（假设变化/风险）
④ 独立验证 测试agent → 全量 226 + 新断言 + 样本抽检（英/俄双语言）+ UI 实测
⑤ 裁决     主管：绿 → 需求agent 标 DoD 达成 + 补编年史；红 → 带证据打回 ③
⑥ 发布     版本 bump → 构建 → 打包 → commit/tag/push → gh release
⑦ 结构收尾 结构agent → 九项体检 + 产物/缓存/临时残留清理 → .board/STRUCTURE.md（每迭代必做）
```

**数据类任务**（A1–A6）差异：正反例判别集由**测试 agent 独立建**，与开发 agent 的判别集**不共享**，否则是自我验证。

## 5. 验收标准书写规范

必须**可观测、可度量**。不接受"优化拆解算法"，只接受：

> "`морфология` 被正确拆出；既有 20 词判别集不回归；尺子 R 覆盖率 30.8% → >X%"

写不出可测标准的需求，项目主管**打回需求 agent 重写**，不进入开发。

## 6. 防循环熔断（继承 `AGENTS.md` §4，并追加多 agent 专属条款）

| 情形 | 上限 | 到限后必须做什么 |
| --- | --- | --- |
| 同一操作、同一方法失败 | 2 次 | 换方法（换工具/换入口/换数据源），或直接问用户 |
| 同一文件同一函数改动仍不通过断言 | 3 轮 | 停止调参。写清「假设 A/B」做**判别实验**，而非继续微调阈值 |
| 同一环境类错误 | 1 次 | 立刻查 `AGENTS.md` §3，按既定解法执行 |
| **同一任务在主管↔开发↔测试之间往返** | **2 轮** | 第 3 轮由项目主管下场**重新分解需求**，不再原样重派 |
| **发现新的环境事实** | 1 次 | 立刻写进 `.board/BOARD.md`，由主管统一归并进 `AGENTS.md`——不要只留在某个 agent 的上下文里 |
| **同一路径反复清理仍复发**（结构 agent 专属） | 2 次 | 停止重删，**改为查根因**（谁在生成）并报告——如 `scripts/_tmp/booklang_tmp/run-*` 由 `book_lang.mjs` 每次运行重建 |

**判别实验优先于调参**：本项目在 `breakdownWord` 上连续 4 次调参回退的教训（`AGENTS.md` §4.1）必须避免。任何算法改动前，先建正例 5 + 反例 5 的判别集。

## 7. 硬性铁律（继承，适用于全部角色）

1. **不要用 `git add -A`**——`data/db/` 有数百 MB 文件，历史上误提交过 509MB 备份。只有项目主管执行 git 写操作，且只 `git add <明确路径>`。
2. **禁止 `sandbox_permissions` 升级**——本会话审批已禁用，请求会被自动拒绝。
3. 改 `packages/core` 后必须 `pnpm --filter @zidiankaifa/core build`，且**重启 sync-server**（它读 `dist`，不热更新）。
4. 改 `roots.json` / `roots_ru.json` 后必须重建词素表（`$env:PYTHONIOENCODING='utf-8'; python packages/data-pipeline/build_db.py morphemes`）。
5. 改 `apps/desktop/src/main.mjs` 的 `resolveDbPath()` / `dbmigrate.mjs` 必须跑 `pnpm --filter @zidiankaifa/desktop test`（22 项）。
6. 根目录 `需求总结.md`/`已完成需求.md`/`任务排序.md`/`运行日志.md` 是**僵尸产物**，`check_requirements.py` 已停摆且**会覆盖废弃横幅，绝不运行**。权威文档在 `docs/`。

## 8. 已知风险与缓解

| 风险 | 缓解 |
| --- | --- |
| **测试 agent 无法真机验证 exe 升级换库**（需安装 exe） | `--user-data-dir=<临时目录>` 伪端到端验证；DoD 中写明"真机验收待用户在实机确认" |
| **发布链路的凭据依赖** | `git push` / `gh release create` 需 GCM 凭据与代理（`AGENTS.md` §3）；若被审批门拦住，挂起发布步骤交用户手动执行 |
| **agent 上下文无法压缩** | 主管只接收结构化回执；subagent 的大段工具输出不得回灌主会话 |
| **并发写同一文件** | 独占写表（§2）是硬约束 |
| **需求漂移** | 需求 agent 每次发布后跑「文档声称 vs 代码实际」对账 |
| **覆盖率尺子口径混乱** | 见 `.board/BASELINE.md`——项目里存在**三把分母不同的尺子**，提目标前必须先声明用哪把 |
| **结构熵增**（每迭代新增脚本/产物，历史上三次误提交临时文件） | 第 4 角色**项目结构管理 agent**：每次发布后九项体检 + 额度制（`.board/BASELINE.md` §7 结构预算）+ 自主清理产物（`.board/roles/structure-agent.md` §3.1） |
| **结构 agent 误删关键文件** | 保护清单是绝对的：`data/**` 整棵树 + 全部已跟踪文件只报不删；未跟踪且未被 gitignore 覆盖者只列清单；清理后必须复跑门禁 |

---

## 9. ⚠ 会话 goal 的**口径已被取代**（必读，防下个会话照旧口径继续）

### 现状

本会话存在 goal `goal-a653766a-957d-4852-855e-c2e5a7be86cf`（**revision 1**，max_goal_rounds 40），
其 objective **原文**为：

> 「……完成首个迭代 A2「俄语词根词缀拆解覆盖率 30.8% → >45%」——含人工审定门（词素候选须经用户确认）、
> 20 词正反例判别集全绿、全量 226 项测试不回归、bump 版本 + 打包 + 发 GitHub Release v0.8.0。」

### ★ 该 objective 中的「覆盖率 30.8% → >45%」**已被用户拍板取消**，不可再作为靶子

**取消依据（均可复核）**：T17 准入实验全库端到端实测判定**不通过**（详见 `.board/BOARD.md` 裁决十三）：
- 合法候选（过了「附来源」审定门的）上限只有 **33.88%**，距 45% 差 88 词；
- 能达 45% 的方案 **91.9% 的增益来自 2 字符片段**（`безаварийный → без- + ва- + -ный`），属**假达标**；
- 这类方案会让**全库空洞≥3 口径A +3,666 上升**（质量劣化）。
⇒ **「45% 与质量门槛在规模上互斥」**是本项目的一条实测结论，不是估计。

**实际已交付**（objective 中已达成并**超出**的部分）：
- `.board/` 多 agent 协作中枢 + 三常驻 agent + 角色委任书 → ✅
- **v0.8.0 已发布**：D1 缺陷修复（`packages/core/src/db/index.ts` `>= 2` → `>= 1`，267 误拆 → 0）
  + L4 质量守卫 + 覆盖率如实值；tag `v0.8.0` + Release（**仅源码 0 附件**，用户拍板）
- 测试 **226 → 506**（新增 `ru_morph.mjs` 60 + `ru_morph_d1fix.mjs` 194 + `ru_morph_d1guard.mjs` 26）
- 人工审定门按 **`DEC-004-R1`** 变更（`DECISIONS.md:92`，DEC-004 下的**执行方式修订块**，非替代）：
  agent 审定 + **可复核的联网词源来源**（用户保留事后否决权）——用户授权「自行审定」
  ⚠ 编号订正的经过（**两层错误，均由需求 agent 顶回**）：主管在 `BOARD.md` 的原指令要求落盘为
  **DEC-014**，后口头改称 `DEC-004-v2`，**却从未令其落盘** ⇒ 「引用不存在的决策编号」；
  而主管第二次指定的 **`DEC-014` 本身也已被占用**（`DECISIONS.md:253` 的「A2 45% 未达成」条，
  被 ≥4 处引用）⇒ 差一步造成「两个决定共用一个编号」。
  ⇒ **正名 = `DEC-004-R1`**；`DEC-004-v2` 与 `DEC-014` 两个别名**本台账均不采用**
  （别名对照段 `DECISIONS.md:94-96` 使搜索任一别名都能被纠正）。

**当前实际靶子（用户最新拍板 · 2026-09-13）**：**v0.9.0 = 规则 + 语义纠错补词素，目标 30 条全清**
（T22 判定「结构签名不可机器判定」后，用户**解除**「不预设补词素路线」约束；
不动任何阈值、不放宽位置规则、**不设覆盖率目标**）。
判定式 **`(30 − |H-d|) 红 → 0 红`**（**DEC-018**，预备稿 B）+ **AC-11 词素落库门**；
验收标准为 **AC-1…AC-11**（`.board/REQ.md` §6.2）。
⛔ 已被 T22 与裁决十七**关闭**的两条路：结构签名机器判定、放宽规则/阈值（VH-a′/a″，骗绿）。

### ⚠ 为什么主管部门不能自行改掉这个 goal

`update_goal` 的 `edit` **只允许在人类回合（direct human turn）执行**；在目标轮内调用报
`Error: this goal operation requires a direct human turn on a top-level agent`。
**经 `ask_user_question` 获得的用户选项答复不算人类回合**（已实测两次）。
⇒ **只能由用户亲自发一条普通消息**才能改。主管**不再重试**（`AGENTS.md` §4.1 熔断）。
**本节的作用**：即使 goal 文本仍是旧口径，**后续会话以本节 + 裁决十三为准**，不得据旧口径复燃「45%」目标。

### 进度快照（2026-09-16，第 6 轮）

| goal objective 的要素 | 状态 |
| --- | --- |
| 建立 `.board/` 多 agent 协作中枢 + 三常驻 agent | ✅ 达成（**已扩为 4 个常驻 agent**：2026-09-16 按用户指令新增**项目结构管理 agent**，见 §2 与 `.board/roles/structure-agent.md`） |
| **A2「覆盖率 30.8% → >45%」** | ⛔ **不可达，已被用户拍板取消**（见上） |
| 人工审定门（词素候选须经**用户**确认） | ✅ 达成，但**形式已按用户授权变更（`DEC-004-R1`）**：agent + **可复核的联网词源来源**，用户保留事后否决权（`DEC-004-v2` 与 `DEC-014` 两个别名**均不采用**） |
| 20 词正反例判别集全绿 | ✅ **已交付**：`packages/core/test/ru_morph_semantic.mjs`（T20，判词集 = 真族 10 + 假族 26 + `уч-`11 + `дн-`1 = **48 词**；§8b 区分能力经 4 变体机器验证） |
| 全量 **226** 项测试不回归 | ✅ **超额达成**：门禁已由 226 扩至 **506 通过 / 0 失败**（core 484 + desktop 22） |
| bump 版本 + 打包 + 发 Release **v0.8.0** | ✅ **已发布**（tag `v0.8.0` + Release，**仅源码 0 附件**）；v0.9.0 迭代进行中 |

⇒ **objective 中仅剩「20 词判别集全绿」未完成**（T20 进行中）；其余或已达成、或已被用户改向取代。
**改向后的当前靶子**：v0.9.0 的 **AC-1…AC-10**（`DEC-015`），其中 **AC-2 = `ru_morph_defects.mjs` 30 红 → 0 红**
（D4 6 + V9-2 24；**D2 3 红转 v0.10.0**），判定式见 `.board/TASKS.md` 与裁决十四·七。
