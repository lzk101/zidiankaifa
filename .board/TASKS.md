# 调度板 — 在飞任务

> **维护者**：项目主管。任何 agent 只读；如需变更请向主管提请。
> **最后更新**：2026-09-16（§11.11：T55 收口提交 `fe2da36` + T60 派发 + 结构治理与门禁互斥约束）

---

## 用户拍板（本轮，原文依据）

1. **迭代范围** =「**先修 D1 并发布，补词干放下一迭代**」
2. **审定方式** =「**能否从网上查找资料，自行审定**」（授权 agent 联网查词源自行审定）
3. 发布节奏 =「**每个迭代完成即直接 bump 版本 + 打包 + 发 Release**」（此前已拍板）

---

## 当前迭代 = **v0.8.0 「正确性与质量修复」**

**阶段**：✅ **v0.8.0 已发布收口**（AC-1…AC-7 全达成、T14 文档完成）→ 🚀 **v0.9.0 已派 T17 准入实验**

| # | 任务 | 负责人 | 状态 | 产出 |
| --- | --- | --- | --- | --- |
| T1 | 把 A2 的歧义裁定成可测验收标准 + 需求漂移对账 | 需求管理 agent | ✅ **完成** | `.board/REQ.md`(305行) · `DECISIONS.md`(13条) · `CHANGELOG.md` |
| T2 | 可行性判别实验（791 词分类、天花板量化） | 开发编程 agent | ✅ **完成** | `scripts/exp_ru_ceiling.mjs`(1145行，791词0例不一致) |
| T3 | 独立判别集 + 尺子 R 复核 + 缺陷发掘 | 功能测试 agent | ✅ **完成** | `packages/core/test/ru_morph.mjs`(54条) · `ru_morph_defects.mjs` |
| T4 | 需求漂移对账（F7/F10/F13 抽查 + 12 项表全量核验） | 需求管理 agent | ✅ **完成** | `.board/REQ.md` 对账小节（5 处差异，F1–F13 未漂移） |
| **T12** | D1 修复：判别实验 → 改 `index.ts`（`>=2`→`>=1`）→ build → 全量回归 | 开发编程 agent | ✅ **完成并验收** | `packages/core/src/db/index.ts:859` · `:848-858` 注释 |
| T10 | 把 `ru_morph.mjs` 接入 `pnpm test` | 开发编程 agent | ✅ 已接入（**exit 2 待 T16 解除**） | `packages/core/package.json:25` |
| T15 | 独立反核 D1 修法 8 项对照值 + 按订正后断言改写正/反例 | 功能测试 agent | ✅ **完成**（EVIDENCE.md 第 6–9 轮） | `.board/EVIDENCE.md`(84.8KB) |
| **T16** | **拆分 2 条 A2 目标断言到 `ru_morph_goals.mjs`，`ru_morph.mjs` 归 exit 0** | 功能测试 agent | ✅ **完成**（60 条恒绿 exit 0；goals 0/2 exit 2 待 v0.9.0） | `packages/core/test/ru_morph*.mjs` |
| T13 | bump 版本 → core/web 构建 → 打包 → commit/tag/push → `gh release v0.8.0` | 项目主管 | ✅ **已完成**（源码发布，无安装包） | commit `4c89272` · tag `v0.8.0` · [Release](https://github.com/lzk101/zidiankaifa/releases/tag/v0.8.0) |
| T14 | 补 `docs/需求总结.md` §21 + README Roadmap + 交接文档 | 需求管理 agent | ✅ **完成**（§21 全 8 小节；另订正 3 处账目） | `docs/` |
| T17 | **v0.9.0 准入条件实验**：内存注入候选词根，测「R 覆盖率 ↑ 且 全库 D1 计数不上升、全库空洞≥3 不上升」 | 开发编程 agent | ❌ **完成，判定【不通过】**（裁决十三：**v0.9.0 覆盖率目标取消**；合法候选上限 33.88% ≪ 45%） | `scripts/exp_v9_admission.mjs` · `scripts/out_v9_admission.json` · `scripts/out_v9_audit.json` |
| T18 | **V9-3 全库 D1 双向计数守卫** | 功能测试 agent | ✅ **完成并验收**（门禁 **506/0**；双向鉴别力经 4 变体实验证实） | `packages/core/test/ru_morph_d1guard.mjs`（26 条断言） |

### ★ T18 的前提数据（主管 2026-09-16 实测，供守卫定容差）
| 项 | 实测值 |
| --- | --- |
| 全库俄语词条 | **101,512** |
| 库内俄语前缀 | **185**，其中**单字符恰为 4 个：`в` `о` `с` `у`** |
| **全库 D1 模式计数** | **0**（双向守卫的主判据） |
| `gap≥2 且 gap∉前缀集` 残留 | **0** |
| 尺子 R 覆盖率 | **238/791 = 30.09%** |
| 全库可拆俄语词条 | **33,174**（修法前 33,441，Δ = −267） |
| **全库扫描耗时** | **8,971 ms（11,316 词/秒）** ⇒ 9 秒可接受，**不得**为省时降级为采样子集 |
| 真前缀组哨兵 | **`удачный`** = `{да-@1 + -ный@4}`，词源 `удача` = `у- + дача`；**R 内真前缀组唯一 1 词**，必须保持可拆 |

### T13 完成记录（2026-09-16）

| 项 | 结果 |
|---|---|
| 版本 bump | 0.7.1 → **0.8.0**（4 个 `package.json` + 3 处文本） |
| core build | ✅ `tsc -p tsconfig.build.json` exit 0 |
| sync-server build | ✅ exit 0，**`/health` 实测返回 `version: "0.8.0"`**（v0.7.1 遗留项 C1 闭环） |
| **web build** | ❌ **受限沙箱下无法完成**（esbuild 需管道 IPC，`spawn EPERM`；已用 4 个判别实验逐一排除绕行方案） |
| 门禁 | ✅ **480 通过 / 0 失败**（core 458 + desktop 22），exit 0 |
| commit | ✅ `4c89272`（97 文件；已剔除 `scripts/_tmp` 与 `.board/_tmp` 的临时引擎副本） |
| push | ✅ `055c753..4c89272  main -> main`，与 `origin/main` 差异 `0 0` |
| tag | ✅ `v0.8.0`（附注 tag）已推送 |
| Release | ✅ https://github.com/lzk101/zidiankaifa/releases/tag/v0.8.0 （非草稿、非预发布、**0 附件 = 仅源码**） |

**⚠ 本次 Release 不含安装包**（用户拍板）：`apps/web/dist` 未重建，仍是 v0.7.1 产物，
故 UI 关于页会显示「v0.7.1」而实际为 0.8.0。恢复正常发布只需在**普通（非受限）终端**跑：
```powershell
$env:ELECTRON_MIRROR='https://npmmirror.com/mirrors/electron/'
$env:electron_config_cache='D:\lzk17\Documents\zidiankaifa\.electron-cache'
$env:ELECTRON_BUILDER_CACHE='D:\lzk17\Documents\zidiankaifa\.eb-cache'
pnpm --filter @zidiankaifa/web build
pnpm --filter @zidiankaifa/desktop build
```
根因与完整处置见 `AGENTS.md` 第 3 节与 `docs/release-v0.8.0.md`。

### 阶段门（Gate）—— 已通过

```
T2/T3 判别实验结论
   └─ 45% 在「不动阈值」前提下不可达（硬上限 36.54%）→ 主管据实修正目标 ✅ 已执行
```
**完成情况**：主管**未**强行让开发 agent 硬凑 45%，而是把目标修正为「D1 修复 + 质量守卫 + 覆盖率如实值」，
并把补词干移到 v0.9.0。`AGENTS.md` §4.1 的 4 次调参回退教训未被重演。

### v0.8.0 验收标准（**取代**原「覆盖率 >45%」）

| # | 验收项 | 判据 | 实测 |
| --- | --- | --- | --- |
| AC-1 | 既有测试全绿 | core 204（99/67/38）+ desktop 22 = **226 通过 / 0 失败** | ✅ core 204/0 · desktop 22/0 |
| AC-2 | 独立判别集无回归 | `node packages/core/test/ru_morph.mjs` → **exit 0，0 回归** | ✅ **已达成**（60 条恒绿，exit 0；A2 目标已拆至 `ru_morph_goals.mjs`） |
| AC-3 | **D1 已修复** | 全库 D1 模式**假前缀组 267 词全部消除**；`плескание`/`хлестаться` 词族不再污染为「森林」 | ✅ **267 → 0**（修法前 342 = 75 保留 + 267 消除） |
| AC-4 | **无「修过头」** | 真前缀组 75 词**全部保留、0 误杀** | ✅ **75 → 75，0 误杀**（另一独立来源：R 内仅 6 词由可拆变不可拆，全为 D1 假词根） |
| AC-5 | **L4 质量守卫** | 空洞≥3 计数 **≤ 134**（口径 A：未覆盖字符总数） | ✅ **134 → 134**（口径 B 单段：129 → 129） |
| AC-6 | 覆盖率如实交付 | 尺子 R 实测值**如实报告**；L1 反塌陷守卫 `≥25%` 必须绿 | ✅ **30.85% → 30.09%**（−0.76pp，为消除误拆的预期代价）；L1 ≥25% 绿 |
| AC-7 | 不回归 | 无「可拆 → 不可拆」回退 | ✅ 全库**变可拆 = 0**；自洽性检验「gap=1 但 gap 字符 ∉ 前缀集却仍可拆」= **0 词** |

**明确非目标（本迭代不做）**：补词素（v0.9.0）· 放宽任何阈值或位置规则（已用两来源证据否决）· 覆盖率提升 ·
**D2（词中空洞无限）/ D4（假词根）/ D5-сучить（prefix 词素库语义未核证）** · 英语侧 gap=1 专项普查。

#### ★ 裁决七：D1 修复的范围界定与「剩余 16 条红」的定性（**发布前必须记档**）

`packages/core/test/ru_morph_defects.mjs` 修法后为 **23 通过 / 33 失败**（⚠ 原文误记为 11 通过 / 16 失败，
该数字来自 T15 第 6 轮之前的旧版本；**真值见 `.board/EVIDENCE.md:1074` 与主管 2026-09-16 实测**）。
逐条定性——**33 条红全部是 v0.7.1 既存缺陷，D1 修复未新增任何一个**（33 = D2 3 + D4 6 + D6/V9-2 24）：

| 类 | 条数 | 性质 | 裁定 |
| --- | --- | --- | --- |
| **D1** | 7 | 词首 gap=1 且 gap 字符**非**前缀 ⇒ 假词根 | ✅ **本迭代已全部修复（7/7 绿，D1 块 0 红）** |
| D2 | 3 | 词中空洞无上限（`землетрясение` 空洞 3「ряс」、`водопад`） | ⏸ 范围外（文档 intent ≤1，实现无限；属独立规则议题） |
| D4 | 6 | **词根语义误判**（`столп`→`стол-`、`казус`→`каз-`、`доминировать`→`дом-` 等） | ⏸ 范围外（靠**词素级词源知识**修正，非位置规则） |
| D6/V9-2 | 24 | 其中含 `суч-` 系**假词根**（`сучить`→`уч-` 等）与 75 词「真前缀 gap」语义核证 | ⏸ 范围外（与 D4 同类）；**D5 预言性守卫已 0 红** |

**★ D1 的范围定义与完成度（关键，防误导）**：
D1 缺陷的完整定义是「**词首 gap=1 且该 gap 字符 ∉ 库内前缀集**」。
在此定义下 **267/267 全部消除（100%）**，并由**四项独立检验**确认：
① 假前缀组 267 → 0；② 自洽性检验泄漏 = 0 词；③ 尺子 A 覆盖独立减少 267 词（与 ① 逐词吻合）；④ AC-4 真前缀组 0 误杀。
**⇒ D1 修复是完整且真实的，不是部分修复。**

**★ 已知残余（如实记录，非本迭代）**：修法后仍有 **75 词**呈「gap=1 但 gap 字符 ∈ {в,о,с,у}」形态，
其首片段分布 = **`да-`×63 / `уч-`×11 / `дн-`×1**。其中：
- `да-`×63（如 `удаваться` = `у + да- + -вать`）—— `да-` 来自 `дать`（给），对 `удаваться` 属**边缘可辩护**（`удача` 确含 `да-` 词源），但会与 `дача/дательный` 词族混同；
- **`уч-`×11（`сучить`/`сучок`/`сучковатый` …）与 `дн-`×1（`однако`）是真假词根污染**，与 D4 同类。

**裁定**：**不扩大本迭代范围**。理由：① 这 75 词**修法前就已可拆**（`gapExplain` 对 gap≥2 早已放行同类），**修法未新增它们**，
非回归；② 消除它们需新增「**词素级相等 + 词源一致性**」判据，属**新机制**而非本 bug 修复；
③ 与 D2/D4/D5 是同一条「词素语义核证」议题，拆开做会碎片化。
⇒ **升为 v0.9.0 的 P0 项，并写入 `ru_morph_defects.mjs` 留红**（红即待办，不得删除断言）。
**⇒ 发布 v0.8.0 时必须在 Release Notes 与 `docs/需求总结.md` §21 如实披露此残余**，不得沉默。

---

## 下一迭代 = **v0.9.0「词素语义核证（防假拆解）」** —— ★★裁决十三：**覆盖率目标已取消**

> **准入实验 T17 已执行完毕，判定【不通过】** ⇒ 原立项「补词首词根刷覆盖率」**取消**。
> 主管已逐项独立复核并接受（详见 `.board/BOARD.md` 裁决十三）。要点：
> - **①「全库 D1 计数」对补词根结构性恒 0**（`packages/core/src/db/index.ts:859` 只收 `kind==='prefix'`）⇒ 无鉴别力。
>   有鉴别力的替代量 = **「守卫退回修复前（`gapMin=2`）后的 D1 形态数」**，基线 **267**，
>   注入后实测 **2,183 / 802 / 4,145** ⇒ 注入实际**制造**了大量 D1 形态（被守卫否决），并**吃掉 R 增益 31/14 词**。
> - **② 空洞判据（★ 裁决十三·六 定稿）**：**口径A 与口径B 必须同时不得上升**（绝对计数）。
>   基线：口径A **20,214** / 口径B **18,762**（**全库** 101,512 词口径，**不是** T18 守卫冻的 R 样本内 134）。
>   ⚠ **两口径会反向**：机械达标 14 条（R 45.76%）→ 口径A **+3,666 ❌** 而口径B **−59 ✔**
>   （机制：2 字符片段把长空洞打散成多个单字符空洞 ⇒ 总数不降、最大单段降）⇒ **单看口径B 会误判**。
>   ⇒ 本节原写的「以**空洞率**为主判据」**已作废**（n=3 实测绝对计数与率同向，率的敏感性是尺度稀释假象）。
> - **③ 尺子 R 达 45% 属「假达标」**：机械可达 45.76%/45.13%，但 **91.9% 增益来自 2 字符片段**；
>   188 条即可刷到 R **97.22%**、A **87.38%**。逐字证据：`безаварийный → без- + ва- + -ный`（空洞 4）、
>   `Мальта → мал- + та`。⇒ **覆盖率是可被伪造的 KPI，不得单独使用。**
> - **审定门实测**：前 26 条（按边际增益降序）→ **通过 2 / 部分通过 8 / 否决 16（61.5%）**。
>   只有通过者可落库时 R = **30.85%（+6 词）**；通过+部分通过 10 条 → **33.88%（+30）**，距 45% **差 88 词**。
> - **★★ 我的「一举两得」假说 —— 更正为「机制成立、规模不够」（裁决十三·六）**：
>   T17 初版据此判「**证伪**」**过强**。终版五组叠加全库实测证明**机制真实存在**：
>   n=3（`япон`/`защит`/`звон`）口径A **−26** / 口径B **−26**；n=5（＋`гор`/`бог`）**−18** / **−29**、R **+6**、`loses` 零回归；
>   **但**掺入 8 条「部分通过」后**两条判据双双转正恶化**（**+377** / **+231**）。
>   ⇒ 真实否决理由是「**45% 目标与质量门槛在规模上互斥**」，**不是**「机制不存在」。
> - **★ 用户拍板（本轮）**：v0.9.0 **只做 V9-1/V9-2 防假拆解**，不补词素、不碰 `roots_ru.json`。

### 立项依据（数值，**已由 T17 实测取代** —— 旧估值作废留档）
| 项 | 数值 |
| --- | --- |
| ~~补词根达 45% 所需~~ | ~~**67～70 条**~~ → T17 实测**理想 greedy 下界 47 条**，但**合法候选上限仅 33.88%**，**45% 不可达** |
| 保守（只补多词共享词根） | 61 条 → **42.86%**（不可达 45%） |
| 达 60.6% 所需 | 约 **169 条**（174 锚定语素词中段去重 169，其中仅 61 个能解锁 ≥2 词） |
| 有效候选池噪声 | 3093 个候选中仅 **61 个能解锁 ≥2 词（2.0%）**；前 70 条中 **19 条（27%）实为后缀片段**，须审定否决 |
| 只修规则的零新增上限 | **36.54%（289/791）** —— 不足以达 45%，且放宽支撑规则会造出 `вдыхать={в-+-ать}` 空心骨架 |
| 审定方式 | **联网查词源自行审定**（用户授权），须附可复核来源链接 + 实测边际增益 |

### ★ 缺陷标签收敛表（**权威口径，先读这张表再看任何缺陷编号**）

`ru_morph_defects.mjs` 修法后 = **23 通过 / 33 失败**（共 56 条断言，exit 1；**33 = 红数**）。
同一块缺陷在历史上被起了**三个名字**，曾导致三份文档互相矛盾——现以本表为唯一权威：

| 权威标签 | 别名（历史遗留，勿再用） | 条数 | 内容 |
| --- | --- | --- | --- |
| **D2** | — | 3 | 词中空洞无上限（`землетрясение` 空洞 3「ряс」、`водопад`） |
| **D4** | — | 6 | 词根语义误判（`столп`→`стол-`、`казус`→`каз-`、`доминировать`→`дом-`） |
| **V9-2** | ~~D6~~（release-v0.8.0.md）、~~D5~~（裁决七） | **24** | `уч-`×11 + `дн-`×1 = **12 词 × 2 断言**，均有**硬词源反证**（`суч-` 源自 `*sučiti`） |
| **D1** | — | **0 红** ✅ | 本迭代已全部修复（7/7 绿） |
| **D5 块** | — | **0 红** ✅ | 预言性守卫，修法目标达成 |

**33 红算术核验**：3 + 6 + 24 = **33** ✅（`.board/EVIDENCE.md:1074-1075` 与主管实测一致）
**`да-`×63 不在此列**：需逐词查词源才能判定，测试 agent 依「宁可小也不小写错」**未为其写断言**（写了就是猜期望值）。

---

## ★★ v0.9.0 迭代计划（用户已拍板：**只做 V9-1/V9-2**）

> **用户拍板（本轮原文）**：「**按裁决十三：只做 V9-1/V9-2 防假拆解**」
> ⇒ v0.9.0 **不补词素**、**不碰 `roots_ru.json`**、**不动任何阈值**；目标是**消假拆解**，不是刷覆盖率。
> ⇒ 预期覆盖率**不升或微降**，这是**预期的正确代价**（同 v0.8.0 的 −6 词）。

### 迭代范围

| 项 | 内容 | 规模 | 门 |
| --- | --- | --- | --- |
| **V9-1** | **D4 词根语义误判**：`столп`→`стол-`（ столб 系）、`казус`→`каз-`、`доминировать`→`дом-`、`больной`→`боль-` 等 | 6 条红 | `ru_morph_defects.mjs` D4 块转绿 |
| **V9-2** | **75 词「真前缀 gap」语义核证**：`уч-`×11 + `дн-`×1 = 12 词真污染（`сучить`→`уч-`、`однако`→`дн-`） | 24 条红 | 同上 V9-2 块转绿 |
| **V9-8** | 判据① 的替代量（守卫退回 `gapMin=2` 后的 D1 形态数，基线 **342** —— ⚠ 原记 267 已订正，见裁决十五·五）**不得上升** | 1 条断言 | 新增 |
| **V9-5** | D2 词中空洞上限（文档 intent ≤1，实现无限） | 3 条红 | **范围待定，见下** |

### ⚠ 本迭代的**核心未知**（先做判别实验，再改代码）

**V9-1/V9-2 与 D1 修复的机制不同**：D1 是**结构缺陷**（两道守卫恰好都放过 gap=1），改一行即可；
V9-1/V9-2 是**语义缺陷** —— `breakdownWord` 是纯字符串匹配，**没有语义知识**，
它无法知道 `суч-` 不是 `уч-`、`столп` 不是 `стол-`。

⇒ **必须先回答**（判别实验，禁写实现）：**在什么机制层上能区分「真词根」与「字母巧合词根」，而不放宽任何阈值、不引入 4 次回退那类调参？**
候选机制假设（待实验判别，**不得直接实现**）：
- **H-a 词素级负向表**：给词素加「不得在何处前缀出现」的禁用规则（如 `уч-` 不得在 `с` 之后起于位置 1）；
- **H-b 词素级共现约束**：词素需与已知词根共现才生效；
- **H-c 词源链交叉核证**：用 `word_etymology.chain`/`text_en` 反查（`сучить` 词源为 `*sučiti` 单词根）；
- **H-d 不可行**：三者都不足以区分 ⇒ 如实报告「本迭代无法消除 V9-1/V9-2」，**转文档化**。

### 派单

| # | 任务 | 派给 | 门 |
| --- | --- | --- | --- |
| **T19** | V9-1/V9-2 的**机制判别实验**（只读，禁写 `src/**`）——回答 H-a/H-b/H-c/H-d 哪个成立、各自能消除几条红、代价是什么 | 开发 agent | 判别实验优先于调参（`AGENTS.md` §4.1） |
| **T20** | **V9-1/V9-2 的独立语义核证** —— 用词源库独立确认 `суч-`/`столп`/`казус`/`доминировать`/`больной` 的**真实词根**，并产出「真词根 vs 巧合词根」判别集 | 测试 agent | 断言须先于实现落盘 |
| **T21** | 需求侧：V9-1/V9-2 的**验收标准冻结**（含「修过头」反例 + 是否纳入 D2/V9-5） | 需求 agent | ✅ **完成并验收**（**DEC-015/016/017**；AC-1…AC-10 冻结；**D2 转 v0.10.0**；**AC-3 草案自相矛盾已被其修正**，主管已向 T19 发最高优先级更正） |

### 迭代验收标准（草案，待 T20/T21 细化后冻结）

- **AC-1** 门禁 **506 通过 / 0 失败** 不回归（core 484 + desktop 22）
- **AC-2** `ru_morph_defects.mjs` **33 红 → 0 红**（V9-1 6 + V9-2 24 + D2 3）
- **AC-3** **无「修过头」**：`смекать`、`удачный`、`вода` 等真前缀词**必须保持可拆**（v0.8.0 的 75 词真前缀组不得被杀）
- **AC-4** 全库可拆数 **不得低于 33,162**（T18 守卫 `breakableMin`）；尺子 R **≥238/791 = 30.0%**
- **AC-5** 全库空洞≥3 **口径A 与口径B 同时不得上升**（20,214 / 18,762）
- **AC-6** 判据① 替代量（`gapMin=2` 下的 D1 形态数）**不得高于 267**
- **AC-7** 覆盖率**如实值**（预期不升或微降）；**不设覆盖率目标**
- **AC-8** 发布：bump **0.9.0** → 三个 build → commit/tag/push → `gh release`（**用户拍板：每个迭代直接发**）
**:1152 EVIDENCE 第 9 轮**另有 1 条独立议题（主管曾报「d1fix 致 pnpm test 崩溃」，实测证伪、缺陷已于第 3 轮修复）。

### v0.9.0 的 P0 项（由本迭代裁决七升格，**必须先于补词素**）

| # | P0 项 | 规模 | 依据 |
| --- | --- | --- | --- |
| V9-1 | **词素级语义核证**：消除 D4 假词根（`столп`→`стол-`、`казус`→`каз-`、`доминировать`→`дом-`）+ `суч-` 系（`сучить`→`уч-`） | **33 条红**（D2 3 + D4 6 + V9-2 24） | `ru_morph_defects.mjs` |
| V9-2 | **75 词「真前缀 gap」的语义核证**：`да-`×63 / `уч-`×11 / `дн-`×1 —— 其中 `уч-`/`дн-` 12 词是真污染（**已固化为 24 条断言留红**） | 12 词 × 2 | 裁决七；EVIDENCE.md:1074 |
| ~~V9-3~~ | ~~**独立 D1 计数守卫**：把「gap=1 且 gap 字符 ∉ 前缀集却仍可拆」固化为断言~~ → ✅ **已完成（T18）**：`packages/core/test/ru_morph_d1guard.mjs`（26 条断言，双向；门禁 480 → **506**） | — | 裁决六·二(d) |
| V9-4 | `ru_morph_goals.mjs` 的 2 条目标（`вдыхать` 需 `дых-`、`безаварийный` 需 `аварий-`） | 2 条 | T16 拆分产出 |
| V9-5 | D2 词中空洞上限（文档 intent ≤1，实现无限） | `землетрясение`/`водопад` | `ru_morph_defects.mjs` D2 |
| V9-6 | **C3 倒排表缺口**（新增）：`morphemes(ru)` 441 vs `roots(ru)`154 + `affixes(ru)`277 = 431，**差 10 条**（疑 `word_count = 0` 未入倒排表） | 10 条 | 交接文档 §6-C3 |
| ~~V9-7~~ | **挖掘方法复盘（T17 产出）**：① `scripts/exp_ru_ceiling.mjs:147` 的 `>= (g.gapMin ?? 2)` **更新为生产值 `>= 1`**；② 头部标注过期；③ 复刻引擎必知 `packages/core/src/db/index.ts:691` 的**词素顺序 tie-break**（kind 分组 + 组内串长降序；漏掉 = 29 例不一致） → ✅ **主管已执行完毕**：`GATES_STRICT` 加 `gapMin: 1`；`buildMatchers` 加 `KIND_RANK` 排序；**修复后自检「不一致 0 例 ✔」exit 0**，复现基线 238/30.09% 与尺子A 33,174 | ✅ 完成 | T17 §41.6 |
| V9-8 | **判据替换（新增）**：把 V9-3 守卫的判据① 由「全库 D1 计数 == 0」**补上**「守卫退回（`gapMin=2`）后的 D1 形态数不得上升」—— 现判据对补词根结构性恒 0、**无鉴别力**（T17 实测注入后 2,183/802/4,145）。**⚠ 基线已由 267 订正为 342**（裁决十五·五：主管用临时副本法以**两份官方实现**权威测定——`gapMin=1` → 可拆 33,174 / D1 形态 75；`gapMin=2` → 可拆 33,441 / D1 形态 **342**；两实现差异 **267** = 被消除量）。**测量方法强制**：只能用「守卫退回变体」测，**不得用生产输出测**（生产已拒绝这些词，输出里只有 75） | 1 条断言 | 裁决十三·三；裁决十五·五 |
| V9-9 | **合法词干候选清单（新增，T17 终版产出）**：`япон`/`защит`/`звон` + `гор`/`бог` 五条 = 唯一经全库实测**两条判据同时改善**的组（n=5：口径A **−18** / 口径B **−29** / R **+6** / `loses` 零回归）。**状态：候选，未过审定门** —— `гор`/`бог` 有 Wiktionary 链接；**`япон`/`защит`/`звон` 本轮无链接来源** ⇒ **补链接（Wiktionary/词源词典）前不得落库**，`roots_ru.json` 一行未动 | 5 条 | 裁决十三·六 |

**风险（已记录，**已由 T17 实测证实为现实风险**）**：候选池与历史上被否决的 `analyze_ru_morphemes.py` TOP40 同源；
T17 的 26 条审定给出了直接证据——8 条「部分通过」清一色「真:<某词>；巧合:<另几个词>」，
例如 `кар`（真 `карканье`，巧合 `карасик`/`кардамонный`/`карт`/`скарн`，R+5 里只有 1 个是真的）、
`кон`（巧合 `конголезский` 专名）、`бел`（巧合 `рентабельный` ← 法语 rentable）。
⇒ **子串挖矿必然产出「对部分词真、对另一部分纯巧合」的条目，无法通过「附来源」的审定门。**
**落库前必须逐条经词源证据审定**（T17 的 26 条审定表**不是落库清单**，无一条达可落库标准）。

### v0.9.0 的自限制（T17 主动声明，如实记档；不构成翻案理由）
候选池只挖自 **R 缺失词的头部子串**（1,992 条），**未纳入全库 68,338 个不可拆词**；
且 1,966 条未审定。⇒ **33.88% 是「该挖掘方法下的上界」，不是「R 的可达上限」**。
若未来仍要走这条路，须先换挖掘方法（全库不可拆词 + 词源多源交叉）并重新过准入；
**当前证据不支持继续投入**（见新增 **V9-7**）。

---

## ★★ v0.9.0 范围变更（裁决十七 · 2026-09-13 · 用户拍板）

> 本节**取代**上方「迭代验收标准（草案）」表中与 AC-2/AC-3 冲突的旧文案。
> 正式 AC 文本以 `.board/REQ.md` §6.2 + **DEC-018**（需求 agent 落盘中）为准。

### 一、T22 结论：结构签名路线**关闭**（数学级不可达）

| 证据 | 结果 |
| --- | --- |
| `§3` 碰撞组 | 恰 **1 组**：`удачный`(真,H1=`ч`,候选`дач`) vs `ударный`(假,H1=`р`,候选`дар`)，除空洞字母外全部结构特征逐项相同 ⇒ 纯结构判据必然错一边 |
| `§4` 暴力搜索 | **2324** 条特征合取规则，最佳 **79.2%**（`lastSegSuffix→判假`，真族判真 **0/10**） |
| `§5` 全库 veto | 可拆 33,174→**33,117**（−57）；杀 gap=1 组 75→**18**；**误杀哨兵 `удачный`**；R 238→**237**(29.96%) ⇒ **踩穿 `ru_morph_d1guard.mjs` 的 `rRateMin:0.30`**；L4 134 未踩穿 |
| `§6` 附带更正 | T20「真族空洞是 `ть`」对 `удачный`/`удачливый`/`сдаточный` **不成立** |
产出：`scripts/exp_v9_signature2.mjs`（340 行）· `scripts/out_v9_signature2.txt`（90 行）· 主管亲自运行 exit 0，`§0` 保真 0 例不一致。

### 二、用户拍板（原文，两项）

1. 迭代范围 =「**规则 + 语义纠错补词素：目标 30 条全清**」（**解除**「不预设补词素路线」约束）
2. AC-2 口径 =「**落盘 DEC-018 预备稿 B：条件式 (30 − |H-d|) 红 → 0 红**」

### 三、目标红计数（权威：**30 条** = D4 6 + V9-2 24）

| 块 | 断言数 | 现绿/红 | 归属 |
| --- | --- | --- | --- |
| D1 | 7 | 7 / 0 | — |
| D2 | 3 | 0 / 3 | **移 v0.10.0**（AC-2 原文） |
| D3 | 2 | 2 / 0 | — |
| **D4** | **6** | **0 / 6** | 本迭代目标（D4a 3 + D4b 3） |
| D5 | 2 | 2 / 0 | — |
| **V9-2** | **36** | **12 / 24** | 本迭代目标（11 词×3 + `однако`×3；第 3 形态 `!atWordStart` 12 条**恒绿**） |
| 合计 | **56** | **23 / 33** | 目标 **30** = 33 − D2 3 |

### 四、本迭代可行的两条手段（均已实测，唯一组合）

| 手段 | 实测效果 | 副作用 | 护栏 |
| --- | --- | --- | --- |
| **H-a 两条人工负向规则**（`уч-` 不得在 `с` 后起于 pos=1；`дн-` 不得在 `о` 后起于 pos=1） | 消 **12/30**（`суч-` 全族 11 词 + `однако` 2 条） | **零**（全库 33,162、真前缀组 75→63、`да-` 63 全保、R 238 不变、L4 134 持平） | ✅ 六项全过（T19-C） |
| **语义纠错补词素**（补**真词根**替换被误配的假词根） | 待 T24 实测（VH-e4 补 8 条时 33 红→4） | 须动 `roots_ru.json` + 重建词素表；候选须逐条附联网来源 | 待 T24 全库差分测 |
| ~~结构签名机器判定~~ | **关闭**（T22） | — | — |
| ~~放宽规则/阈值（VH-a′/a″）~~ | **禁止**（T18 实测骗绿：L4 134→163 踩穿） | — | — |

### 五、派单（本轮）

| # | 任务 | 派给 | 状态 |
| --- | --- | --- | --- |
| **T22** | 判别分类器实验（结构签名能否机器判定） | 开发 agent | ✅ **完成 · 判定不通过**（裁决十七） |
| **T24** | 只读判别实验：H-a 基线 / 词素候选全枚举（含「整词兜底型」单列）/ **全库 101,512 逐词差分** + 六护栏 + 判据①替代量 / `\|H-d\|` 下限 | 开发 agent | ✅ **完成 · 已裁决**（见下方「T24 回执与主管四项裁决」） |
| **T25** | 落盘 **DEC-018**（AC-2 条件式 B 版）+ `REQ.md` §6.2 更新 AC-2/AC-9③ + **新增 AC-11 词素落库门** + DoD **D11** | 需求 agent | ✅ **完成**（DEC-018 @ `DECISIONS.md:331`；AC-2 @ `REQ.md:391`；AC-11 @ `REQ.md:441`；**顶回主管 5 处口径问题**） |
| **T26** | 编号正名（`DEC-004-R1`）+ `да-` 表述纪律统一 + AC-11(f) 可测判据 | 需求 agent | ✅ **完成**（`DEC-004-R1` @ `DECISIONS.md:92`；**顶回主管 `DEC-014` 撞号**） |
| **T27** | 开发 agent：`P_recommended` 最终组合的完整六护栏 + **口径A/B 绝对值与率** + loses/gains + 余红与 `\|H-d\|` 终值 | 开发 agent | ⏳ **进行中** |
| **T28** | 独立验证最终修法（反例族 + 六护栏 + 全库差分复核 + **H-d 名单独立复核**） | 测试 agent | ⏸ 待 T27 回执后派 |
| **T29** | 主管：`roots_ru.json` diff **逐条复核来源链接**后合并 → 重建词素表 → 跑齐门禁 | **项目主管** | ⏸ 待 T27 |
| **T30** | 需求 agent：按 T27 实测填实 AC-3/AC-4 占位 + **AC-5 改双口径** + AC-6 改「只报不判」 | 需求 agent | ⏸ 待 T27 |
| **T31** | 发布：bump **0.9.0** → 三个 build → 判据全过 → commit/tag/push → Release | **项目主管** | ⏸ |

**判据① 替代量基线 = 342**（`gapMin=2` 退回变体下的 D1 形态数，**未加 H-a**）；
⚠ **已由主管裁决降级为「只报不判」**（实测非单调，342→330→334→336）；AC-6 改为「只报值 + 逐项解释回升」。

### 六、★★ 方法纪律：`да-` 族**不得用规则式禁令**（与 H-a 有本质区别，防后人重蹈）

| | **H-a（可用于 `уч-`/`дн-`）** | **❌ 不可用于 `да-`** |
| --- | --- | --- |
| 规则形态 | 词素级：**词素 `уч-` 不得在 `с` 后起于 pos=1** | 位置+形态级：`да-` 不得在 pos=1 |
| 为何可行 | `уч-`（教/学）与 `суч-`（节/枝）**是两个不同的词素**，禁令只打假的那个 | `да-` 在真族（`вдаваться`=`в-|да|+ть|-ся`）与假族（`вдавить`=`в-|дав|-и|+ть`）里**是同一个词素字符串** |
| 误杀风险 | **零**（实测 75→63，`да-` 63 全保，R 238 不变） | **杀 `удачный`**（AC-3 四哨兵之一）⇒ R 238→237、率 **29.96%** ⇒ **踩穿 `r_morph_d1guard.mjs` 的 `rRateMin:0.30`**（T22 §5 已实测此路径） |
| 唯一正解 | 规则式禁令 | **补入真词根** `дав-`/`дар-`/`дал-`/`долж-`/`удар-`——让 DP 找到一个**更长的真词根**，从而自然不再选中 `да-` |

**⇒ 铁律**：凡是「真假例共用同一词素字符串」的缺陷，**只能靠补入更长的真词素/真词缀**，
不能用「禁止该词素出现在某位置」的规则——后者必然误杀真例，且会被三道「防多杀」护栏
（`truePrefixMin` / `daCoreMin` / `l4Hole3Max`）拦下。**这是 H-a 与 `да-` 族的分水岭。**

### 七、熔断记录追加

| 事件 | 处理 |
| --- | --- |
| 主管在 T24 派单前，把 T19-C 的 `EX_ALL`（`scripts/exp_v9_criterion_d.mjs:97`：`суч-`/`суд-`/`-ной`/`пад-`/`тряс-`/`столп-`/`стат-`）当作「可落库候选」引用 | **主管自查发现并作废**——它们是**未经来源核证的注入件**（只为测护栏反应而造），仅作线索交 T24 独立验证 |
| T22 开发 agent 产出脚本后**未回执**（已 `ready`） | 主管**亲自运行其产物**取得权威结论（exit 0 + 90 行输出已落盘），**不以「未回执」为由搁置**；同时如实记档「agent 未回执」这一事实 |
| 主管两次**决策编号失误**（① 引用 `DEC-004-v2` 却从未令其落盘；② 第二次指定的 `DEC-014` **本身已被占用**） | 均由**需求 agent 顶回**（T25/T26），主管**全部采纳**；正名 **`DEC-004-R1`**（`DECISIONS.md:92`）。教训：**派单指定编号前必须先查号是否已被占用**；执行方**有权且有义务**核对并顶回 |
| 主管把「判据①替代量」当作**单调改善量**（原话「本轮应下降」） | 开发 agent 实测 **342→330(H-a)→334→336**，其中回升的 6 项里 **4 项是因 `а-` 前缀不在库而被守卫拒掉的正确切分** ⇒ 主管裁定**降级为「只报不判」**，AC-6 改为「只报值 + 逐项解释回升」。**根因同前：拿非单调量当单调量** |
| 主管以「`-ной` 使口径A/B +71/+73」为由将要否决该候选 | 主管**亲自跑反事实探针** `scripts/probe_sup_bolnoy_mincost.mjs` 推翻自己：`-н` 单独加**零效果**（⇒ `-ной` 非兜底条目）、**0 词变差 / 87 词变好**、**率下降**（口径A −0.1087pp / 口径B −0.0796pp）⇒ 绝对值上升是**分母膨胀（+176 词）**的算术结果。⇒ **判据升级为「绝对值 + 率」双口径**，`-ной` 全权放行 |

---

## ★★ T24 回执与主管四项裁决（2026-09-13）

**T24 = 开发 agent 只读判别实验**，引擎为 `scripts/exp_ru_ceiling.mjs:44-120` 逐行复刻（该文件**无 export 且顶层即执行**，`import` 会重跑 ⇒ 必须复刻），
**§0 中止式保真自检在尺子 R 791 词上 0 例不一致**（6 个脚本全过）。脚本在 `scripts/_tmp/exp_t24{,b,c,d,e,f}.mjs` + `exp_t24_src.mjs`，日志 `out_t24*.txt`。
**`data/db/dict.db` 保持 2026-09-13 05:37:43 / 494.23 MB ✔**

### A) H-a 单独效果（基线）

| 指标 | V0 | H-a | 判据 |
| --- | --- | --- | --- |
| 全库可拆（≥33162） | 33,174 | **33,162** | ✅ |
| 真前缀组（≥63） | 75 | **63** | ✅ |
| `да-` 子集（=63） | 63 | **63** | ✅ |
| 尺子 R（≥238） | 238 | **238** | ✅ |
| 率（≥0.30） | 30.09% | **30.09%** | ✅ |
| L4（≤134） | 134 | **134** | ✅ |
| 口径A（绝对值） | 20,214 | 20,210 | ✅ |
| 口径B（绝对值） | 18,762 | 18,758 | ✅ |
| 判据①替代量 | 342（未加 H-a） | 330 | ⚠ **只报不判** |

- **30 条红 → 剩 18 条**（消 12，恰为 12 条 `.i`）
- 逐词差分：**新增可拆 0 个**；由可拆变不可拆 **12 个**（**全为靶词**：`однако` + `суч-` 全族 11 词，**零附带**）
- 哨兵逐字不变：`вдаваться`/`одалживать`/`сдабривать`/`удачный`/`смекать`/`вода`（H-a 前后输出完全一致）

### B/C) 组合包净效应（全库 101,512 逐词差分）

| 包 | 可拆 | Δ | 口径A | ΔA | 口径B | ΔB | 红余 | 判据① |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| V0（H-a） | 33,162 | — | 20,210 | — | 18,758 | — | 18 | 330 |
| **P1 = `суч-` `суд-` `стат-` `домин-`** | 33,300 | +138 | 20,191 | **−19** | 18,731 | **−27** | **4** | 334 |
| **P2 = P1 + `столп-` `казус-` `однако-`** | 33,299 | +137 | 20,190 | **−20** | 18,731 | **−27** | **1** | 336 |
| P3 = P2 + `-ной` | 33,475 | +313 | 20,261 | +51 | 18,804 | +46 | **0** | — |

- **P1/P2 六护栏全过**：真前缀组 63 ✅ · `да-` 63 ✅ · R **241** ✅ · 率 **30.47%** ✅ · L4 **132** ✅
- ⚠ **单条 `суч-` 会踩红线（+7/+7）**，但在 P1 里被 `суд-`(−13/−10) 与 `стат-`(−10/−23) **吸收 ⇒ 净 −19/−27 ✅** ⇒ `суч-` **必须与 `суд-`/`стат-` 同批落库**
- **`да-` 族补词素路线确认会误伤真族**：注入 `дав-`/`дар-`/`дал-` 改变 23 词，**其中 4 个真族词被改**（`вдаваться`/`сдавать`/`сдаваться`/`удаваться`：`{да-@1 -вать…}` → `{в-@0 дав-@1 -ать@4 …}`）⇒ **不宜采用**（P1/P2 未含它们）

### D) `loses` 逐词定位（真回归）

- **P1：1 条** —— `экстатический: экс-@0-3 -ческий@7-13 → []`，**单加 `стат-` 即致**（`стат` 在 pos 3 竞争）
- **P2：再 3 条** —— `поднаковальня`、`поднакопить`，**成因均为单加 `однако-`**（`однако` 假配于 `под|однако|вальня`、`под|однако|пить`）
- gains：P1 **139 词** / P2 **140 词**；parts 变但未丢拆解 118 / 124；全量落盘 `scripts/_tmp/out_t24d_diff.txt`（75 KB）

### ★★ 主管四项裁决（均已发回开发 agent）

| # | 裁决 | 依据 |
| --- | --- | --- |
| 一 | **判据①替代量降级为「只报不判」**；AC-6 改为「只报值 + 逐项解释回升」。原话「本轮应下降」**作废** | 实测非单调（342→330→334→336），回升 6 项中 **4 项是缺 `а-` 导致的正确切分被拒** ⇒ 非改善量。**另立**：`а-` 缺失记为 **v0.10.0 候选**，本迭代不补 |
| 二 | ★★ **口径A/B 升级为「绝对值 + 率」双口径**；`-ной` **全权放行** | 主管亲跑 `scripts/probe_sup_bolnoy_mincost.mjs`：`-н` 单独加 **Δ=0**（⇒ `-ной` 非兜底）；**0 词变差 / 87 词变好**；**率下降**（A −0.1087pp / B −0.0796pp）⇒ 绝对值 +71/+73 是**分母膨胀 +176** 的算术结果。判据：**率不得上升**；绝对值若为正**必须 Δ率 ≤ 0** |
| 三 | **采纳 `\|H-d\| = 0` 档**：允许 `столп-`/`казус-`/`однако-` 三条**兜底条目**（须单列 + `origin` 注明 + 三条来源链接 + 附带损失清单 + Release Notes 披露）；**`больной-` 兜底不采用**（`больной`=`боль + -ной`，兜底即语义撒谎），走 `-ной` 正解 | `однако-` 的附带损失按「**由假拆解转为不拆**」记档（`поднаковальня`/`поднакопить` 现状本就含假词根 `дн-`）⇒ 是**净改善**而非代价 |
| 四 | `доми-`/`домин-` **最后一次联网取证**（取 ru.wiktionary `доминировать` 的 `{{морфо-ru}}` + `больной` 的 `{{морфо-ru}}` 加固 `-ной` 来源）；**不成即计入 H-d，`\|H-d\| = 1`** | 本地词源表 parts 只有 `["dominieren"]`、**无词根 part** ⇒ 判**无来源**（与作废 `EX_ALL` 同类） |
| 五 | T24 脚本**保留在 `scripts/_tmp/`**，不提升为 `scripts/exp_t24_*.mjs` | 归档价值在回执与裁决，不在脚本本体；省一次重跑验证 |

### ★ 判据①口径澄清（本项目**第五次**口径陷阱）

`342` = `gapMin=2` 退回变体下「`gap=1` 且片段数 ≥2」的全库词数（**未加 H-a**，主管权威值）；
`330` = **加 H-a 之后**的同口径值（H-a 降 12）。**两者不可混用**——前后对照必须同为「未加 H-a」。
开发 agent 首版曾把该量算成「`gapMin=2` 下**全部**可拆词 33,429」，**自行发现并改正**。

---

## 已完成（主管执行）

| # | 任务 | 产出 |
| --- | --- | --- |
| D1 | 落地多 agent 协作架构（三常驻 agent + 文件中枢 + 角色章程） | `PROJECT_CHARTER.md` · `.board/roles/dev-agent.md` · `.board/roles/test-agent.md` |
| D2 | 基线核验（与交接文档完全一致） | HEAD `055c753`、工作区干净、226 项全绿、词库 494.23MB |
| D3 | 发现并量化「三把尺子」口径混乱 | `.board/BASELINE.md` §4（R 30.8% / A 32.9% / B 38.1%） |
| D4 | 发现 `measure_ru_breakdown.mjs` import 路径损坏 | `.board/BASELINE.md` §5.1 |
| D5 | 三把尺子合一探针 | `scripts/probe_ru_coverage.mjs` |
| D6 | **发现并量化「词首关卡」机制**（三关：首片段 / 单字符前缀支撑 / 覆盖率阈值） | `scripts/exp_ru_head_gap.mjs` · `exp_ru_attribution.mjs` |
| D7 | **独立复核 D1 缺陷（7/7 确认）与 D3 伪拆解口径（175/72%、134）** | `scripts/probe_ru_d1_impact.mjs` · `probe_ru_gap_audit2.mjs` |
| D8 | **验证「一举两得」假说**（补词干同时提升覆盖率与消除空洞） | `scripts/exp_ru_dual_win.mjs` |
| D9 | 两次**自我订正**（撤回两版错误机制描述与"高频词根是突破口"猜想） | `.board/BOARD.md` ★★订正节 / ★裁决二 |
| **D10** | ★★ **T27 定稿表三项独立查证**：① 「真前缀组 63→9」= **度量变形非损失**（66 词首字符本就是真词根首字母）② **`стат-` 是真坏候选**（丢 `экстатический` + 68 词 parts 变化，仅收益 `термостат` 1 条）③ **`термостат-` 兜底是我引入的错误**（吃掉已存在的真前缀 `термо-`，且使 D4.6 断言**侥幸变绿 = 假绿**） | `scripts/probe_sup_v9_placement_audit.mjs` · `probe_sup_v9_decide.mjs` |
| **D11** | ★★ **T29 落库完毕（v0.9.0 实施，2026-09-16）**：`roots_ru.json` 441→**450**（root 171/suffix 94/prefix 185）+ 词素表重建 + 倒排表重建；**`packages/core/src/**` 一行未改** | `scripts/apply_v9_morphemes.mjs` · `apply_v9_morphemes2.mjs` · `probe_sup_v9_final_audit.mjs` · `out_sup_v9_final_audit.txt` |
| **D12** | **落库后独立端到端复核**：**loses = 2**（⚠ **主管原报「loses 0」已由 T28 独立实测推翻并订正**，见下方 ★★ 订正节）· gains 323 · changed 216（算术自洽 33,174+323−2 = 33,495 ✔）· 可拆 33,495 · 空洞≥3 口径A 20,243（率 **60.4359%**，Δ−0.4974pp）/ 口径B 18,799（率 **56.1248%**，Δ−0.4315pp）· 真前缀组 63（**⚠ 混合计数**，见下）· `да-` 63 · R 241（30.47%）· L4 132 · D1 0 | `scripts/out_sup_v9_final_audit.txt` · `scripts/probe_t29_verify.mjs` |
| **D16** | ★★ **T28 独立验证（测试 agent）——推翻主管 1 处 + 补 3 处口径**：① **`loses = 2`**（`поднаковальня` / `поднакопить`，成因 = `однако-` 兜底）② **口径A/B 上升全部源自 `-ной` 一条**（留一法 +70/+71；其余 7 条全为负）③ **`-ной` 空洞变少我测 263**（= 176 新可拆 + 87 已可拆但空洞缩小；主管报 90 = AC-11(c) 子口径）④ **`gains 176` 是单条值、`323` 是 9 条合计**，须标口径 | `.board/EVIDENCE.md` §43.1–§43.15（1970 → **2248 行**）· `scripts/probe_t28_baseline.mjs` · `probe_t28_variants.mjs` · `probe_t29_verify.mjs` · `probe_t29_contrib.mjs` · `probe_t29_noi_check.mjs` |
| **D17** | ★★ **T28 方法学产物：v0.8.0 基线引擎重建法**（因落库后基线不可复现）——用基线原件 `roots_ru.json.bak-2026-09-13T13-46-08-977Z`（108,262 B / mtime 05:32:31）+ 三份 `dist` 副本 + 三个钩子（`__setInjected`/`__setBlock`/`__setReplace`），**算法逐字节不动**；保真自检：`__REPL=null` 时与生产 dist 在**全库 101,512 词** `parts` 不一致 **0**；重建自检六项全中（33,174 / 20,214 / 18,762 / 75 / 63 / R 238 / L4A 134） | `scripts/_tmp/eng_t28{,_pre,_v080}/` |
| **D18** | ★★ **T28 新机制发现**：`H-a` 两条负向规则在 P2/P3 之上**全库逐词差异 = 0**（`суч-`/`однако-` 落库后 `уч-@1`/`дн-@1` 直接变 `суч-@0`/`однако-@0` ⇒ 规则无事可做）⇒ **路线甲（规则）与路线乙（补词素）不互补，在 P2 之上规则贡献恰为 0**，`H-a` 属可省冗余复杂度 | `.board/EVIDENCE.md` §43 |
| **D13** | **C3 成因查明**（v0.8.0 遗留项）：`build_roots_tables.mjs` 只收「有真实关联词」的词素。**构成已由 T30 订正**：**ru 缺 1（`о-`）＋ en 缺 9（`al-` `auto-` `circum-` `hyper-` `mal-` `ment` `multi-` `omni-` `un-`）** ⇒ **勿再用「ru 差 10 条」** | `packages/core/test/lexicon.mjs:122-141` · `REQ.md` §7 B-2 |
| **D14** | **需求整理（用户指令「整理需求」）**：闭项 §0.3 + 新增 §7「v0.10.0 需求整理」。**⚠ 越界写入需求 agent 独占写域，已报备；T30 复核后订正 10 处**（日期 · 真前缀组 63 混合计数 · AC-2 改 🟡 · A-1「3 条红」过时改 3/0 · B-3「53/3」过时改 55/1 · B-2 构成 · **§7.4 序改 A-3 第 1** · **§7.5 三条→六条** · C-2 错字） | `REQ.md` §0.3 / §7（**现归需求 agent 定稿 710 行**） |
| **D15** | **T30 独立复核（需求 agent）**：AC 账本收口 + §0.3/§7 逐行复核；**DEC-019**（v0.9.0 AC 收口与三处改判）· **DEC-020**（v0.10.0 准入条件与排序，瘦身版）· 新增 **Q6** | `REQ.md` 710 行 · `DECISIONS.md` 442 行 · `CHANGELOG.md` 123 行 |

### ★★ 监管订正（T28 独立验证推翻主管 1 处，采纳）

**主管原报「loses **0** ✅ 零回归」→ 订正为 loses = **2**。**
- 我原探针的差分基线取在**落库后**，而 `поднаковальня`/`поднакопить` 在落库**前**已是 `[]`（其在更早的 `однако-` 试注入期由可拆变不可拆）⇒ 差分看不见这两条 ⇒ **误报 0**。
- 测试 agent 用**基线原件重建 v0.8.0 引擎**（`scripts/_tmp/eng_t28_v080/`，保真自检全库 101,512 词 `parts` 不一致 **0**）后测得 **loses = 2**；**主管已独立复跑 `scripts/probe_t29_contrib.mjs` 确认 = 2** ✔
- 涉及词：`поднаковальня`（旧 `по-@0-2 дн-@2-4 -ко@5-7 -льня@9-13` → `[]`）· `поднакопить`（旧 `по-@0-2 дн-@2-4 -ко@5-7 -ить@8-11` → `[]`）
- **定性（双方一致）**：旧态是「**假词根 `дн-` + `-ко` 碎片**」双重错误拆解（`дн-` = день「日」，而 `наковальня` 词源 chain = `["накова́ть","-льня"]`，真词源是 ковать「锻造」）⇒ **由假拆解转为不拆 = 净改善**；**但 `loses` 是计量量，两件事必须并存登记**，不得因定性为改善就把计量值写成 0（这正是 `DECISIONS.md:418-③` 要求 `loses = 0` 的判据冲突点）。

### ★★ 裁决十八 · AC-11(c) 正式豁免 + 判据缺陷认定（主管 T29 末）

**背景**：T28 留一法实测显示 —— 全库空洞≥3 **口径A/B 的上升（+29/+37）全部源自 `-ной` 一条**（去掉它口径AΔ **+70**、口径BΔ **+71**；其余 7 条全为**负**）。剔除 `-ной` 即 AC-11(c) 满足，代价 = `больной` 留红（`|H-d|` 1 → 2）。

**裁定：保留 `-ной`，并认定 AC-11(c) 的「绝对值」判据存在缺陷，正式豁免。** 理由三条：
1. **DEC-002「正确性 > 覆盖率」**：剔除 `-ной` 会让 **176 个词由可拆变不可拆**（含 `больной`/`внеземной`/`входной`/`выводной` 等真词），且 `больной` 回到留红 ⇒ **以正确性换一个被误写的判据，方向错误**。
2. **剔除 `-ной` 的「AC-11(c) 满足」是分母缩小的算术结果，不是质量改善**：`-ной` 使 176 个原本**不可拆**的词变为可拆，这些词带来的**残差空洞计入全库总数** ⇒ **任何「让 N 个不可拆词变为可拆」的词素都必然推高空洞绝对值**。故「空洞总数不得上升」作为 **per-morpheme 判据在结构上不可满足**，属**判据缺陷**而非条目不达标。
   （对照：同一 `-ной` 在**固定分母**下的率是 **下降**的 ⇒ 与 AC-5 双口径结论方向一致；剔除 `-ной` 后绝对值虽降，但**率同样下降**，两者不可区分谁「更好」。）
3. `-ной` 的三条实测证据均成立：**新增可拆 176 · 变差 0 · 空洞变多 0**，且来源可复核（本地 chain `больной=["боль","-ной"]`；ru.wiktionary 作 `боль|-н|+ой`，两源实质相容）。

**据此**：
- **v0.9.0 的 AC-11(c) 与本条一并视为「豁免放行」**（与 AC-5 同性质），**须在 Release Notes 明示**；
- **v0.10.0 起 AC-11(c) 改为三条**：① **变差 = 0 词**（不得由可拆变不可拆）② **空洞变多 = 0 词** ③ **固定分母下的率不得上升**（绝对值仅作报告项，不作判据）。

### ★★ 三条不得误读的口径（T30 订正，引用前必读）

1. **「真前缀组 63」是混合计数** = **真族 10 ＋ 假族 26 ＋ 27 条不可判定**（27 条因库内无 `chain[].parts`）。**不得读作「63 词全部正确」**；它作为**地板常量**有效（防「修过头」把真族一起杀掉），但**不得当作「正确性已达成」的证据**。（此项推翻我在 §0.3 里按「✅ ≥63」写的表述——**即把 T20 已推翻的前提当成了通过依据**，属本项目同类错误第 6 次。）
2. **AC-5 属「豁免放行」，不是「通过」**：空洞≥3 **绝对值上升**（口径A +29 / 口径B +37），仅**率**下降（−0.4974pp / −0.4315pp），且率降**含分母长大的机械成分**（可拆 33,174→33,495，+321）。留痕：**旧判据 = 不通过，新判据 = 通过** ⇒ 按裁决二的「绝对值 + 率」双口径放行。**须在 Release Notes 明示；v0.10.0 不得沿用同一豁免。**
3. **「30 条全清」不得宣称**：AC-2 为**条件式**且现为 🟡（`|H-d| = 1`，余红 `термостат`）；H-d 四件证据（DEC-016）未出 ⇒ **待 T28 + 用户拍板后方可定**。
4. **D2 三条断言已绿 ≠ 机制已修**：D2 随落库 `пад-`/`тряс-` **顺带转绿**（点状断言），但**词中空洞的通用上限仍不存在** ⇒ A-1 仍留 v0.10.0，须补**通用上限断言**（非点状）。

---

## 任务派单记录

| 轮次 | 时间 | 派给 | 内容 | 回执 |
| --- | --- | --- | --- | --- |
| R1 | 2026-09-16 | 需求 agent | T1 + T4 | ✅ 完成（13 条决策 + 5 处对账差异） |
| R1 | 2026-09-16 | 开发 agent | T2（仅判别实验，禁写实现） | ✅ 完成（825 行复刻引擎，0 例不一致） |
| R1 | 2026-09-16 | 测试 agent | T3（独立判别集 + 基线复核） | ✅ 完成（54 条断言 + D1–D5 缺陷） |
| R2 | 2026-09-16 | 测试 agent | 补充情报：重心转向词首 + 真假前缀判别 | ✅ 完成（3 轮回执，撤回自己的 2 个错数） |
| R2 | 2026-09-16 | 开发 agent | 补充情报：订正机制 + 交叉验证 | ✅ 完成（逐项命中 8/8） |
| **R3** | 2026-09-16 | **开发 agent** | **T12：D1 修复（先判别实验）** | ✅ 完成（`index.ts:859` `>=2`→`>=1`，267→0） |
| **R3** | 2026-09-16 | 测试 agent | T15/T16：独立反核 + 拆分目标断言 | ✅ 完成（EVIDENCE.md 第 6–9 轮；60/194 恒绿） |
| **R4** | 2026-09-16 | **项目主管** | T13：bump + build + commit/tag/push + Release | ✅ 完成（commit `4c89272`+`55f8745`，tag 与 Release 已发） |
| **R5** | 2026-09-16 | 需求 agent | T14：§21 + 交接文档 + 需求台账 | ✅ 完成（§21 全 8 小节；另订正 3 处账目） |
| **R5** | 2026-09-16 | **项目主管** | 文档合并 `8ef5a0a` + 缺陷标签收敛 | ✅ 完成（`docs/release-v0.8.0.md` 别名同步） |
| **R6** | 2026-09-16 | **项目主管** | 裁决十三更正（「机制被证伪」→「目标与门槛在规模上互斥」）+ T17 产出提交 | ✅ 完成（commit `3df49d3` `8f17e0a`） |
| **R6** | 2026-09-16 | **项目主管** | v0.9.0 迭代计划冻结 + **裁决十四**（基线复核 + 两处账目订正） | ✅ 完成（33 红结构订正；KPI 175/69 → 171/67） |
| **R6** | 2026-09-16 | **开发 agent** | **T19：机制判别实验**（H-a/H-b/H-c/H-d） | ⏳ 进行中（已收 2 条主管更正） |
| **R6** | 2026-09-16 | **测试 agent** | **T20：语义核证 + 真/巧合词根判别集** | ⏳ 进行中（已收 1 条主管补充情报） |
| **R6** | 2026-09-16 | **需求 agent** | **T21：V9.0 验收标准冻结** | ✅ 完成（DEC-015/016/017；AC-1…AC-10） |
| **R6** | 2026-09-16 | **项目主管** | T19/T20 双回执裁决（**裁决十五**）+ T20 附加项/T23 入账（**裁决十六**） | ✅ 完成（commit `54bdc05` `89cbc1b`） |
| **R7** | 2026-09-13 | **开发 agent** | **T22：判别分类器实验**（结构签名能否机器判定） | ⚠ 产出已落盘但**未回执**；主管亲自运行取结论 ⇒ **判定不通过**（裁决十七） |
| **R7** | 2026-09-13 | **项目主管** | 裁决十七落盘 + T19-C/T20 产物入账 | ✅ 完成（commit `3eaa13a`，18 文件 +~900 行） |
| **R7** | 2026-09-13 | **开发 agent** | **T24：词素候选全枚举 + 全库逐词差分 + H-d 残余** | ⏳ **进行中**（只读，禁落库） |
| **R7** | 2026-09-13 | **需求 agent** | **T25：DEC-018 + AC-2 条件式 + AC-11 词素落库门 + D11** | ⏳ **进行中** |

### ✅ 环境事实更正：goal 口径**已由用户亲自发普通消息解决**（勿再引用旧结论）

上方「⚠ 环境事实：goal 口径无法自主修改」的**结论仍有效**（`edit`/`resume` 确需人类回合），
但**该问题已闭环**：用户于 2026-09-13 亲自发送普通消息后，主管成功执行
`update_goal(action: edit)` → `revision 1→2`，随后 `update_goal(action: resume)` → **`revision 3`、`activation: armed`**。
新 objective 已改写为 **v0.9.0「词素语义核证 · 防假拆解」**口径（含 45% 已取消的事实）。
⇒ 今后需改 goal 时，仍须**让用户亲自发一条普通消息**，不要重试工具调用。

### ⚠ 环境事实：goal 口径无法自主修改（已实测，勿重试）
用户已明确授权把 goal objective 从「覆盖率 30.8% → >45%」改为 v0.9.0 口径，但：
- `update_goal` 带 `action: edit` 在**目标轮（goal_round）内**报
  `Error: this goal operation requires a direct human turn on a top-level agent`；
- **经 `ask_user_question` 获得的用户答复不算「direct human turn」**——本回合已实测再失败一次。

⇒ **结论**：修改 goal 需要用户**亲自发送一条普通消息**（非选项回答）。主管**不再重试**（熔断）。
当前 goal `goal-a653766a-957d-4852-855e-c2e5a7be86cf`（revision **1**）的 objective 仍含旧口径，
但这**不影响 v0.9.0 已获授权的方向**（用户本轮明示「改成 v0.9.0 口径并保持活跃（推荐）」）。

---

## ★★ v0.9.0 遗留欠账处置（主管，2026-09-16 发布后）

> 三项欠账全部结清。其中第 3 项**推翻了此前的定性**，是本节最重要的更正。

### 欠账 1 · 编号域碰撞 → 已消除
- **问题**：`docs/交接文档.md` §6-B 的 **B1–B7**（产品功能）与 `.board/REQ.md` §7 的 **B-1…B-7**（工程债）**共用 `B` 前缀**，跨文档引用极易错指。
- **裁定**：**工程债侧 `B-*` 保持不动**（它是活跃指针，已被 AC 与本文件多处引用）；
  **产品侧改 `P1–P7`**（在 `交接文档.md` 内改名，影响最小）。
- **已执行**：`docs/交接文档.md` §6-B 标题改为「编号 **P1–P7**」+ 表内 7 行全部改名 + 段首加**编号纪律**警示块（指明「工程债以 `REQ.md` §7 为准」）。**364 → 367 行**；校验残留 `| **B1** |` 形式 **0** 处、新 `| **P1** |` 形式 **7** 处。

### 欠账 2 · 证据误溯源 → 已订正
- **问题**：v0.9.0 文档中 `loses = 2` 的证据被标注为 **`scripts/_tmp_readme_v090.mjs:17`** ——
  那只是主管用来改写 `README.md` Roadmap 的**临时脚本，且早已删除**。**拿临时脚本当证据来源属溯源错误**。
- **正确来源**：`scripts/probe_t29_contrib.mjs`（测试 agent 的**留一法贡献度表**）与测试 agent 的 `.board/EVIDENCE.md` 第 10 轮。
- **已执行**：三份文档（`docs/需求总结.md` · `.board/REQ.md` · `docs/交接文档.md`）中该引用全部替换；校验残留 `readme_v090` 引用 **0** 处。
- **纪律**：**临时脚本（`_tmp*`）不得作为证据来源**；证据须落在 `scripts/probe_*.mjs` / `scripts/exp_*.mjs` 或 `.board/EVIDENCE.md`。

### 欠账 3 · `roots_ru.json` 「10,492 行 diff 噪声」→ ★定性被推翻，实为行尾翻转
- **原先的定性（含需求 agent 的采纳）**：「文件被**整体重新序列化**，格式与顺序改变 ⇒ 放大 AC-11(d) 逐条复核成本；后续落库须保持文件原有格式与顺序」。
- **主管实测推翻**（按 `(kind, morpheme)` 逐条比对两个 git revision）：
  | 项 | 实测 |
  | --- | --- |
  | 条目数 | 441 → **450** |
  | **真实新增** | **9**（`suffix|-ной` `root|домин-` `root|казус-` `root|однако-` `root|пад-` `root|столп-` `root|суд-` `root|суч-` `root|тряс-`） |
  | **真实删除 / 字段改动** | **0 / 0** |
  | 首个条目逐字节 | base 与 new **完全相同** |
  ⇒ **内容零损坏、零重排**；那 10,492 行**全部是行尾翻转**（5304 增 + 5188 删 ≈ 2×5306 行 = 整文件按行重写）。
- **根因**：仓库**此前没有 `.gitattributes`**（`git ls-files --eol` 显示 `attr/` 为空），
  而 Node 数据管线脚本（`scripts/apply_v9_morphemes*.mjs`）写 JSON 时未显式指定行尾，
  与仓库既有行尾不一致 ⇒ 下次 `git add` 整文件翻转。
- **已执行（结构性修复）**：
  1. **新建 `.gitattributes`**：`*.json text eol=lf` + `*.json.bak* -text`（最小范围，仅锁会被脚本重写的 JSON；避免波及已跟踪文本文件）。
  2. 归一两个**混合行尾**的 `package.json`（`apps/web/package.json` CR=1/LF=25 · `apps/sync-server/package.json` CR=1/LF=22）⇒ 消除隐患。
  3. 验证：加规则与归一之后 `git diff --numstat -- '*.json'` **零 diff**（内容未变，仅行尾统一）。
- **结论更正**：`docs/需求总结.md` §22.8 与 `docs/交接文档.md` §6-C6 中「整体重新序列化」的表述**须订正为「行尾翻转噪声」**；C6 的处置**已闭环**，不再是欠账。

### 附：本节的自我校验
本节的每一条结论均来自**主管亲自执行的对照实验**（`git show <rev>:<file>` 双版本 JSON 逐条比对 + `git ls-files --eol` + `git diff --numstat`），
非转述。这是本项目第 7 次「主管用实测推翻一个已被写进文档的定性」。

## 熔断记录（`AGENTS.md` §4.1）

| 事件 | 处理 |
| --- | --- |
| 主管的 `exp_ru_recoverable.mjs` 得出错误的 86.7% 上限 | 主管自查发现（任意位置子串匹配的假命中），加位置约束后修正为 70.0%，并在 BOARD.md 显式标注"勿引用" |
| 主管的 `exp_ru_gap.mjs` 分类器有 bug（类别不互斥，310+182≠492） | 主管自查发现，标记结论不可用，改用源码阅读法定位真机制 |
| 测试 agent 的 `chain.parts` 粒度不匹配，一度得出「准确率 0.7%」 | 测试 agent 自查发现（度量工具未先用已知正例校准），作废并修正到 193 词对齐子集 |
| 测试 agent 的归因表首版错（漏抄 `index.ts:764` 的 `minLen`） | 测试 agent 读源码核对后自行撤回，修正后与主管数字精确一致（215 / 58.0%） |
| 开发 agent 的「专名是天花板主因」假设 | 开发 agent 用数据自我否定（专名仅 10 词 / 1.1%） |

**结论**：本轮**无一次需要主管强制熔断**——五处错误全部由产出者自查发现并主动撤回。
这是 `AGENTS.md` §4.1「判别实验优先于调参」在本项目的首次规模化生效。


---

## ★ v0.10.0 派单与裁决记录（主管，2026-09-16）

### T34 验收 → T35 需求 agent 三处订正（**已闭环**）

| 项 | 落点 | 结果 |
| --- | --- | --- |
| **A** C6 定性订正 | `docs/交接文档.md:282`（§6-C6）· `docs/需求总结.md:1303-1307`（§22.8）· 第三处残留 `REQ.md:624-625` | 「整体重新序列化」→「**行尾翻转噪声**，真实新增 9 / 删除 0 / 字段改动 0、首条逐字节相同，已由 `.gitattributes`（`*.json text eol=lf`）结构性修复 ⇒ **C6 闭环**」；**原判定按 `DEC-014` 保留留痕并标注「已被推翻」**，另加更正理由「原判定系**从 diff 行数推断**、未逐条比对 ⇒ 推断须让位于取证」 |
| **B** AC-12 第 8 条 | `REQ.md:794` ＋ D15 ＋ 判定载体 | **迁移前备份 `<db>.bak-<ISO时间戳>`；备份成功才迁移；备份失败 ⇒ 放弃迁移**；幂等重跑不重复备份；备份不入 git |
| **C** 证据来源纪律 | **`DEC-022`**（`DECISIONS.md:464`）＋ `REQ.md:941` 红线第 7 条 | **临时脚本（`_tmp*`）一律不得作为证据来源**；起因 = `loses = 2` 曾被引到**已删除**的 `scripts/_tmp_readme_v090.mjs:17` |

行数：`REQ.md` 944→**969** · `DECISIONS.md` 476→**492** · `CHANGELOG.md` 142（+2）· `docs/交接文档.md` 364→**367** · `docs/需求总结.md` 1309→**1313**。
**主管独立复核**：AC-12 第 8 条、`DEC-022`、C6 订正三处均已实测在位；`scripts/probe_t29_contrib.mjs:107/110-111`（3 处）与 `.board/EVIDENCE.md:2044/2113-2114/2160/2212/2242`（7 处）引用路径成立。

### ★ 开发 agent 构建断链（主管实测发现，已发回执）

首次审阅 T35 中途产出时实测：`pnpm --filter @zidiankaifa/core build` **exit 2** —
`src/db/index.ts(171,19): error TS2304: Cannot find name 'fs'.` · `(173,9): error TS2304: Cannot find name 'fs'.`
因 `packages/core/src/db/index.ts` 的 import 块（`:7-30`）**无 `node:fs`**，而 `:171` 用 `fs.existsSync`、`:173` 用 `fs.copyFileSync`。
⇒ 已派回执要求补 import 并**重跑 build 确认 exit 0**；同时要求按 AC-12 第 8 条把**备份失败即放弃迁移**（当前实现是 `console.warn` 后继续迁移，**与刚冻结的 AC-12 相反**）。

### ★ 主管审阅 T35 中途产出的结论（改动方向正确）

| 文件 | 改动 | 评价 |
| --- | --- | --- |
| `packages/core/src/db/schema.ts` | +29/−3：新增导出 `BOOK_COLUMNS_SQL` 与 `BOOK_TABLE_SQL` 作**唯一来源**，`SCHEMA_SQL` 内 `book` 改 `PRIMARY KEY (word, lang)` | ✅ 建表 DDL 与迁移 DDL 不再两处漂移 |
| `packages/core/src/db/index.ts` | +92：新增 `migrateBookCompositeKey(db)`，在 `openDatabase()` 中于 `migrateBookLang(db)` **之后**调用（顺序不可颠倒：先 ADD COLUMN `lang`，再升复合主键） | ✅ 幂等（读 `PRAGMA table_info` 判两列 `pk>0`）· 事务 + `ROLLBACK` + 行数校验（`after < before` 抛错）· `INSERT OR REPLACE` 显式列名含 `deleted` 墓碑 · 重建 `idx_book_updated` |

### ★ v0.10.0 两条新约束（主管裁决，T36 已通报测试 agent）

1. **AC-12 判定载体（测试 agent 的块 A）必须加两条断言**：① **备份文件存在**；② **重跑不产生第二个备份**。
2. **真实数据副本测试建议**：需求 agent 指出盲区 —— **`lang='ru'` 且含拉丁的 8 条**若只用手工夹具测迁移，**不会被任何断言覆盖**。
   ⇒ 建议测试 agent 复制 `data/db/dict.db` 到临时目录（原库只读）做逐字段保真断言，并实测那 8 条迁移后 `lang` 是否保持 `'ru'`；若因 494 MB 体积退化为小库注数据方案，**须在回报中写明是退化方案、不得宣称覆盖真实形态**。

---

## ★ 主管需求检查（2026-09-16，用户指令「检查需求」）

### 一、需求文档核验结论：**通过**（文档侧无缺陷）

- **文件长度（换行符计数，权威口径）**：`REQ.md` 969 · `DECISIONS.md` 492 · `CHANGELOG.md` 143 · `TASKS.md` 587 · `BOARD.md` 2432 · `docs/交接文档.md` 367 · `docs/需求总结.md` 1313
- **§8 结构齐备**：`8.0` 总览(`REQ.md:739`) / `8.1` 背景与拍板(`:752`) / `8.2` **AC-12…AC-19**(`:786`) / `8.3` **DoD D13…D22**(`:887`) / `8.4` 优先级依赖与 UI 冒烟协议(`:902`) / `8.5` **不做清单 10 条**(`:924`) / `8.6` **红线 7 条**(`:939`) / `8.7` 裁决表(`:949`)
- **锚点抽样全部在位**：`REQ-V10-001`(4) · AC-12(14) · AC-13(4) · AC-14(4) · AC-15(4) · AC-16(9) · AC-17(9) · AC-18(13) · AC-19(7) · D13(4) · D22(5) · `DEC-021`(2) · `DEC-022`(2) · `DEC-020-R1`(4)
- **T35 三处订正在位**：AC-12 第 8 条(`REQ.md:794`) · `DEC-022`(`DECISIONS.md:464`) · C6 订正(`docs/交接文档.md:282` · `需求总结.md:1303-1307`)
- **内部一致性**：优先级链 `AC-18 > AC-15 > AC-12 > AC-13 > AC-14 > AC-16 > AC-17 > AC-19` 与 §8.7 裁决表、红线、不做清单**无自相矛盾**；AC-18 已按 T34 订正写成「506 + N」而非写死 506

### 二、需求 ↔ 实现双向核对：**发现 3 处文档锚点漂移（不影响可判定性）**

| 需求引用 | 实际位置 | 说明 |
| --- | --- | --- |
| AC-17④ 引 `types.ts:239` | **`packages/core/src/types.ts:245`** | `bookGroups?(lang?: string)` 已实现，仅行号 +6 |
| D14 引 `schema.ts:85-96` | **`packages/core/src/db/schema.ts:109-123`** | 开发 agent 在文件头插入 `BOOK_COLUMNS_SQL`/`BOOK_TABLE_SQL`(−29/+3) 后下移；`PRIMARY KEY (word, lang)` 在 **`:123`** |
| AC-17⑤ 引 `sync-server/src/index.ts:182` | **`:181`** | 仅差 1 行 |

⇒ **均属「实现推进导致的锚点漂移」，非需求缺陷**；D22 更新交接文档时须一并**按最终行号重述**（否则后来者按旧行号找不到代码）。**原因记档**：需求冻结时引用的是**改动前**行号，而本迭代本身就在改这些文件 ⇒ 迭代内引用的行号必须视为**指示性**而非规范性。

### 三、★ 实现层缺口清单（需求检查的实质产出，与 §8.2 逐条对照）

| AC | 要求 | 现状（主管实测） |
| --- | --- | --- |
| AC-12 | 迁移 + 保真 + 幂等 + 备份 | ✅ **已完成并经主管独立实测 22/0** |
| AC-13 | 跨语言隔离（含墓碑 lang） | ✅ **核心已成立并经实测**（见下） |
| AC-14 | `syncMerge` 按 `(word, lang)` | ⏳ 未开始（`packages/core/src/db/index.ts:1171`） |
| **AC-16⑤⑥** | **`localStorage` 路径同样隔离 + 旧数据一次性迁移** | ⏳ **未改，且发现具体缺口**：`apps/web/src/api.ts` 的 `bookAdd` 仍按 **`word` 单键** 去重（`:136-144`「已有同词直接 return 不再加」）⇒ 同一 `word` 的 en/ru 两条**互相顶掉**；`bookRemove`(`:158`)/`bookUpdate`(`:166`) 同样按 word 匹配；旧数据**无一次性按 `isCyrillic` 归语言**的逻辑（D18） |
| AC-16 | UI 子页签 + 手动覆盖 | ⏳ 未开始（`BookPanel.tsx`） |
| AC-17 | 第 5 面板 + `RootClassPanel.tsx` | ⏳ 未开始（`App.tsx:17` `type Panel` 仍 4 个） |
| AC-17⑤ | 契约贯通 `bookGroups(lang?)` | 🟡 **部分**：`types.ts:245` 已加可选 `lang`；`main.mjs:120` `ipcMain.handle('book:groups', () => …bookList(db))` **未传 lang**；`sync-server/src/index.ts:181` `bookList(syncDb)` **未传 lang**；`api.ts:201` 未改 |
| AC-15 | 桌面 22 断言 | ✅ **实测 22 通过 / 0 失败**（核心改动后复跑） |
| AC-18 | 既有 506 不回归 | ✅ **实测 core 484/0 + desktop 22/0 = 506/0 exit 0**（口径不同，见下注） |

> **口径注（重要）**：AC-18 判定命令是 `pnpm --filter @zidiankaifa/core test` ⇒ 既有 core 数为 **484**（99+67+38+60+194+26），**desktop 22 是其独立包的另一条命令**。AC-18 第 1 条写「既有 506 项不回归」是**跨两条命令的合计**，而第 2 条的门禁总数写 **506 + N** ⇒ 两条命令分别应为 **484+N1** 与 **22+N2**（需求 §8.4 判定命令已如此写）。**「506」是合计口径，勿在单条命令上要求 506。**

### 四、★ 主管独立实测（新探针 `scripts/probe_sup_book_migration.mjs`，**只读、用临时合成库，不碰 `data/db/dict.db`**）

**结论：22 通过 / 0 失败（exit 0）** —— 覆盖 AC-12 判定式 1/2/3/4/7/8 与 AC-13 全部、D17 缺省语义：
- §1 迁移后 `PRAGMA table_info(book)` **两列 `pk>0`（word, lang）** ✔
- §2 **10 字段 × 3 行逐字段相等**（含 `deleted=1` 墓碑与 `lang='ru'` 墓碑）✔
- §3 **产生且仅产生 1 个** `.bak-<ISO时间戳>` 备份 ✔
- §4 连续两次 `openDatabase()` 行内容一致 + **幂等重跑不产生第二个备份** ✔
- §5 `idx_book_updated` 仍在 ✔
- §6 同词双语言各存一条；删 ru 后 en 仍存活；**ru 墓碑 lang 仍为 `'ru'`（未回退 `'en'`）** ✔
- §7 `bookListAll` 缺省不过滤（2 条）· `bookList(db,'en'/'ru')` 各只返 1 条 · `bookGet` 缺省 `'en'` · **`bookRemove` 缺省只删一条、另一语言仍存活** ✔

> **★ 主管自纠一次（记入熔断记录）**：本探针**第一版有缺陷** —— `bookListAll`/`bookList` 经 API 层把 SQL 的 `deleted` 由 `0/1` **映射为 boolean**（`false/true`），我首版用 `b.deleted !== 1` 过滤 ⇒ **把墓碑误判为存活**，据此一度报告「`bookRemove` 缺省未删任何条目」。
> 复核 SQL 原样（`deleted` 为 integer 且 en 行确已置 1）后定位为**断言写错**，改为 `!== true` 后全绿。
> **教训**：假红与假绿同源 —— 断言口径与被测对象的**类型**不符时，会产出与事实相反的报告（本项目第 8 次同类）。**下结论前先看 SQL 原样**。

---

### 五、★★ 裁决十九 —— AC-13 缺省 `lang` 删除语义定案（主管，2026-09-16）

**需求侧提问**（`REQ.md` §9 审计报告提出「动手前须定案」）：`bookRemove(db, word, lang?)` 的 `lang` 缺省时该删谁？同词双语言场景下 `existingLangForWord()` 按「未删除优先、其次最近更新」挑一条、**不按语言**，而当时 UI 调用点（`apps/desktop/src/main.mjs:116`、`apps/web/src/components/ClipboardPopup.tsx:70`）**均不传 lang** ⇒ **在俄语列表点删可能删掉英语条**。

**候选三案**：① 缺省删该词全部语言；② 核心强制必填 `lang`；③ 核心保留可选（兼容）+ **UI/IPC 一律显式传** ＋ 新增红线。

**★ 裁定：方案 ③。** 理由三条：
1. 核心缺省已按 `existingLangForWord` 实现，主管探针 `scripts/probe_sup_book_migration.mjs` **22/0** 实测「只删一条、另一语言存活」，**语义可用且向后兼容**（老调用点不破）；
2. 但该选择**不按语言**、属任意性（同词双语言时挑谁取决于 `updated_at` 与 `deleted`）⇒ **UI 不得依赖它**；
3. **不选 ①**：一次删除两语言是破坏性操作，且与用户「英俄分开」的迭代意图**方向相反**。

**★ 新增红线（v0.10.0 起）**：**任何 UI / IPC 调用点都不得省略 `lang`**；`bookRemove` / `bookUpdate` 的核心缺省回退**仅作向后兼容**，**UI 层依赖缺省即视为缺陷**。

**执行结果（主管实测，开发 agent 已在 T39 前落地）**：全部调用点已显式传 lang ——
`App.tsx:145 bookRemove(word, entryLang)` · `App.tsx:147 bookAdd(word, [], entryLang)` ·
`ClipboardPopup.tsx:72/74`（`const lang = detail.i18n?.lang ?? 'en'` 后传入）·
`BookPanel.tsx:78 bookList(lang)` · `:94 bookGroups?.(lang)` · `:132 bookRemove(item.word, item.lang ?? lang)` · `:147 bookAdd(w, [], target)` ·
`main.mjs:114/115/118/123` · `preload.mjs:12-16` · `sync-server/src/index.ts:184`。

**★ 裁决十九执行后：`:78` 的 `bookList()` 经复核为「设计正确」而非缺口（主管两次判定，第二次更正）**

- **首次判定（错）**：`apps/web/src/App.tsx:78` `refreshBook` 的 `getBackend().bookList()` 未传 lang ⇒ 列为 T39 缺口 1。
- **更正依据（开发 agent 顶回 + 主管复核采纳）**：该处**不是列表展示读取**，而是**跨语言状态判定** —— `App.tsx:145/:147` 的 `toggleBook` 需按 `(b.lang ?? 'en') === entryLang` 判断目标语言条目是否已存在；若此处只取单一语言，**跨语言状态判定会误判**。开发 agent 已在 `scripts/check_v10_ui_contract.mjs:16-22` 写明豁免理由，脚本块 B 的「无 `bookList()`」断言**只作用于 `BookPanel.tsx`**，当前形态与脚本自洽。
  ⇒ **保留全量读取 + 保留豁免注释**；裁决十九的红线**不适用于此点**（红线管的是 mutations 与**列表展示读取**）。
- **T39 实际唯一缺口**：`apps/desktop/src/main.mjs:121` `ipcMain.handle('book:update', (_e, item) => bookUpdate(db, item))` ⇒ `item.lang` 缺失时落到核心 `packages/core/src/db/index.ts:1163` 的任意性回退。已要求开发 agent 修（建议显式校验拒绝）+ 在脚本块 D 补一条可持续判定的断言。

### 六、★ 状态订正：§三「实现层缺口清单」多行已过期（截至 2026-09-16 实测）

> 背景：§三 写于**需求检查时点**，此后开发 agent 已推进多个 AC。下表为**主管逐文件实测**的最新状态，**替代 §三 的对应行**。

| AC | §三 旧记 | **最新实测（权威）** |
| --- | --- | --- |
| AC-13 | 核心已成立 | ✅ 核心 + **API/`localStorage` 双层均已隔离**（见下 AC-16⑤） |
| **AC-14** | ⏳ 未开始（`index.ts:1171`） | ✅ **已实现**：`syncMerge:1173`，语言守卫 `:1178 WHERE word = ? AND lang = ?` |
| **AC-16⑤⑥** | ⏳ 未改，按 word 单键去重 | ✅ **已修**：`api.ts` `bookAdd:183-204`（按 `(word, itemLang)` 去重 `:186-188`）· `bookRemove:206-213`（只删指定语言）· `bookUpdate:215-224`（按语言映射）· **`lookup` 的 `inBook` 按语言判定 `:148-153`** · `readLocalBook:54-80` 新旧结构兼容 + 迁移写回一次 · `inferLang:45-47` · `itemLang:50`；**D18 旧数据归语言逻辑存在**（`api.ts:16` import / `:33` 注释 / `:46` 判定，**共 3 处**） |
| **AC-16** | ⏳ 未开始 | ✅ **已实现**：`BookPanel.tsx:45-65` 语言子页签（`type BookLang = 'en' \| 'ru'`）· `bookList(lang):78` · `bookGroups(lang):94` · 各自独立计数 `:164`「共 N 词」· 自动判语言 + **手动覆盖** `:145`（`choice === 'auto' ? (isCyrillic(w) ? 'ru' : 'en') : choice`）· 空态按语言分流 `:254/:312` |
| **AC-17** | ⏳ 未开始（4 面板）；⑤ 部分 | ✅ **已实现**：`App.tsx:19` `type Panel` 含 `'rootclass'`（**共 5 个，未加第 6 个**）· `RootClassPanel` import `:12` / 渲染 `:354-355` / 导航 `:229-231` `:277-278` · **`RootClassPanel.tsx` 已建 152 行**（含 §6 分层纪律注释 `:22`）· ⑤ 契约贯通：`types.ts:245` + `api.ts:257/328` + `main.mjs:123-124` + `sync-server/src/index.ts:184` |
| AC-18 | 既有 506 不回归 | ⏳ `packages/core/test/book_lang.mjs` 仍不存在（测试 agent T36 在办）；`packages/core/package.json` test 链仍 6 文件 |
| AC-19 | 未开始 | ⏳ 零 bump（四个 `package.json` 与三处文本仍 `0.9.0`） |

### 七、★ 审计报告一处结论作废（主管实测证伪）

需求 agent §9 称「`apps/web/src/api.ts` **完全未引用 `isCyrillic`**（grep 0 处）⇒ **D18 旧数据归语言逻辑不存在**」。
**主管实测证伪**：`api.ts:16` `import { groupBookByMorphemeData, isCyrillic } from '@zidiankaifa/core';` · `:33` 注释（旧裸数组按 `isCyrillic(word)` 归语言并立即写回一次）· `:46` `return isCyrillic(word) ? 'ru' : 'en';` —— **命中 3 处**，且 `readLocalBook:54-80` 已实现完整 D18。
⇒ **「D18 不存在」作废**；成因是**取数时点早于开发 agent 的写入**（时点差，非其审计方法有误）。**她的 4 处 `localStorage` 缺口（含 `lookup` 的 `inBook` 跨语言误报）经验证全部属实，已由开发 agent 修复**。

### 八、★ 主管本轮独立实测（2026-09-16，全部为亲自执行，非转述）

#### 8.1 门禁：core 大改后**零回归**（AC-15 / AC-18 护栏守住）
| 命令 | 实测 |
| --- | --- |
| `pnpm --filter @zidiankaifa/core test` | **484 通过 / 0 失败 exit 0**（99 + 67 + 38 + **60**〔无「结果：」前缀〕+ 194 + 26） |
| `pnpm --filter @zidiankaifa/desktop test` | **22 通过 / 0 失败 exit 0**（场景 1–5 全过） |
| **合计** | **506 通过 / 0 失败** |

⇒ 开发 agent 的复合主键迁移（`migrateBookCompositeKey` + `ON CONFLICT(word, lang)` + `existingLangForWord`）**未破坏任何既有护栏**，且 AC-15 数据安全护栏（dsktop 换库迁移）完好。

#### 8.2 UI 契约脚本实测（`scripts/check_v10_ui_contract.mjs`，开发 agent 所建 238 行）
**实测 `node scripts/check_v10_ui_contract.mjs` → 53 通过 / 0 失败 exit 0**（六块 A–F：A 第 5 面板 / B AC-16 UI / C D18 localStorage / D D19 契约贯通 / E AC-12+D16 数据层复核 / F D21 门禁接入条件式）。
**主管评价：优于我拟写的版本** —— 额外覆盖了 `LexiconPanel.tsx` 侧职责区分注释、`schema.ts` 层复核（含「未给新列写 CREATE INDEX」这条铁律）、以及**块 F 的条件式保护**（断言「`book_lang.mjs` 未落地时门禁链**不得**含它」⇒ 从机制上杜绝「提前加链崩 `pnpm test`」）。
**已知的正当豁免（主管复核采纳）**：`apps/web/src/App.tsx:78` 的 `bookList()`（不传 lang）—— 该处是**跨语言状态判定**（`toggleBook` 需按 `(b.lang ?? 'en') === entryLang` 判断目标语言条目是否存在），**不是列表展示读取**；豁免理由已写在脚本 `:16-22`，块 B 的「无 `bookList()`」断言**只作用于 `BookPanel.tsx`**，与当前形态自洽。

#### 8.3 样式文件位置（口径订正，勿再引错）
`apps/web` 的样式表是 **`apps/web/src/styles.css`（39,936 B）**，**不是** `index.css`（主管一度按后者查找，属口误）。

### 九、主管 T40 需求文档整理（用户指令「整理需求文档」，2026-09-16）

**触发**：用户直接指令。**背景**：需求管理 agent 的 T40 回合**只落一半**（`REQ.md` 改到 00:43，但 §9 审计表两行旧断言未撤、`DECISIONS.md` 停在 00:27、§8.6 红线 9 末句仍挂着向主管提问），故由主管**逐项收口**。

| # | 整理动作 | 结果 |
| --- | --- | --- |
| 1 | **§9.1 审计表三行订正** | AC-15 `⚪ 未验证` → **✅ 主管实跑 22/0**；AC-16 `🔴 未开始` → **✅ 已实现**；AC-17 `🔴 未开始` → **✅ 已实现**（均附实测行号与机械判定入口）。**原判定按 `DEC-014` 保留留痕**（表内已注明「本行 T38 断言已被实测推翻」） |
| 2 | **§9.2 第 5 行作废 ＋ 新增「9.2 订正」块** | 作废「`api.ts` 未 import `isCyrillic`」；写明**三处命中**（`:16` / `:33` / `:46`）＋ `readLocalBook:54-80` 已实现 D18；**成因记为「时点差」并明确「不是审计方法有误」**；补记 1–4 项属实且已全修 |
| 3 | **§9.4 三项状态化** | ① 缺省删除语义 → **✅ 已定案（裁决十九）**，并写明**原建议（删全部语言 / 报错必传）均未采用**、主管裁定第三案及三条理由；② **✅ 已采纳**（改写「幂等」）；③ **✅ 已采纳**（判定层 = `api.ts`） |
| 4 | **§9.5 / §9.6 收口** | §9.5 补「T40 后续核对」；§9.6 增**逐项状态表**（2①–2④ 与 3①–3② 全部逐条对照现状），并如实指出**本审计的核心价值 = 其中两项直接改变了实现约束**（催生红线 9 ＋ `DEC-024`；使 AC-16⑥ 由不可判定变可判定） |
| 5 | **§8.6 红线 9 结清** | 把「需主管确认 `App.tsx:78` 是否属有意例外」**改为已裁定**：**属有意例外**（跨语言状态判定），并写明**例外纪律**（须脚本内书面登记 ＋ 主管裁定，**可机械复核**，不得口头豁免）；**另记契约不一致**（核心 `bookUpdate` vs `types.ts:239`） |
| 6 | **§8.0 迭代总览状态化** | 新增「**逐 AC 状态（T40 主管实测）**」与「**机械判定入口**」两行 ⇒ 打开需求文档即可看到真实进度，不必翻到 §9 |
| 7 | **`DECISIONS.md` 新增 `DEC-024`（裁决十九）** | 含：① 缺省 `lang` 删除语义定案（三案对照 ＋ 理由三条 ＋ 未采用候选留痕 ＋ 执行落点 ＋ 未闭环点）；② **三条并行迭代纪律**（并行文件的断言有时效性 · 豁免必须可机械复核 · 假红与假绿同源）；③ **编号占用核对声明**（`DEC-024` 检索未被占用）。**`DEC-024` 经核对未被占用**（本项目已两次因编号被占被顶回） |
| 8 | **`DECISIONS.md` 待拍板表新增 Q7 并即刻关闭** | Q7 = `App.tsx:78` 是否违反红线 9 → **属有意例外，不违规**（含理由与双落证据：脚本 `:16-22` ＋ 块 B 断言范围） |
| 9 | **`CHANGELOG.md` 追加 T40 条目** | 状态收口（AC-15/16/17 已实现 ＋ 506 不回归）· 裁决十九落 `DEC-024` · 一处审计结论作废 · **新证据两条（正式路径）** · **⚠ 剩余未闭项三条**（AC-18 断言、`book:update`、契约不一致、零 bump） |

**整理后规模（换行符计数，权威口径）**：`REQ.md` **1122 → 1155** · `DECISIONS.md` **513 → 541** · `CHANGELOG.md` **144 → 146** · `TASKS.md` **688 → 731**。
> ⚠ 上述四个数字为主管**逐文件实测**（`[System.IO.File]::ReadAllText()` + 统计 `[char]10`；**不用 `Measure-Object -Line`**，它不数空行 —— 本项目已踩过）。

**★ 本轮仍**未**解决的两项（如实登记，勿误读为已完成）**：
1. ~~**`packages/core/test/book_lang.mjs` 仍不存在** ⇒ **AC-18 阻塞**（测试 agent 上一回合未落盘，已催办）；**加链由主管在文件落地后执行**（契约脚本块 F 有条件式保护，提前加链会判红）。~~
   ⚠ **本行已被 T41 推翻**（2026-09-16 主管实测）：文件**已落地**（39,486 B）且主管**已加链** ⇒ 见第十节。原判定按 `DEC-014` 保留留痕。
2. **`apps/desktop/src/main.mjs:121` `book:update` 未校验 `item.lang`** ⇒ 裁决十九的最后一个未闭环调用点（T39 在办）。

---

### 十、★★ T41 —— AC-12⑧ 的 WAL 备份缺陷：**主管复核成立 · 裁决二十 · 已修复**

**来源**：功能测试 agent T36 回执的 **F1【P0 候选】**。**性质**：**用户数据安全**（迁移前的「第二道独立防线」在真实库上不成立）。

#### 一、机制（她给、主管逐条复核确认）
- `packages/core/src/db/schema.ts:29` = `PRAGMA journal_mode = WAL;` ⇒ **所有** `openDatabase()` 打开的库都是 WAL 库。
- 备份实现（原 `packages/core/src/db/index.ts:180-188`）= `fs.copyFileSync(file, bak)` —— **只复制主库文件，不含 `-wal`/`-shm`**。
- ⇒ **已提交但尚未并回主库的事务不在备份里** ⇒ 「可回退副本」的**声明的目的**不成立（**字面**要求「先复制/失败则放弃/幂等不重复备份」则满足）。

#### 二、★★ 主管独立受控实验（`scripts/_tmp/probe_sup_wal_snapshot2.mjs`，**修正版**）
> ⚠ **主管第一版实验 `probe_sup_wal_snapshot.mjs` 有缺陷、结论相反**：它在 copy 前 `db.close()` ⇒ 触发 **clean shutdown 自动 checkpoint** ⇒ 掩盖了问题（当时误得「方案0 也含 delta」）。**教训与「假红/假绿同源」同类：夹具的前置条件不成立会产出与事实相反的结论。** 第二版改为「**持有打开的写连接** ＋ 用第二个连接写入」，才复现。

| 方案 | 副本行数 | 含「用户刚加的生词」 | 与主库逐字节相等 |
| --- | --- | --- | --- |
| **方案0 · 现行（只复制主库）** | **3** | **NO ❌** | NO |
| **方案A · 先 `wal_checkpoint(TRUNCATE)` 再复制** | **4** | **YES ✔** | **YES** |
| 方案B · `VACUUM INTO` | 4 | YES ✔ | **NO** ⇒ 会打破「备份==快照」逐字节断言 |

前提实测：主库 12,288 B · `-wal` 8,272 B · 写连接仍打开 · 表内可见 4 行。

#### 三、★ 裁决二十（主管，2026-09-16）：**采用方案 A（checkpoint 后再复制）**，理由三条
1. **备份文件与主库同构** ⇒ 应用可直接打开，回退路径最短。
2. **不破坏既有断言**「备份内容 == 迁移前快照」的**逐字节**语义（方案 B 会破坏它）。
3. **改动面最小**（无 API 变化，不引入 `VACUUM INTO` 的额外失败模式）。
> 否决方案 B 不是因为其结果错，而是因为它把「回退副本」换成「另一种格式的副本」，代价大于收益。

**实现（已落盘并 build）**：`packages/core/src/db/index.ts` 的 `migrateBookCompositeKey()`（`:154`）备份段内，`:182` 的 `fs.copyFileSync` **之前**插入：
```ts
const cp = db.prepare('PRAGMA wal_checkpoint(TRUNCATE)').get() as unknown as {
  busy: number; log: number; checkpointed: number;
};
if (cp.busy !== 0) {
  console.warn(`[zidiankaifa] book 复合主键迁移：WAL checkpoint 未完成（busy=${cp.busy}），放弃迁移（数据优先）`);
  return;
}
```
- **`busy !== 0` ⇒ 放弃迁移**：与既有「备份失败即放弃迁移（数据优先）」同一条原则 —— **不完整的一致快照不配当回退点**。
- `dist/db/index.js:71` 已含 `wal_checkpoint`（build exit 0）。

#### 四、★★ AC-18 加链已完成（主管执行，**该活按 AC-18② 明确属主管**）
`packages/core/package.json:25` 的 test 链**末尾追加** `&& node test/book_lang.mjs`（此时 `book_lang.mjs` 已落地，块 F 有条件式保护不会误判）。

**门禁实测（主管，2026-09-16）**：
| 文件 | 断言 | exit |
| --- | --- | --- |
| `packages/core/test/regress.mjs` | 99 | 0 |
| `packages/core/test/lexicon.mjs` | 67 | 0 |
| `packages/core/test/related.mjs` | 38 | 0 |
| `packages/core/test/ru_morph.mjs` | 60 | 0 |
| `packages/core/test/ru_morph_d1fix.mjs` | 194 | 0 |
| `packages/core/test/ru_morph_d1guard.mjs` | 26 | 0 |
| **`packages/core/test/book_lang.mjs`** | **98** | **0** |
| **core 合计** | **582** | **0** |
| `apps/desktop/test/dbmigrate.test.mjs` | 22 | 0 |
| **门禁总计** | **604** | **0** |
- **AC-18 算术闭合**：`N1 = 98`（测试 agent 交付后又自行扩到 98：块 A 29 / B 26 / C 18 / D 25）· `N2 = 0` ⇒ **core = 484 + 98 = 582**、**跨命令合计 604**。**506 不回归已实测成立。**
- **铁律 6 双 cwd**：`book_lang.mjs` 两种 cwd 均 **98 通过 / 0 失败 exit 0**。

#### 五、待办（已派 T42 / T43）
- **T42（功能测试 agent `843b7bf5-6f12-44f4-b51a-9b87c4037bdd`）**：把 F1 的「⚠ 观察项」升为**会红的断言**（新块 E）：① 源库有未并回 WAL 时备份必须含全部已提交行；② 备份与源库逐字节相等；③ 移除 checkpoint 步骤必须变红（变体 dist 自证鉴别力）。
- **T43（需求管理 agent `4ce66e63`）**：按裁决二十把 **AC-12⑧ 改写为「一致性快照」**（明文：先 `wal_checkpoint(TRUNCATE)` 并回、`busy !== 0` ⇒ 放弃迁移）；刷新 `REQ.md` §8.8 行号表；登记 `roots_ru.json` 四条 `origin` 文本订正；`DECISIONS.md` 新增裁决二十条目；`CHANGELOG.md` 追加 T41。

#### 六、主管对 T36 其余发现的处置（未采纳者亦记录理由）
| 发现 | 处置 |
| --- | --- |
| **F1 备份非有效回退点** | **采纳并已修**（见上） |
| **F2 真实老库迁移保真** | 采信（她的探针用真实历史库 `data/db/dict.db.bak-v02pipe`，272,752,640 B。**已知事实：那次真实迁移未留备份**——当时备份步骤抛 `fs is not defined` 而代码是「继续迁移」） |
| **F3 「8 条 `lang='ru'` 含拉丁」是范畴错误** | **采纳**：那 8 条在 `words_i18n`，`migrateBookCompositeKey` 只读写 `book` 表（实测 159,127→159,127、62→62 未动）。**建议 §8.5 登记时补记第 8 条 `имя прилaгaтeльнoe` 是字符集损坏行** |
| **F4 dist 新鲜** | 采纳（`dist/db/index.js` mtime 晚于 src ⇒ 484/0 与 92/0 非旧行为假绿） |
| **F5 她自查两处假红** | 已记入熔断记录（**计数类断言必须精确匹配文件名全形**，`startsWith` 会把 SQLite sidecar 算进来） |
| **F6 省略 `lang` 的删除「破坏性且不确定」** | **已由裁决十九覆盖**（定性为「任意性·仅向后兼容」＋ UI 必须显式传 lang）；F6 作为**读码事实**登记，不加断言 |
| **T32 的 9 条来源复核三档** | **采纳**（档1 六条充分 · 档2 三条部分 · 档3 零条）。四条 origin 文本订正已派 T43（**均不撤词素**）。详细：`домин-` origin 抄的模板逐字不符且来源自身不自洽、`пад-` 的 `*pasti` 应为 `*padati`、`-ной` 外部来源不支持当单一后缀（改为引 `боль|-н|+ой` 并说明引擎合并「后缀+词尾」）、`однако-` 须补共时/历时区分 |
| **AC-13⑦ 判定链「缺一环」（她称未见 `check_v10_ui_contract.mjs`）** | **已订正**：该脚本存在；主管实测加链后 **53 通过 / 0 失败 exit 0**（她报的 52/1 唯一失败项正是「`book_lang.mjs` 已落地但未接入 test 链」= 加链前的**过渡态**，已被主管的加链消除） |
| **AC-18 算术（92 ⇒ 实际 98）** | 采纳她的方法，数字按她后续交付更正为 **98** |

#### 七、★★ T41 附带发现：行号漂移由主管自己的修复造成（第 4 次同类）
- 测试 agent 实测 `packages/core/src/db/index.ts` = **1249 行**（`mtime 2026-09-14 00:37:10`），主管此前给的是 **1215 行**（两代之前）。
- **主管本次实测 = 1268 行 / 51,023 B**：差额 **+19 = T41 的 WAL checkpoint 段**（加在 `:182`，其后所有符号整体下移 **+19**）。
- **权威当前行号**：`migrateBookCompositeKey` `:154`（不变，改动在函数体内）· 备份块 `:173` 起 / `const bak` `:201` / `copyFileSync` `:203` · `BOOK_DEFAULT_LANG` **`:1072`** · `existingLangForWord` **`:1079`** · `bookGet` **`:1115`** · `bookList` **`:1127`** · `bookListAll` **`:1139`** · `bookAdd` **`:1144`** · `bookRemove` **`:1163`** · `bookUpdate` **`:1190`** · `syncMerge` **`:1216`** · `groupBookByMorpheme` **`:1256`**。
- ⇒ **纪律（建议写入 `REQ.md` §8.8 表头）**：**任何改动 `packages/core/src/db/index.ts` 的迭代，结束前必须刷新行号对照表**；引用一律**符号名 + 行号成对**（`DEC-023`）。

#### 八、★ T39 闭环：`book:update` 的 IPC 边界防御（主管补，`apps/desktop/src/main.mjs`）
- 实测 `lang` 链路**本已贯通**：`apps/web/src/components/BookPanel.tsx:116` `lang: item.lang ?? lang` → `apps/web/src/api.ts:218` `const l = item.lang ?? … ?? inferLang(item.word)` → IPC → core `bookUpdate`。
- **但 IPC 边界无防御**（原 `apps/desktop/src/main.mjs:121` = `ipcMain.handle('book:update', (_e, item) => bookUpdate(db, item))`）⇒ 弱类型通道下任意调用方省略 `lang` 即触发核心的 `existingLangForWord()` 不确定语义。
- **已改**：拒绝非 `'en'|'ru'` 的 `lang`，抛 `Error('book:update 需要显式 lang（"en" | "ru"）：缺省语义在同词双语言下不确定（裁决十九）')`。合规 + 防未来调用点回归。
- **回归实测**：`pnpm --filter @zidiankaifa/desktop test` = **22 通过 / 0 失败 exit 0**（铁律 5 要求）。

#### 九、★★ 发布前必查：两个**新增测试文件未入 git 索引**（测试 agent 三次回执提出，主管已核实）
`git ls-files --error-unmatch` 逐文件实测（`packages/core/test/` 共 11 个 `.mjs`）：
| 文件 | 大小 | 索引状态 |
| --- | --- | --- |
| **`packages/core/test/book_lang.mjs`** | **43,045 B** | **★ 未跟踪**（SHA256 `229DCE266CF9CDD008A810F1D26C78BFDFE66D27F8CBE77387169DC4E911848C`） |
| **`packages/core/test/ru_morph_v090_guard.mjs`** | **33,962 B** | **★ 未跟踪**（T32 产出） |
| 其余 9 个（`regress` / `lexicon` / `related` / `ru_morph` / `ru_morph_d1fix` / `ru_morph_d1guard` / `ru_morph_defects` / `ru_morph_goals` / `ru_morph_semantic`） | — | 已跟踪 |
- **风险**：两文件均由**不做 git 写操作**的 agent 产出 ⇒ 只存在于工作区。**若 AC-19 的 `git add` 遗漏**，则发布的 v0.10.0 会**带着接入 test 链的 `package.json` 却缺少 `book_lang.mjs`** ⇒ `pnpm test` 在干净克隆上**直接崩**（本轮最实际的发布风险，高于任何未达标项）。
- **处置**：AC-19 的 `git add` 明确清单**必须包含**这两个路径；并在 push 后用 `git ls-files | Select-String 'book_lang|v090_guard'` 复核。
- ⚠ 主管订正：本节前文曾把 `book_lang.mjs` 记为 39,486 B，那是**加链前后的中间态**；权威值为 **43,045 B / 790 行**（SHA256 已比对一致）。

#### 十、★ AC-16 / AC-17 的真实缺口：**渲染判定链仍缺**（对手续不粉饰）
- 测试 agent 如实声明：`book_lang.mjs` 的 D 块只是**文本级契约**，**「文本级契约已通过 ≠ 界面可用」**（该提醒由 `scripts/check_v10_ui_contract.mjs` 末行自己打印）。
- **当前机械覆盖**：存储侧 98 条（`book_lang.mjs`）+ 契约层 53 条（`check_v10_ui_contract.mjs`，含块 F 门禁接入检查）。
- **仍缺**：`REQ.md` §8.4 的 **UI 冒烟两条路径**（① 浏览器 `localStorage` 后端；② 桌面端 SQLite 后端）—— 需实跑并留痕（5 面板 + 「📚 生词本」内英/俄子页签 + 「词根分类」面板空态/分组 + 添加框语言自动判定与手动覆盖）。
- **处置**：已列为 **T44**（发布前必须走完，或以「未做」如实写入 Release Notes；**不得**用契约脚本冒充渲染验证）。

---

### 十一、★★ 裁决二十一 —— 备份改用 `VACUUM INTO` ＋ 迁移后置校验（T45，主管）

**触发**：需求管理 agent T43 反对意见第 1 条「`busy !== 0 ⇒ 放弃迁移` 的降级路径行为未定义」。**结论：她的推定成立，且实测比她估计的更严重。**

#### 一、★ 受控实验（`scripts/_tmp/probe_sup_degraded_write2.mjs`，夹具由 `BOOK_COLUMNS_SQL` 派生）
> ⚠ 主管**第一版夹具无效**（手写列清单漏了 `added_at` ⇒ 迁移因 `no such column: added_at` 失败，测的是错误的失败原因）。第二版改为**从 `BOOK_COLUMNS_SQL` 派生列定义**（同一组列、`PRIMARY KEY (word)`），才得到有效结论。**这是本轮第二次「夹具前置条件错误 ⇒ 无效实验」**。

老结构（`PRIMARY KEY (word)`）库上的实测行为：
| 操作 | 结果 |
| --- | --- |
| `bookAdd(db,'newword',[],'en')` | **✗ 抛错** |
| `bookAdd(db,'новое',[],'ru')` | **✗ 抛错** |
| `bookRemove(db,'existing','en')` | **✗ 抛错** |
| `bookUpdate(db,{word,lang,status})` | **✗ 抛错** |
| `bookList(db)` / `bookListAll(db)` | **✓ 成功** |

**错误原文**：`ON CONFLICT clause does not match any PRIMARY KEY or UNIQUE constraint`
**⇒ 4/4 写入全部失败，而读取正常** ⇒ 生词本表现为「**能打开、列表正常、但一条也加不进去**」的静默故障。叠加静默语义错误：老结构下 en/ru 同形词互相覆盖（本迭代正是要消除的）。

#### 二、★ 裁决二十一（主管）：两处修改
**① 备份实现由「先 `wal_checkpoint(TRUNCATE)` 再复制」（T41 方案 A）改为 `VACUUM INTO`** —— 理由：方案 A **自己引入了一条降级路径**（checkpoint 遇并发读者/写者返回 `busy !== 0` ⇒ 只能放弃迁移 ⇒ 上表故障）。`VACUUM INTO` 是 SQLite 官方一致性快照导出，**在单个读事务内完成、没有 busy 分支**（要么得到一致快照、要么抛错）⇒ **从根上不存在「迁移被跳过」**。
实测（`scripts/_tmp/probe_sup_vacuum_backup.mjs`）：源库 `-wal` 8,272 B 且写连接仍打开、表内 4 行 ⇒ 备份 **4 行齐全**（含只存在于 `-wal` 的行）· `integrity_check = ok` · `journal_mode=delete`（单文件，可直接拷回退）· **另一连接持未结束读事务时仍成功 ✔**。
代价：备份不再是主库的**逐字节**副本（页布局重排）⇒ **断言须改为逻辑内容比较**（已通知测试 agent 调整 E6/E20）。

**② 新增迁移后置校验 + 有界重试**（`packages/core/src/db/index.ts`）：
- 抽出 `bookPkCols(db)` / `isBookComposite(db)`（前置判据与后置校验共用，避免漂移）。
- **后置校验**：迁移流程走完后若 `book` 仍非复合主键 ⇒ **抛错**（`book 表未能迁移到 (word, lang) 复合主键…`），**不再允许静默降级运行**。权衡记录：抛错使 `openDatabase()` 失败，但「列表能看、写不进去」不是可用状态（`DEC-002` 正确性 > 可用性表象）。
- **有界重试**：迁移失败且错误信息含 `book_new`（残留表 ⇒ `table book_new already exists`）⇒ `DROP TABLE IF EXISTS book_new` 后**重试一次**（`isRetry=true` 分支只告警不再递归，杜绝无限重试）。**注**：真正的崩溃**不会**留下 `book_new`（整个迁移在 `BEGIN IMMEDIATE` 事务内），故这是恢复性动作而非正常路径兜底。

**端到端验证**（`scripts/_tmp/probe_sup_loud_fail.mjs`）：
| 场景 | 结果 |
| --- | --- |
| A 残留 `book_new` 表 | **openDatabase 成功** · 主键 `["word","lang"]` ✔ · 残留已清除 ✔（打印「检测到残留的 book_new 表，已清除并重试一次迁移」） |
| B 新库（直建复合主键） | 不受影响 · `bookAdd` 成功 · **备份数 0**（新库不产生备份）✔ |
| C 老结构库（无干扰） | 一次迁移成功 · 行数 2 保持 · 备份恰 1 个 · **备份主键仍为 `["word"]`**（= 真正的**迁移前**快照）✔ · `integrity_check = ok` |

#### 三、★ 门禁现状（T45 后实测：core 112 通过 / 9 失败 exit 1；desktop 22/0 exit 0）
9 条红**全部是行为变更造成的预期回归**，无一是未预期的缺陷：
| 红项 | 成因 | 处理 |
| --- | --- | --- |
| **A26** 迁移失败后 `book` 仍是老结构 | **自动恢复生效** ⇒ 迁移反而成功（主键变 `["word","lang"]`） | 派 T46 改写为「自愈后成功」断言 |
| **A28** 「尝试迁移即已先落备份」实测备份数 **2** | 重试路径各落一次备份 | 派 T46 改为「≥1 且机制正确」 |
| **E6 / E20** 备份与主库**逐字节相等** | `VACUUM INTO` 的**预期**代价 | 派 T46 改为**逻辑内容比较** |
| **E11 / E12 / E13 / E14** 变体删 checkpoint 的鉴别力自证 | 生产已无 checkpoint 逻辑 ⇒ **补丁失配**（她写的 `补丁失配即红` 守卫**正常工作**） | 派 T46 改为「删 `VACUUM INTO` 换成 copyFileSync」变体 |
| **E15** busy 放弃迁移的边界登记 | **该分支已不存在** | 派 T46 反转为「持未结束读事务时迁移**仍成功**」 |
> **评价**：块 E 的 7 条红**不是她写错了** —— 她按当时的实现正确编码了断言；是主管在 T45 更换了实现。**这正是「断言有鉴别力」的证明**（E6/E11 明确标注了「换成 `VACUUM INTO` 即红」）。

#### 四、需求侧同步（派 T47）
`AC-12⑨` 当前写的是「先 `wal_checkpoint(TRUNCATE)` 并回、`busy !== 0` ⇒ 放弃迁移」⇒ **已过期**，须改为：① 备份用 `VACUUM INTO`（一致性快照，**无 busy 分支**）；② 后置校验失败 ⇒ **抛错**（不得静默降级）；③ 残留 `book_new` ⇒ 清除后**重试一次**；④ 备份断言为**逻辑内容等价**而非逐字节。

---

## 十、T52 收尾 ＋ T53 发布记录（v0.10.0 已发布）

### 10.1 T52（测试 agent `6b08a4c5`）—— 日志文案同步，**1 轮内完成**
- `packages/core/test/book_lang.mjs` **134（132 通过 / 2 失败）→ 137 通过 / 0 失败**，两种 cwd 均 exit 0
- 净 **+3 条**：2 条文案红转绿 ＋ 3 条新增钉死断言；逐块 **[A] 42 · [B] 26 · [C] 18 · [D] 25 · [E] 26**
- **单一来源**：新增 `REUSE_LOG_RE`（`book_lang.mjs:466`）＋ `reuseLogName/reuseLogNames/reuseLogs` 三个 helper，全文不再手写复用文案串
- ★★ **她指出的一处假绿风险（已采纳）**：**反向**断言（「不得出现复用日志」）若仍匹配**旧**串，在 T51 下**恒为 0** ⇒ 永远通过。故正向与反向断言**统一走 `REUSE_LOG_RE`**，旧串只留在注释与失败提示里。
- **新增 3 条**（而非主管要求的 1–2 条，理由：S2 在 T51 下有两个语义入口，且**重试路径是复用唯一的合法场景**）：
  - `A28c`（`:935`）S2 正常路径：只有 `<db>.bak-<ISO>-wal` sidecar ⇒ 必**新建 1 份**可用快照 ∧ 无复用日志
  - `A28d`（`:959`）S2 重试路径：**恰 1 条**复用日志 ∧ 文件名 == 目录内唯一真实快照（≠ sidecar）∧ 内容 == 迁移前 ∧ 可用快照数 == 1
  - `A28e`（`:986`）S1：陈旧异库 `.bak-<ISO>` **不得**当回退点 ⇒ 总数 == 2 ∧ 新建份内容 == 本次迁移前 ∧ 陈旧件未被改写 ∧ 无复用日志
- **探针复核**（`scripts/probe_t50_reuse_edges.mjs` 已改并实跑 exit 0）：S1「复用了陈旧文件 = **NO**」＋「新建份内容 == 本次迁移前 = **YES**」；S2「可用快照数 **1**」＋「迁移在零可用快照下进行 = **NO**」⇒ **T50 两个洞在 T51 下均已堵死**，且任一洞回归即变红

### 10.2 ★★ T52 澄清一处主管表述（已采纳）：跨启动累积**仍然存在**
- 主管此前写「T49 复用顺带修掉跨启动累积 ⇒ `[1,1,1]`」是**错的**。
- 实测 **`[1,2,3]`**：`openDatabase()` 每次调用都传**新的** `{ bak: null }` ⇒ **复用只在同一次调用内生效，跨启动不复用**。
- ⇒ 已同步订正三处：`docs/release-v0.10.0.md` §四.1、§六-B4，以及本条记录。**性质**：一次自愈仍恰 1 份 ✔；跨启动累积 ✘（有意取舍 —— 备份是最后一道防线，宁可占磁盘也不删；但需失败时的人工介入指引）。

### 10.3 ★ 账本口径：**旧「506」结论数没错，错的是「汇总方式」**（T54 订正主管的归因）
主管初稿写「旧账本漏了 `ru_morph.mjs` 的 60 条」并给出 `99+67+38+194+26` —— **该式子本身不自洽**（= **424**，而 424+22 = **446 ≠ 506**）。**T54 需求 agent 顶回并按实情落盘，主管采纳**：
| 口径 | 构成 | 值 | 自洽性 |
| --- | --- | --- | --- |
| **旧·分项（含 60）** | core 484 = 99+67+38+**60**+194+26 ＋ desktop 22 | **506** | ✅ 自洽（此即 v0.9.0 真实账本） |
| **旧·分项（不含 60）** | 99+67+38+194+26 = **424** ＋ desktop 22 | 446 | — |
| **新** | core **621** = 99+67+38+**60**+194+26+**137** ＋ desktop 22 | **643** | ✅ |

**真正的根因是汇总方式，不是「484」这个数**：`ru_morph.mjs` 的计数行**没有「结果：」前缀**，凡按该前缀做后缀/前缀式汇总的账本都会**静默漏掉这 60 条** ⇒ 于是并存「484（含 60）」与「424（不含 60）」两种**分项口径**。
⇒ 定版见 **`DEC-027`**（门禁账本口径）；旧口径按 `DEC-014` **保留为历史值**。
> 本项目**第 5 次**同类计数陷阱（前四次：`Measure-Object -Line` 不数空行 · 无「结果：」前缀漏 60 · `_tmp` 误提交 · `/^---\s+(.+?)\s+---$/` 漏计数）。已写入 `AGENTS.md` §1。

### 10.4 T44 桌面端冒烟（v0.9.0 遗留项，**已闭环**）
方法：`apps/desktop/node_modules/electron/dist/electron.exe apps/desktop --user-data-dir=<临时目录>` ＋ **库副本**（不碰生产 profile 与冻结库）。
| 路径 | 结果 |
| --- | --- |
| 常规启动（复合主键库副本） | `dict.db opened, 770611 words` · `app version 0.10.0` · dump 含 **5 个顶层面板** · 页脚 `v0.10.0` · 词根分类面板**空态**正确 · `exit 0` |
| **真实老结构库迁移**（`data/db/dict.db.bak-v02pipe`，272,752,640 B，pk `["word"]`） | 主键 → `["word","lang"]` ✔ · 3 行**逐字段相等** ✔ · 墓碑 3→3 ✔ · `words_i18n(ru)` **159,127 不变** ✔ · 生成 **1 份** `VACUUM INTO` 快照（259,026,944 B）✔ · `exit 0` ✔ |
> ⚠ 该次运行另报 `Error: no such table: roots`。**不是迁移缺陷** —— 表集合实测对比确认该老库**本就没有** `roots`/`affixes`（由 `build_roots_tables.mjs` 更晚生成），生产冻结库则有。属**老库特性**。
> ⚠ 冻结库完好：`data/db/dict.db` 518,242,304 B / mtime `2026-09-13 23:13:53` **未被触碰**；两次冒烟的临时目录已清除，并把 `.smoke_*/` 加入 `.gitignore`。

### 10.5 ★★ T53 发布完成（已全链路核验）
| 项 | 值 |
| --- | --- |
| commit | **`041c6e7`**（57 文件；污染扫描通过：无 `_tmp`/`dist/`/`.db`/`.bak`/`.cache`/`node_modules`/`.smoke`） |
| tag | 附注 **`v0.10.0`** → commit `041c6e7`；共 **15 个 tag** |
| Release | https://github.com/lzk101/zidiankaifa/releases/tag/v0.10.0 · `draft=false` `prerelease=false` **assets=4** · published `2026-09-13T17:53:56Z` |
| 附件 | `zidiankaifa-0.10.0-x64.exe` 175,041,867 B · `.blockmap` 182,886 B · `zidiankaifa-portable-0.10.0-x64.exe` 174,812,071 B · `latest.yml` 350 B（均 `uploaded`） |
| 同步 | `git rev-list --left-right --count origin/main...HEAD` = `0 0`；工作区干净 |
| 门禁 | **643 通过 / 0 失败**（core 621 ＋ desktop 22） |
| 版本一致性 | 4 个 `package.json` + 3 处文本 + **web dist 内嵌** + **`/health` 实测** 均为 **0.10.0** |
> `git push origin main` 本次**直接 exit 0**（旧文档记的沙箱绕行已不需要）；`gh` 仍需按 §3 方法取 token（实测 length 40 / 前缀 `gho_`）。

### 10.6 本次发布暴露的一处工具事实
- `gh release create` 的附件列表由 shell 展开时，`Get-ChildItem -Filter '*0.10.0*'` **不含 `latest.yml`**（其文件名无版本号）⇒ 清单预览会**少显示一个附件**。**实测 Release assets = 4，未遗漏**。今后预览附件数须**显式列出四个文件名**，不要靠版本号过滤。

---

## 十一、★★ 第 4 个常驻角色：项目结构管理 agent（用户指令，2026-09-16）

### 11.1 用户原词

> 「添加一个agent，专门做定期的项目结构管理，避免项目结构臃肿，每完成一次版本发布进行一次项目结构调整」

### 11.2 角色卡

| 项 | 内容 |
| --- | --- |
| 角色名 | **项目结构管理 agent**（第 4 个常驻角色） |
| 委任书 | `.board/roles/structure-agent.md`（建立日期 2026-09-16，建立时 HEAD `d558aa0`） |
| 独占写 | `.board/STRUCTURE.md`（台账）· `.board/structure/**`（脚本与证据）· `PROJECT_STRUCTURE.md`（**仅事实性更新**）· `.gitignore`（**只增规则**）· `scripts/audit_*` / `fix_*_corruption` / `structure_*` |
| 绝不碰 | `data/**`（整棵树）· `src/**` · `test/**` · 已跟踪文件的删除 · **任何 git 写操作** |
| 触发节奏 | **每次版本发布 + push 之后做快检**；每 5 个 release 或大版本（词库重建/批量落库）之后做全量审计 |
| 额度依据 | `.board/BASELINE.md` §7「结构预算冻结」 |

### 11.3 权限设计（本角色最关键的一条）

**分三类，判据全部可机械复核**：

1. **自主执行**（无需逐次批准，须逐条记录证据）：删 `_tmp/` `*.tmpdir/` `.tmp/` 残留 · 删早于「当前 + 上一版本」的 `dist-release/` 历史包 · 清缓存过期内容（不删目录本身）· 删测试重建的 `packages/core/test/…` 临时库目录（`scripts/_tmp/booklang_tmp/run-*`）· `.gitignore` 追加规则 · 修结构性文本损坏（须有稳定锚点且改后可验证）
2. **只列清单提请核准**：未被 `.gitignore` 覆盖的未跟踪文件 · **任何已跟踪文件** · `data/**` 的任何改动
3. **绝对禁止**：改 `src/**` `test/**` `packages/data-pipeline/**` · 上调自己的额度 · git 写操作 · `sandbox_permissions`

★ **保护清单（不可删）**：`data/**` 整棵树 —— 其中 `dict.db` 是冻结词库、`data/db/dict.db.bak-v02pipe`（272,752,640 B / 2026-09-09）是**唯一的真实老结构迁移夹具**、`data/raw/` 2.93 GB 是词库**重建原料**。**不得用文件大小或路径名判断可删性**（`raw/` 看着像垃圾，其实是原料）。

### 11.4 结构预算（冻结值，见 `.board/BASELINE.md` §7）

| 维度 | 硬上限 | 冻结时实测 | 余量 |
| --- | --- | --- | --- |
| 已跟踪文件总数 | **300** | 248 | 52 |
| `scripts/` 文件数 | **165** | 158 | **7 ⚠** |
| `scripts/` 顶层文件数 | **35** | 33 | **2 ⚠** |
| `dist-release/` 体量（递归） | **1.5 GB** | **5.49 GB** | **−3.99 GB ⛔ 已越线** |
| `data/` 体量（只报不删） | **4.5 GB** | 3.67 GB | 0.83 GB |
| 缓存合计 | **1.2 GB** | 0.79 GB | 0.41 GB |
| 临时残留 | **0** | 0 | — |

⇒ **一条已确认越线**：`dist-release/` 5.49 GB（历史安装包）。已核实自动更新走 GitHub Release 的 `latest.yml`（`apps/desktop/package.json:67-73` `publish.provider=github`），**不读本地目录** ⇒ 历史包可安全清理。

### 11.5 与我（主管）的权限分离

> **一句话**：**结构 agent 能删产物，不能删代码；能出方案，不能改架构。**

| 事项 | 结构 agent | 主管 | 需求 agent |
| --- | --- | --- | --- |
| 清理产物/缓存/临时残留 | ✅ 自主 | 事后复核 | — |
| 删**已跟踪**文件 | ❌ 列清单 | ✅ 裁决后执行 | — |
| 移动目录 / 重命名模块 / 拆包 | ❌ 只出方案 | ✅ 决策 | 记录决策 |
| 改 `data/**` `src/**` `test/**` | ❌ 绝对禁止 | — | — |
| 上调结构预算额度 | ❌ 不得自行上调 | ✅ 须用户拍板 | — |

### 11.6 排期

| # | 任务 | 负责人 | 状态 |
| --- | --- | --- | --- |
| **T55** | 首轮结构体检 + 基线固化：九项实测填满 `BASELINE.md` §7 的「当前实测」列 · 产出 `.board/STRUCTURE.md` 首版 · 给出 `dist-release/` 超线 3.99 GB 的**具体清理清单**（文件名 + 字节数，**先不执行**） | 结构 agent | 待派 |
| **T56** | 执行 `dist-release/` 清理（主管核准清单后）+ 复跑门禁 643 验证 | 结构 agent | 待 T55 |
| **T57** | 全量审计：`scripts/` 分类 MECE 校验（A 活跃/B 证据/C 一次性/D 已失效/E 临时）+ 引用完整性（哪些文档引用了哪些脚本）+ 死引用清单 | 结构 agent | 待 T55 完成后 |
| **T58** | **agent 登记册**（`.board/agents.md`）首版 + 临时 agent 回收判定 | 主管已代出首版，结构 agent 复核维护 | ✅ 首版已出 |
| **T59** | 每轮发布后复核 agent 注册表与回收状态（体检第 10 项：常驻 agent 恒为 4） | 结构 agent | 随快检 |

### 11.7 与既有 `scripts/audit_text_health.mjs` 的分工

- **字节健康**（控制字符 / 行中 CR / CR+BEL 同行）⇒ 已由 `scripts/audit_text_health.mjs` 覆盖（v0.10.0 结构整理时新建，`README.md:144` 的损坏即由此发现）。结构 agent **直接复用，不重复实现**。
- **结构维度**（文件数 / 体量 / 额度 / 分类 / 引用完整性）⇒ 结构 agent 的职责。

### 11.8 ★★ agent 结构管理（用户追加职责，2026-09-16）

> **用户原词**：「这个agent还要负责清理临时agent。只有主agent和三个常驻agent不能清理，结构管理agent还可以帮助主agent管理agent结构」

**★ 第一件必须说清的事：本工具 agent 层没有「删除」API。** 可用通道只有三条：

| 通道 | 实际作用 | 属于清理吗 |
| --- | --- | --- |
| `list_agents` | 只读快照 | 是**依据** |
| `interrupt_agent` | 停某 agent **当前一轮**（之后仍可被唤醒） | 是**止损**，唯一直接动作 |
| `send_message` | **发起新一轮 = 唤醒** | **反清理**（结构 agent 禁止使用） |

⇒ **「清理临时 agent」= 台账化 + 标记可回收 + 报主管请其止损**，**不是物理删除**。台账**禁止**把「标记已回收」写成「已删除」。
⇒ **`ready` ≠ 会话结束/上下文已释放**，它只表示「可被续跑」。**把 `ready` 的临时 agent 当「已死」而随手重派，会重复消耗上下文并可能产出与既有结论冲突的第二版** —— 这正是登记册要防的主要浪费。

**受保护名单（恒 4 个，结构 agent 不得清理、不得 `interrupt`）**：

| # | 角色 | id |
| --- | --- | --- |
| 0 | 主 agent / 项目主管 | 本会话顶层 |
| 1 | 需求管理 agent | `4ce66e63-8f5f-4b15-8203-c23b3dccce6f` |
| 2 | 开发编程 agent | `6c6cedb8-f03d-49d3-ab1a-c1aa2d9dc62c` |
| 3 | 功能测试 agent | `7ff57407-f229-40e9-ba31-3d3e58b9655b` |

（第 5 个是**结构管理 agent 自身** `2d4ba4d0-64b5-4449-a295-66c327942977`，它是执行者，不在被清理对象内。）

**首轮快照实测（2026-09-16，**主管 T55 权威口径（第二轮终局）**，见 `agents.md` §4）**：受保护 **4** · 结构常驻 **1** · **agent 总数 14** · **临时 agent 累计 9**（depth1 = 7 / depth2 = 2 · 全部 `ready`）· 可回收 **7** · 异常·未落盘 **2** · 孤儿/重复/失效 **0**。
> ⚠ **本行曾连续错两次，均已由结构 agent 拦下**：① 原写「临时累计 10」——把**结构 agent 自身**计入了临时列表（它正是「明细缺 1 行」假象的根因）；② 我第一次订正时**应减 1、实减 2**（写成 8/depth1 6），被结构 agent 用「**可回收 7 ＋ 未落盘 2 = 9**」的算术交叉校验拦下，终局值 = **临时 9 / 总数 14**。
> **两条纪律**：① **结构 agent 自身永不计入「临时 agent」**（它是执行者与写者）；② **统计表必须与明细行做算术交叉校验** —— 本项目口径/计数类陷阱至此已第 7 次，**统计表是唯一能对明细做算术审计的地方**。

**已登记的结构不良项**：三个常驻 agent 的 **label 只记录了首轮任务**（`6c6cedb8` 的 label 还写着「开发 agent 启动+可达性判别」，`7ff57407` 写着「测试 agent 启动+判别集」）⇒ 从快照看不出它们就是当前常驻角色。

### ★★ 11.9 主管裁决（T55 回执后，2026-09-16）—— 三项

**裁决 A · label 过时：不重建。** 采纳结构 agent 的三条独立理由（均强于我的原版理由）：
1. **重建会丢弃唯一不可再生的资产 = 上下文** —— 本项目多次依赖历史 agent 复述其当时的取舍（最典型 `book_lang.mjs` 块 E 的 T42→T46→T50→T52 判据演化链），这些"为什么当时这么定"**不在任何落盘文档里**。
2. **重建会主动制造本册要防的风险** —— 「重派产出与既有结论冲突的第二版」，为改名而重建 = 明知故犯。
3. **label 是创建时刻的历史事实，不是可变字段** —— 改它 = 改写留痕，与 `DEC-014` 同源。
> **📌 落盘规则（结构 agent 提议，主管采纳）**：**常驻 agent 的 label 为创建时历史，只读；职责以 `.board/agents.md` §1/§2 为准。** 今后**不得**再提议为改名而重建常驻 agent。

**裁决 B · 「常驻 4」口径三分（采纳结构 agent 的 D3 建议，本项目口径陷阱第 7 次）**：

| 口径 | = | 含主 agent | 含结构 agent | 用途 |
| --- | --- | --- | --- | --- |
| **受保护 4** | 主 ＋ 业务常驻 3（需求/开发/测试） | ✅ | ❌ | **用户的清理豁免白名单** |
| **业务常驻 3** | 需求 / 开发 / 测试 | ❌ | ❌ | 角色计数 |
| **结构常驻 1** | 结构管理 agent | ❌ | ✅ | 执行者（**既不被清理、也不是被豁免对象**） |
| **常驻合计 5** | 受保护 4 ＋ 结构 1 | ✅ | ✅ | **体检项口径** |

⇒ **体检第 10 项改述为「常驻合计恒为 5（受保护 4 ＋ 结构 1）」**（原写「常驻恒为 4」会漏掉结构 agent 自身）。

**裁决 C · 两个未落盘孙代理：结案（不再追查、不重跑）。** 主管侧追查结果：

| 项 | 结论 |
| --- | --- |
| 结构 agent 的四通道 | 全 0 命中（`exp_v9_admission.mjs` / `out_v9_admission.json` / `EVIDENCE.md` §41 / `git log --all`） |
| **主管侧第五通道（决定性）** | **`.board/EVIDENCE.md:1541` `### 41.5 逐条候选审定表（按边际增益降序；每条均附可复核联网词源来源）`** —— **开发 agent 自己在 T17 里已完成逐条词源审计**（`:1548-1558` 是 11 行带 Wiktionary 链接的审定表） |
| ⇒ 定性 | 两个「Audit etymology batch A/B」子代理的工作**与父任务内置的 §41.5 完全重复** ⇒ 它们**本就无独立产出可落盘**，这解释了"查无痕迹" |
| ⇒ 处置 | **记为「已沉没的子任务 · 证据未固化 · 与 §41.5 重复」并结案**。**不再唤醒**（唤醒 = 重做一遍已内置于 §41.5 的审计，且两个 batch 会互相重复） |
| ⚠ 副产物 | 结构 agent 的**「归属存疑」异常类成立且值得保留** —— 它的判据（父子主题不一致 ⇒ 不据父任务推断子任务产出）**正好阻止了一次错误的"并入父产物"结案** |

**新增台账**：`.board/agents.md`（agent 登记册，结构 agent 独占写；含受保护名单 / 临时 agent 登记与回收判据 / label 映射 / 异常分类）。
**新增体检项**：第 10 项 = **常驻合计恒为 5（受保护 4 ＋ 结构 1）**（按裁决 B 改述）。
**新增排期**：**T58** = agent 登记册首版 + 回收判定（✅ 主管已代出首版）· **T59** = 每轮发布后复核注册表与回收状态 · **T60** = 修 `booklang_tmp/run-*` EPERM 根因（**测试 agent 写域**，见 `BASELINE.md` §7 订正块第 4 条）。

### 11.10 ★ 主管裁决（T55 二轮回执后）—— 追加两项 + 一处终局订正

**裁决 D · 「结构 agent 自身不计入临时」已确立，但我的算术连错两次（第三次被拦）**
| 轮次 | 我写的 | 真值 | 谁发现 |
| --- | --- | --- | --- |
| 建册时 | 临时 **10** | — | 结构 agent 建册时即报「统计与明细不符（10 − 9 = 缺 1 行）」 |
| 一轮订正 | 临时 **8** / 总数 **13** | **9 / 14** | **结构 agent** 用 `可回收 7 ＋ 未落盘 2 = 9` 算术交叉校验拦下 |
| 二轮终局 | 临时 **9** / 总数 **14** | ✔ | 主管采纳 |
⇒ **纪律**：**统计表的分类计数必须与其明细行数做算术交叉校验**（本项目口径/计数类陷阱已第 7 次：`Measure-Object -Line` 不数空行 · `ru_morph.mjs` 无「结果：」前缀 · `_tmp` 误提交 · `/^---\s+(.+?)\s+---$/` 漏计数 · 临时脚本作证据源 · 转述无出处的「误伤 7 词」 · 把启发式计数当端到端实测）。

**裁决 E · 两个未落盘孙代理：结案（采纳结构 agent 的「不再追查、不重跑」建议）**
- **主管侧第五通道（决定性，结构 agent 四通道之外的）**：**`.board/EVIDENCE.md:1541` `### 41.5 逐条候选审定表（按边际增益降序；每条均附可复核联网词源来源）`** —— **开发 agent 在 T17 内部已完成逐条词源审计**（`:1548-1558` = 11 行带 Wiktionary 链接的审定表）。
- ⇒ 两个「Audit etymology batch A/B」的工作**与 §41.5 完全重复**，本就无独立产出可落盘；这解释了四通道全 0 命中。
- ⇒ **记为「已沉没的子任务 · 证据未固化 · 与 §41.5 重复」并结案**（在 `agents.md` §3.2 已登记；**不再唤醒** —— 唤醒 = 重做一遍已内置于 §41.5 的审计，且两个 batch 会互相重复）。
- ✅ **副产物**：结构 agent 的**「归属存疑」异常类成立且应保留** —— 其判据（父子主题不一致 ⇒ 不据父任务推断子任务产出）**正好阻止了一次错误的「并入父产物」结案**（首版正是这么推断的，本轮被它自己推翻）。

**裁决 F · 两项建议采纳**
1. **label 规则落盘** → 见 §11.9 裁决 A 末段（「常驻 agent 的 label 为创建时历史，只读；职责以 `.board/agents.md` 为准」）。
2. **「常驻 4」三分口径** → 见 §11.9 裁决 B（受保护 4 / 业务常驻 3 / 结构常驻 1 / **常驻合计 5**）；**体检第 10 项改述为「常驻合计恒为 5（受保护 4 ＋ 结构 1）」**。

**裁决 G · `interrupt` 动作 = 0 次（确认结构 agent 的判断）**
它指出 **7 个可回收者与 2 个孙代理全部 `ready`、无一个 `running`**，而 `interrupt_agent` 的语义是「停当前一轮」⇒ **对 `ready` 者调用是无操作**。⇒ 本轮**不需执行任何 `interrupt`**；请求的是「**不再唤醒**」这条纪律的确认。**该判断正确，照此办理。**

---

## §11.11 · T55 收口（提交 `fe2da36`）与 T60 派发

### 一、提交与复核
- **提交 `fe2da36`**「board(structure): T55 首轮结构快检收口」（4 文件 +78/−22：`.board/BOARD.md` · `.board/STRUCTURE.md` · `.board/agents.md` · `PROJECT_STRUCTURE.md`）→ **已 push**，`origin/main...HEAD` = `0 0`，工作区干净。
- **主管独立复核结构 agent 申报数字（全部命中）**：已跟踪 **253** · `dist-release/` **8 文件 / 700,062,244 B** · `dist-release` 已跟踪数 **0** · `.board` tracked **14** · 污染扫描通过。

### 二、★★ 新增操作性约束（结构 agent 提出，主管采纳为纪律）
> **在 `scripts/_tmp` 根因修好之前，任何一次 `pnpm --filter @zidiankaifa/core test` 都会重新制造 1 个 `run-*` 目录 ⇒ 体检⑦「临时残留 = 0」与「门禁跑绿」在当前根因未修时互斥，二者只能保证其一。**

- **这正是结构 agent 本轮未复跑门禁的理由**（防主动制造越线）。⇒ **纪律：结构 agent 复跑门禁后，体检⑦ 允许为「复跑产生的 N 个 `run-*`」，不得据此判越线；须与「本次复跑次数」对账。**
- 最近一次门禁实测（T55 清理后）：core **621/0 exit 0** · desktop **22/0 exit 0** ⇒ 合计 **643/0**。

### 三、`_tmp` 死引用的真实根因（制度自相矛盾，非证据丢失）
- 三处死引用：`.board/REQ.md:383`（`.board/_tmp/count_defects.mjs` ＋ `defects_out.txt` 标「有意保留，勿当垃圾清理」）· `.board/REQ.md:1318`（`.board/_tmp/probe_t47_backup_fail.mjs` 标「未删除」）· `.board/BOARD.md:2440`（`scripts/_tmp/out_t24d_diff.txt` 标「保留」）—— **四目标均不存在**。
- **根因**：目标**全在 `_tmp/` 目录**，而 `roles/structure-agent.md` §3.1 **授权结构 agent 自主清理 `_tmp/`** ⇒ **同一制度内一处说「有意保留」、另一处说「可自主清理」**。
- ⇒ **新判据（已下令写入 `STRUCTURE.md` §7）**：**`_tmp*` 目录下任何文件，无论文档怎么写「保留/勿删」，一律按可清理处理；凡结论必须落在非 `_tmp` 路径或 DEC/AC 正文中。**

### 四、T60 派发（测试 agent `7ff57407`，已发出）
| 项 | 内容 |
| --- | --- |
| 目标 | 修 `packages/core/test/book_lang.mjs` 收尾 `rmSync` 的 **EPERM** 根因 + 清理遗留 `run-*` |
| 现象 | `EPERM, Permission denied: \\?\...\scripts\_tmp\booklang_tmp\run-<随机>`；**1:1（3 次测试 ↔ 3 目录）**；每目录恒 **54 文件 / 2,470,148 B**；**exit 仍 0**（全绿后才走到的分支） |
| 内容 | 全为合成夹具（`a5_old_with_lang.db` `b_isolation.db` `c_sync.db` `d_*.db` `e_{busy,prod,remnant,remnant_src,vacuum_ctl,variant}/` `dist_variant/`），**无断言输出/快照 ⇒ 不含留红取证** |
| 候选假设（要求验证，非照抄） | `DatabaseSync` 句柄未 `close()` · WAL sidecar 未释放 · `dist_variant/` 的 `file://` 动态 import 句柄 · Windows 延迟释放（`force/maxRetries/retryDelay`） |
| 约束 | **137 条断言数与语义不变**（已发布 v0.10.0 门禁文件）· **两种 cwd 均 exit 0** · 清理失败**不得**改变退出码语义（若要改须先报主管）· 只改该文件 · 不改其余 6 个门禁测试文件 · 无 git 写操作 |
| 验收 | 「修复前 N 个目录 → 复跑 → 修复后 0 个（或不增长）」实测链 + 遗留目录清理数 |

## §11.12 · T55 三轮回执裁决（5 条，全部采纳结构 agent 的顶回）

> 结构 agent 本轮**顶回 4 处 + 提请写死 1 处口径**。主管已逐条独立核验，**5 条全部采纳**，其中 2 条是**主管自己的错**（见「§11.12.6 主管两处被证伪」）。

### 裁决 1 · 档位优先级改为 `D > E > A > F > B > C`（采纳顶回）
- **问题**：`scripts/exp_ru_ceiling.mjs` 是**现役工具**（`A` 档，cites = **17**），但其文件名是 `exp_*`（⇒ 形态上落 `F`）。优先级 `D > E > F > A > B > C` 下 **`F > A` ⇒ 唯一分配会把它判进 F（实验台）**，等于降级一个现役工具。
- **全库仅此 1 个文件受影响**（29 个 `exp_*` = F 24 ＋ D 4 ＋ **A 1**）。
- **裁定**：**`A` 是角色，`F` 是形态；角色压过形态** ⇒ 优先级写死为 **`D > E > A > F > B > C`**。
- **配套纪律**：**唯一分配制**（一个文件只进一个档）**不变**；冲突时按此序裁决，**不得因文件名前缀覆盖角色判定**。

### 裁决 2 · D 档判据改写为「无任何**把它当有效依据**的引用」（采纳顶回）
- **问题**：按字面「当前无任何活跃引用」执行 ⇒ **D 档成空集**，因为现有 5 个 D 档条目 **cites 全 > 0**（5 / 7 / 7 / 6 / 5）—— **作废声明本身就是引用**（`DEC-014` 强制原地加取代声明，不删历史）。
- **裁定**：**判据改为「无任何『把它当有效依据』的引用」**；**作废/取代声明不计入**。
- **保留理由**：字面执行会把 `scripts/exp_ru_recoverable.mjs` 等降到 F/C，**反而丢掉「勿引用 86.7%」的显式标记** —— 那是本项目最有价值的反面教材之一。

### 裁决 3 · D 档判据粒度下沉到 `文件:行/节`（采纳顶回）
- **问题**：`scripts/exp_v9_criterion_d.mjs` 的问题**只在 `:97` 的 `EX_ALL`**（未经核证的注入件），而**同一文件被 `BOARD.md:1949` 引为「★★ 权威表（单一实现）」** ⇒ **判整文件进 D = 连带作废一条权威实现**。同理 `scripts/probe_t15_reconcile_and_harm.mjs` **仅 §B 作废**（「96 词真误伤」）。
- **裁定**：**D 档条目标注必须写到 `文件:行` 或 `文件:§节` 粒度**；文件其余部分的有效性**不受牵连**。
- **代价确认**：结构 agent 已声明**未逐行通读** `exp_v9_criterion_d.mjs` 确认其余部分仍有效 ⇒ **按裁决 3，未通读不构成阻塞**（D 档只钉 `:97`）。

### 裁决 4 · F 档内保留子标注「⚠ 不得重跑」（采纳顶回）
- **问题**：`scripts/apply_v9_morphemes{.mjs,2.mjs}` 原判据是「**不得重跑**（已落库）」，而 F 档名「实验台」**暗示可随意跑**。
- **裁定**：**F 档条目可带子标注**；上述两条**必须**标 `⚠ 不得重跑`（`origin` 落库已完成，重跑会重复注入）。
- **落盘坐标澄清**：结构 agent 问「『不得重跑』是否另有落盘坐标」—— **答：无独立 DEC，本裁定即为其落盘坐标。**

### 裁决 5 · agent 总数写死为 **14（物理口径）**，并附构成式（采纳其推荐值）
- **二选一**：**14（物理口径）** = 受保护 4 ＋ 结构 1 ＋ 临时 9（与 `list_agents` 输出条数一致）**✅ 采纳** / ~~13（管理口径）~~。
- **依据**：主管本轮快照**抬头写「13」而其枚举条目实为 14 条** ⇒ 本册 §4 的 14 **获主管自身枚举背书**。
- **★ 纪律升级（防本项目第 5 次同类计数陷阱）**：**凡写「总数」，必须紧随其后附「＝ A ＋ B ＋ C」构成式**，使集合边界自证。写作范例：`agent 总数 = 14 = 受保护 4 ＋ 结构 1 ＋ 临时 9`。

### §11.12.6 · 主管两处被证伪（结构 agent 字节级核验，均已采纳）
| # | 我的说法 | 实测 | 后果 |
| --- | --- | --- | --- |
| 1 | 删除量 **4,345,647,922 B** | **4,345,641,661 B** | 差值 **6,261 B == `dist-release/builder-debug.yml` 实测 6,261 B** ⇒ **我那一桶把保留件 `builder-debug.yml` 计入了删除**。**按我的桶执行会误删保留件**；结构 agent 的 **36 文件清单正确**（`latest.yml` 350 B 确在位，非它） |
| 2 | `git ls-files` = **251** · `.board` = **12** | **253 / 14** | 差 2 = **我自己的 `2a796c9` 新增的 `.board/structure/classify_mece.mjs` ＋ `scan_dead_refs.mjs`** ⇒ 两值在提交那刻正确，**是那笔提交让它们变旧** |
- ⇒ **纪律**：**凡「已跟踪文件总数」必须带时点或 commit SHA**（否则同一数字在不同时刻都"正确"却互相矛盾）。
- ⇒ 本条是本项目**第 8 次同类计数陷阱**（前 7 次见 `.board/agents.md` §4）。**两次都是「统计 ↔ 明细」对账 + 与独立分类交叉校验发现的**，不是靠"再看一眼"。

### §11.12.7 · 通报（非裁决）
- **T60 探针已出现（测试 agent 写域）**：`scripts/probe_t60_rm_forensics.mjs`（3,908 B）· `scripts/probe_t60_handle_hook.mjs`（4,086 B）—— **未跟踪、未被 gitignore** ⇒ 属「未跟踪待入库」**非垃圾** ⇒ **T60 收口时一并 tracked 或清理**。**结构 agent 不动（非其写域）。**
  > **✅ 已闭环（T60 尾款 `4548987`）**：3 份探针 `probe_t60_rm_forensics.mjs` / `probe_t60_handle_hook.mjs` / `probe_t60_leak_impact.mjs` **均已 tracked 入库**。
- **`scripts/_tmp` 现状 = 0 文件**（结构 agent 已清空，故 ⑦=0 ✅）；T60 修好后「⑦=0 ↔ 门禁跑绿」互斥即解除。
  > **⚠ 该结论在 T61 未落地前不成立（2026-09-16 主管实测）**：复跑 core 门禁后 `scripts/_tmp/booklang_tmp/` **仍有 3 个 `run-*`** —— T60 的「残留恒 0」靠测试侧 `reclaimLeakedConnections()` **兜底回收**得来，**非根因修复**。根因见 §11.13.7 `V11-OPENDB-LEAK`。

---

## §11.13 · T61–T64 派单与裁决（主管，2026-09-16）

> 用户本轮指令原文：**「精简项目结构，包括文件结构和agent结构」**（继 T55 后第 4 轮结构治理）
> 用户两项授权（`ask_user_question` 答复）：① 删除范围 = **「只删『Ⅰ 可清理』档，Ⅱ 档逐条报我裁」**
> ② 根目录僵尸文档 = **「移入 `docs/legacy/` 归档目录」**（**推翻 `DEC-008` 原「保留原位」**）

### §11.13.1 · 已提交清单（均 push，`origin/main` 同步）
| commit | 内容 |
| --- | --- |
| `0155a64` | **T63** 需求 agent：`.board/DECISIONS.md`（672 → **710** 行）· `.board/REQ.md`（1336 → **1338** 行） |
| `98613ac` | **执行 `docs/legacy/` 归档**：5 文件 `git mv`（均 **`R100`**）+ `PROJECT_CHARTER.md` 铁律 6 重写 + `docs/交接文档.md` §8 + 新增 `docs/legacy/README.md` |
| `17ef659` | **T62**：删除 **39** 个零引用脚本 + 新增 `.board/structure/recheck_zero_refs.mjs` |

### §11.13.2 · 裁决一 · **否决「改名解除禁令」**（需求 agent 建议 ①）
需求 agent 提议归档时改名 `check_requirements.py.archived`（「禁令防故意，改名防疏忽」）。**否决**，理由落盘于 `docs/legacy/README.md`：
- 本项目**口径陷阱已发生 10+ 次**；为**同一件东西制造第二个合法文件名形态**，恰恰**生成新的口径歧义**；
- `DEC-008` 修订块的「文件身份」判据**本身就是文件名**，改名 ⇒ **第二次口径分裂**；
- 已显式写明：**「改名不等于解除禁令 —— 即便改名成 `*.archived`，运行它仍被禁止」**。
- **采纳其建议 ③**（为 `docs/legacy/` 定义「只增不改的归档区」写权限）⇒ 已写入 README。
- **采纳其建议 ④ 并把根因写进 `PROJECT_STRUCTURE.md` §6**：我原裁定出错在**把「禁令的坐标系」当成不可变给定条件**，于是「坐标会失锚」反成**阻止移动**的理由。**正解 = 改写指代（位置 → 身份），而非为保住旧坐标放弃治理**（与 `DEC-023` 同类）。

### §11.13.3 · 裁决二 · T62 零引用清理 —— **Ⅰ 档判据必须自建，两份既有分析共享盲点**
- **两份独立分析（我的 20 文档语料 / 结构 agent `classify_mece.mjs`）都得「零引用 = 52」，但语料都不含 `packages/**`·`apps/**`·`scripts/**` 自身** ⇒ **共享盲点**。
- 建 `.board/structure/recheck_zero_refs.mjs`（只读四通道）复核 ⇒ **救回 11 个**，其中两个决定性反例：
  - `probe_t15_d6_residual.mjs` ← `packages/core/test/ru_morph_defects.mjs`
  - `probe_t32_noi_audit.mjs` ← `packages/core/test/ru_morph_v090_guard.mjs:391`（逐字写「由 T32 独立发现（`scripts/probe_t32_noi_audit.mjs`）」）
- **★ 复核器自身暴露两层缺陷（均已修并留痕）**：
  1. **brace 声明式引用**：`book_lang.mjs:131/:401/:1765` 写作 `scripts/probe_t42_wal_fixture{,2,3}.mjs` ⇒ **字面量 `probe_t42_wal_fixture2.mjs` 在该文件里根本不存在**（`.Contains()` 实测 `False`）⇒ 任何字符串匹配都抓不到 ⇒ 判据补 **brace 展开**。
  2. **路径未 normalize**：正斜杠比较对 `packages/`·`apps/` 下文件**全部失效** ⇒ 差一点误删 `probe_t32_noi_audit.mjs`。
- **执行**：删 **39** 个（`git rm`，历史保留于 `98613ac`）。**保留 1 个** `exp_v9_signature.mjs`（被 `BOARD.md`/`TASKS.md` **真引用** ⇒ C 档）。
- **量化**：删 39 个只减 **258 KB / 14.3% 体积**，但减 **27.5% 文件数** ⇒ **价值在导航性**（「结构臃肿」的真实含义），**不在体积**。
- **Ⅱ 档（报主管裁）**：**无** —— 复核后 40 个真候选中 39 个我判 Ⅰ 档、1 个（`exp_v9_signature.mjs`）判 C 档保留，**故本轮无 Ⅱ 档待裁项**。

### §11.13.4 · 裁决三 · **T64 全库扇描提速 = 不做**（测试 agent 判别实验，采纳）
- **结论**：**99.4% 是 JS 拆解开销，SQLite 只占 0.6% 且根本不在计时区内** ⇒ **SQL 侧没有可优化的对象**（不是风险高，是**没有对象**）。
- 硬数字：逐词 SQL 调用 **0**（101,512 词全程 `prepare=0/get=0/all=0`）· 纯 SQL 取数 **64.9 ms** vs 完整扇描 **11,483.5 ms** · 分组**扁平**（长度 4–6/7–9/10–12/≥13 ⇒ **9,997/10,145/10,009/9,455 词/秒**；**词长 5.75× ⇒ 成本仅 1.17×**）⇒ 落点是**每次调用的固定开销**，非 DP 随词长增长。
- 候选：① 复用 `prepare` 落在噪声内且**该段不在 `scanMs` 内** ⇒ 收益 0；② **现实现（批量取）已比逐词 `prepare().get()` 快 ≈13–23×** ⇒ **已是最优形态**；③ `breakdownWord`（`packages/core/src/db/index.ts:961`）**函数体内 0 处 `db.prepare`**，唯一相关点 `loadMorphemes`（`:912`）的 `:917` `prepare` 已由 `morphemeCacheByLang`（`:910/:914/:929`）按 lang 缓存。
- **一并纠正我两处口径**（采纳）：① **「21 秒」在原文中不存在** —— `EVIDENCE.md:1299/1373` 记主管参考 **8,971 ms**、5 次实测 **12,059/16,998/14,230/12,409/19,209 ms**，**最慢 19.209 s**；② **`DEC-010` 不是性能条**（`DECISIONS.md:227` = 「全绿」的定义 226+20=246 项），性能口径在 **`TASKS.md:44`**。
- **将来若真要做**：唯一有实测支持的轴 = 每次调用的固定开销 ⇒ 需把「词素 → 候选结构」**按 lang 预计算一次**（`src` 侧，属开发 agent 写域），须重验 **33,495 / 63 / 134 / D1=0** 与 26 条守卫，**加速比尚未实测**。

### §11.13.5 · 裁决四 · **收回对开发 agent「越权」的指控**（我的判断错误，已订正）
- 我一度认定它越权改测试 agent 写域的 `scripts/check_v10_ui_contract.mjs`。**核验后收回**：
  - 该文件**由它自己在 `041c6e7` 创建**（`git log --diff-filter=A` 实测）；
  - `dev-agent.md:17` 明文允许它「**必要时新建自己的临时探针脚本（放 `scripts/`）**」⇒ **它是该文件的正当写者**。
  - ⇒ **我的分工表记错了边界。** 结构 agent 的 `.board/agents.md` 与我的记忆均须以**委任书原文**为准。
- **同时采纳它的 T39 顶回**：`apps/desktop/src/main.mjs:121-134` 的 `book:update` 守卫**已在 HEAD**（注释自标「v0.10.0 裁决十九 + T41」，白名单 `'en'|'ru'` + `throw`），**我引用的「裸 `bookUpdate(db, item)`」是过时行号表述**。它给出 `git show HEAD:` 级证据 ⇒ 采纳。
- **采纳它的口径建议**（待办）：在 `AGENTS.md` 记一行索引 —— 「`apps/web/src/App.tsx` 的 `bookList()` **全量读取 = 跨语言判定豁免**，见 `scripts/check_v10_ui_contract.mjs` 头部」。
- **★ 一条方法论教训（重要，本轮实测）**：我在**开发 agent 并行写 `apps/web/src/` 期间**跑门禁，测到 `[D] 24/1 · 136/1 · exit 1` 并曾误判为「偶发 flake」。**真相 = 它 v1 方案的中间状态**（它随后自己撞红 `book_lang.mjs` D22 并回退）。
  ⇒ **纪律：任何门禁数字必须在「工作区无并行写入」时采集；否则数字无效，不得作为结论。**

### §11.13.6 · 计数（**带时点 `17ef659`**）
- **已跟踪 219** = `scripts/` **99** + `packages/` 43 + `apps/` 38 + `.board/` 15 + `docs/` 13 + `data/` 1 + 根目录 10（预算 300）
- **`scripts/` 138 → 99**（预算 165）
- ⚠ **第 9 次同类计数陷阱（本轮新增，均为我犯）**：
  1. 我报「218」漏数 `.board/structure/recheck_zero_refs.mjs`（真值 **219**）；
  2. **`git ls-files` 默认对非 ASCII 路径加引号并八进制转义**，我按首段分组**凭空造出一个名为 `"docs` 的目录**（6 个）⇒ 需分组统计时**必须用 `git ls-files -z`**。**磁盘上不存在任何含字面引号的路径**（已实测证伪）。
- ⇒ 纪律升级：**分组统计一律用 `-z` 或 `core.quotepath=false`**；已写入 `PROJECT_STRUCTURE.md` §0。

### §11.13.7 · 未闭环（转 v0.11.0）
| 项 | 状态 |
| --- | --- |
| **`V11-OPENDB-LEAK`** | **T61 已派开发 agent**（修法 `try{…}catch(e){db.close(); throw e}`，**原错误须原样抛出**——`assertBookCompositeOrThrow` 的中文消息经桌面端 `dialog.showErrorBox` **用户可见**）。**主证据 = `book_lang.mjs` 收尾「漏关连接 6 个」⚠ 输出应消失**（该提示只在 `leaked > 0` 时打印）。⚠ `run-*` 残留「恒 0」**是测试侧兜底回收的结果，不能作为修好的证据**。 |
| **结构 agent `2d4ba4d0`** | **`[diagnostic: corrupt]`**，T62 派单**永久未回执**。它的写域（`.board/STRUCTURE.md`·`.board/agents.md`·`.board/structure/**`·`PROJECT_STRUCTURE.md`·`.gitignore`）目前**无人接管** ⇒ 本轮 `PROJECT_STRUCTURE.md` 的 §6 重写与数字订正**由我代写并注明**。**修复手段尚未确定**（工具层无删除 API；`send_message` 对 corrupt 者能否唤醒未验证）。 |
| **agent 结构精简** | **物理「精简 agent 数量」在本工具层做不到** —— `list_agents` 12 条子代理**全 `ready`**、`interrupt_agent` 对 `ready` 者**是无操作**。⇒ 只能做 **台账压缩 · 口径收敛 · 抑制新增**（结构 agent 的 T62-B 方案未产出）。 |
| `docs/legacy/` 后置 | `check_requirements.py` 归档后**写目标转移到 `docs/legacy/`**（不再碰权威 `docs/需求总结.md`），但**废弃横幅仍会被重写**；**风险随移回根目录立即复原**。 |

---

# ★★★ §12 · v0.11.0 立项 —— 手机端 + 端云互通（主管，本轮）

> **本节是 v0.11.0 唯一的立项依据。** 范围重大调整：原定的「正确性梯队」（`а-` 前缀缺失 / D2 通用空洞上限 / `термостат` 取舍 / `roots_ru.json` 四条 `origin` 订正）**整体推后到 v0.12.0**。
> 本节的**数字与结论全部为实测或联网取证**，凡非实测者一律标注「推断」。

## §12.1 · 用户原话与拍板（权威依据）
> 用户原话：「**下一版本先做手机端，以及手机端和电脑端的互通**」

| 问题 | 用户选择 | 备注 |
| --- | --- | --- |
| 手机端形态 | **Capacitor 包成真 App** | 未选 PWA、未选 RN/Flutter 重写 |
| 互通底座 | **云服务商 BaaS** | |
| 互通范围（多选） | **生词本双向同步** ＋ **词根分类/分组同步** ＋ **搜索历史/界面偏好** | **不含**「学习进度与复习记录」 |
| v0.11.0 内容 | **手机端做进 v0.11.0**，正确性梯队推后 v0.12.0 | ⇒ 原 §11.13.7 的 v0.11.0 待办**改挂 v0.12.0** |
| 云底座最终 | **Turso（libSQL）** | 主管否决 Supabase，理由见 §12.3 |
| Android 工具链 | **授权主管安装** JDK + Android SDK | 本机原先 `JAVA_HOME`/`ANDROID_HOME` 皆空 |
| 离线查词 | **内置高频词子集** | 既非全量内置，也非纯在线 |

## §12.2 · 架构（本迭代基准）
```
[手机 App (Capacitor)]  ─┐
[Web 浏览器 (现有)]      ─┼─→ HTTPS ─→ [云端 API 薄层: 鉴权/限流/白名单] ─→ [Turso 云 SQLite]
[桌面端 (Electron)]      ─┘                                              ├─ dict.db 主体（518,242,304 B，零改造直传）
                                                                         └─ 用户数据（生词本/偏好/历史）
[手机本地] ← 内置高频词子集（离线查词 + 离线生词本队列）
          ↑ 联网时双向同步（生词本 · 词根分组 · 搜索历史/偏好）
```

**三层数据分层**（写 REQ 时必须区分，不可混为「同步一切」）：
| 层 | 内容 | 同步性质 |
| --- | --- | --- |
| **共享只读层** | 词典本体：`words_i18n` / `morphemes` / `roots` / `affixes` / `word_etymology` … | 云上**一份**，三端读同一份，**不产生用户写入** |
| **用户私有读写层** | `book`（生词本，`(word, lang)` 复合主键）＋ 词根分组 ＋ 偏好/历史 | **需双向同步**（冲突解决见 §12.8 待裁） |
| **离线层** | 高频词子集（读）＋ 本地生词本队列（写） | 联网后上浮，**不丢不重** |

## §12.3 · 云底座选型（实测与取证）
**Turso 额度**（逐字取自 `https://turso.tech/pricing` 的 schema.org 结构化数据；**抓取时点 = 本轮**）：
> **Free $0/month**：`Databases: 100 · Storage: 5GB · Monthly Rows Read: 500 Million · Monthly Rows Written: 10 Million · Monthly Syncs: 3GB · Point-In-Time Restore: 1 day`
> **Developer $4.99/month**：`Unlimited · 9GB ＋ $0.75/GB · 2.5 Billion …`
> ⚠ **超限即停服**（原文）：「Once you exceed the limit on any single metric (storage, rows read, rows written, or syncs), your databases are blocked, even if the other metrics are still well under quota」—— free plan **没有「超额继续」**。

- **`data/db/dict.db` = 518,242,304 B（494.2 MiB）** ⇒ 装进 5 GB 有 **约 10 倍余量** ⇒ **存储不是瓶颈**；**rows read 才是**（free 层 5 亿行/月）。
- 客户端库：**`@libsql/client` 0.18.0**（`npm view` 实测可解析），`exports` **含 `browser`/`deno`/`workerd`/`edge-light`** 入口 ⇒ 浏览器/Capacitor 端可直接用，**不必手写 libSQL HTTP 协议**。
- **★ 否决 Supabase 的理由（硬的）**：其免费层 Postgres **500 MB**，而词库 **518.2 MB > 500 MB** ⇒ **装不下词典**；且需把 SQLite 转 Postgres（表结构、`rowid` 语义、FTS 全文索引全部重做）⇒ 正是本项目历史上被反复否决的「重造轮子」。
- **★ 选 Turso 的正面理由**：**SQLite 同格式** ⇒ `dict.db` **零改造直传**，`rowid % 97` 尺子 R、`morphemes`/`roots`/`affixes` 倒排表**全部原样可用**。

## §12.4 · ★ 安全红线（已定，不可挑战）
**客户端绝不直连 Turso；Turso token 只存服务端环境变量。**
理由：Capacitor 产出的 **APK 可解包**，token 一旦进客户端产物即等于**整库 + 全量用户数据泄露**。
⇒ 云上**必须有一层薄 API**（可改造现有 `apps/sync-server` 形态）：
1. Turso token **只在服务端**（env 注入，**不进任何客户端产物**）
2. 客户端只持有**用户级会话凭证**（可撤销、有作用域）
3. 服务端做**鉴权 + 限流 + 查询白名单**（只暴露既有 13 条路由语义，**不接受任意 SQL 透传**）
4. 附带收益：**rows read 额度由服务端统一管控**，可超限前熔断，避免 free plan 被一个坏查询打到「整库停服」

## §12.5 · 可复用资产（已核验，互通不是从零开始）
- **`apps/sync-server/src/index.ts`（8061 B）已有 13 条 REST 路由** ＋ `:93` `OPTIONS` CORS 预检：`/health`（`:104`）· `/api/v1/lookup`（`:113`）· `/suggest`（`:119`）· `/breakdown`（`:126`）· `/lexicon`（`:133`）· `/lexicon/entry`（`:150`）· `/lexicon/stats`（`:157`）· `/words`（`:162`）· `/book`（`:174`，POST/PUT `:195`）· `/book-groups`（`:182`）· `/related`（`:189`）· `/sync`（`:195`）
- **`apps/web/src/api.ts` 已有 `rest` / `electron` 双后端抽象** ⇒ 手机端接 REST 是**复用**既有接口层
- **web 端已有移动化底子**：`apps/web/index.html:5` `viewport-fit=cover` · `:6` `theme-color`；`apps/web/src/styles.css` 断点 `:1324`/`:1869`/`:2357`（`max-width:760px`）· `:2056`（`860px`）· `:192`（`780px`）

## §12.6 · Android 工具链（用户授权，主管自装）
**装前实测（全空）**：`ANDROID_HOME` / `JAVA_HOME` **均为空**；`java` / `gradle` / `adb` **全部未安装**。⇒ `npx cap add android` 能生成工程，但 **Gradle 编 APK 必失败**。
**落点 = 仓库根 `.android-toolchain/`**（已写入 `.gitignore`，**绝不入 git**）：

| 组件 | 实测值 |
| --- | --- |
| Temurin **JDK 17.0.20.1+1** | `OpenJDK17U-jdk_x64_windows_hotspot_17.0.20.1_1.zip` = **190,817,615 B**（下载 7 s）⇒ 解压 `jdk/jdk-17.0.20.1+1`，`java -version` 实测 `17.0.20.1 2026-08-18` |
| Android **commandline-tools** | `commandlinetools-win-15859902_latest.zip` = **155,655,386 B**（官方页面实时抓取的最新版号）⇒ `sdk/cmdline-tools/latest` |
| `platform-tools` · `platforms;android-35` · `platforms;android-36` · `build-tools;35.0.1` | 安装中（**最新基础平台 = android-36，无 37**） |
| Gradle 代理 | `gradle/gradle.properties` 写死 `systemProp.http(s).proxyHost=127.0.0.1` / `Port=7897`（**Gradle 不读系统代理**，不写则 `google()`/`mavenCentral()` 全超时） |

其他：磁盘 D 可用 **340,655,833,088 B（317 GiB）**；代理**仅 `127.0.0.1:7897`（Clash）开放**（7890/10809/1080 实测关闭）。
> ⚠ **iOS 编译必须 macOS + Xcode，Windows 上物理做不到** ⇒ 手机端 v0.11.0 只交付 **Android**；iOS 须在需求文档中**如实标注**，不得写成「未做」。

## §12.7 · 派单（三单并行，均已发出）
| 单号 | 承派 | 范围 | 硬约束 |
| --- | --- | --- | --- |
| **T66** | 需求 agent `4ce66e63` | `.board/REQ.md` 新增 **§REQ-V11-001**：架构总述 · **AC-20…AC-26** · DoD · 不做清单 · 三条红线 | **取代其未执行的 T65**；只改 REQ/DECISIONS/CHANGELOG |
| **T67** | 开发 agent `6c6cedb8` | **只判别实验不写实现**。A：Capacitor `webDir`/`apps/web/dist` 兼容性与 base URL 改造点清单 · B（**最有价值**）：`node:sqlite` → `@libsql/client` 差异面与**是否存在「零改造」路径**（embedded replica / `file:` 本地模式）· C：词频列探查与「高频」判据 | 探针前缀 `probe_t67_`；不写 `.board/REQ.md`/`DECISIONS.md` |
| **T68** | 测试 agent `7ff57407` | **高频词子集的断言级实测**：词频列有无 · N ∈ {5k,10k,20k,50k} 的**体积-覆盖率曲线**（对**尺子 R 791 词**、口径 A 101,512、英语词条）· 子集自包含性与**静默降级**清单 · `rowid % 97` 子集可复现性 | 探针前缀 `probe_t68_`；**只读冻结库**；不改 `src` |
> ⚠ **同题不重派**：T67-C 与 T68 都涉及「高频词」，但**分工写死** —— T67 只出**判据与方法**，T68 出**断言级实测数字**，避免同题两份互相冲突的结论。

## §12.8 · ★ 待裁事项（挂账，落 REQ 前须有结论）
| # | 事项 | 为什么必须裁 |
| --- | --- | --- |
| 1 | **云端用户数据的库位**：与 `dict.db` 同库 / 独立用户库 | **直接决定 Turso 成本模型**——同库则词典本体与用户数据**共用 rows read 额度**；独立库则**用户读写不影响词典额度**，且可单独回滚 |
| 2 | **跨端同步的冲突解决**：是否沿用 `apps/sync-server` 现有的 **last-write-wins** | 生词本含**墓碑（`deleted`）**；本项目墓碑曾因**经 API 层从 `0/1` 映射为 boolean** 造成**假红与假绿同源**（`AGENTS.md` §3 第 8 条）⇒ 必须写死口径 |
| 3 | **Capacitor 工程落点**：`apps/mobile`（第 4 个 workspace 包）还是根级 | 影响 `pnpm-workspace.yaml`、`packages/core` 复用方式、已跟踪文件计数预算（当前 **219**/300） |
| 4 | **离线子集的 N 与内容** | 待 T68 曲线出来再定；**不得先定体积后补依据** |

## §12.9 · 未闭环转 v0.12.0（原 v0.11.0 待办，整体顺延）
原 §11.13.7 未闭环项**不消失、不改口径**，顺延为 v0.12.0：`V11-OPENDB-LEAK`（**T61 已修复并入库 `2c908e8`**，此项实际已闭环）· `а-` 前缀缺失 · D2 通用空洞上限 · `термостат` 取舍 · `roots_ru.json` 四条 `origin` 订正 · 结构 agent 席位口径 · `docs/legacy/` 后置风险。
> ⚠ **T65（需求 agent 的 v0.11.0 正确性 DEC 派单）已被 T66 取代**，若需求 agent 已产生部分落盘，须**如实登记写到哪**（依 `DEC-014`，已落盘史实句不得回改，只标状态）。







