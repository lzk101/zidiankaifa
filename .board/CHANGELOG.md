# CHANGELOG.md — 发布流水

> **维护者**：需求管理 agent（独占写权）
> **格式**：按版本倒序。每条 = 版本 / 日期 / 主题 / 关键数字 / 验收结果 / Release 链接。
> **规则**：`v0.2.0 → v0.7.1` 为**回填**（来源：`docs/交接文档.md` §4 版本时间线，标注「回填」）；**新版本在发布当日由本 agent 追加，不由发布者自己写**，以保证「文档声称」与「实际发布」对得上。
> **交付边界**：用户定的规则是「**每个迭代完成即直接 bump 版本 + 打包 + 发 GitHub Release**」，故本文件的条目与 `.board/REQ.md` 的迭代是一一对应的。

全部 Release：https://github.com/lzk101/zidiankaifa/releases

---

## 待发布

### v0.11.0 — 手机端（Capacitor 真 App）＋ 端云互通（Turso）（**迭代 4，已立项**）
- **日期**：待定（立项 2026-09-16）
- **对应需求**：**`REQ-V11-001`**（`.board/REQ.md` **§10**）；决策 **`DEC-028`**（方向拍板）· **`DEC-029`**（驱动不换）· **`DEC-030`**（R 不可移植）· **`DEC-031`**（用户维度阻断项）
- **来源（用户原话逐字）**：「**下一版本先做手机端，以及手机端和电脑端的互通**」＋ 六项拍板：**Capacitor 包成真 App** · 互通底座 **BaaS / Turso（libSQL）** · 互通范围含**生词本双向同步 ＋ 词根分组 ＋ 搜索历史/界面偏好**（**不含**学习进度与复习记录）· **正确性梯队顺延 v0.12.0** · 授权安装 **JDK ＋ Android SDK** · 离线查词用**内置高频词子集**
- **设计红线**：**Turso token 只在服务端**（Capacitor APK **可解包** ⇒ **客户端不得直连 Turso**）· **不接受任意 SQL 透传** · 服务端做**鉴权 / 限流 / 路由白名单**
- **验收标准**：**AC-20…AC-27**（**2026-09-16 T69 定案**，`REQ.md` §10.4；**AC-27 = 发布阻断项**）；DoD **D23…D34**（§10.5，共 **12** 项）；**原「8 项待裁定」＋ 第 ⑨ 项 → 九项全部定案**（§10.8-A 保留原提问 ＋ 追加「裁决」列）
- **不做（8 条）**：学习进度与复习记录同步 · **iOS 编译产物**（Windows 无法编译，需 macOS）· 全量词库内嵌（`data/db/dict.db` **518,242,304 B / 494.2 MiB**）· 客户端直连 Turso · 任意 SQL 透传 · 改 `breakdownWord`/阈值/位置规则 · v0.12.0 的正确性梯队 · **★ 首版不做 494.2 MiB 全量词典导入云端**（T69 裁决 ⑧：云端只存**用户数据 ＋ 按需缓存被查过的主词条**；全量镜像列 v0.11.x 候选）
- **★ T69 定案（2026-09-16）新增三条 DEC ＋ 两项实测真值**：
  - **`DEC-029`（架构）**：**core 不换 SQLite 驱动** —— 保留 `node:sqlite` 的 `DatabaseSync`，**`@libsql/client` 只承担「与云端同步 / 镜像」**（表述纪律：**不是「两套库」，是「一个驱动 ＋ 一个同步器」**）。依据 `scripts/probe_t67_libsql.mjs`（exit 0）：**同一文件格式、双向可读** · **同文件并发可行**（libsql 持连接时 `node:sqlite` 可写 INSERT，且 libsql **随后可见**）· 换驱动 = **59 个同步调用点 async 重写**（`db.prepare(` **44** ＋ `db.exec(` **15** ＋ 接收 `db` 形参导出函数 **25**）且**本地收益为零** · 撞 `packages/core/test/` **621 条**同步语义断言。
  - **`DEC-030`（度量，★）**：**尺子 R 依赖 `rowid`，物理不可移植** —— 整库按 `rowid` 原序重建后 **R = 829**，且「与冻结库逐词相同」= **`false`**（机制：R 含 `rowid % 97 = 0`，`packages/core/test/related.mjs:144`）。三条纪律：**引用 R 必须同时声明库的物理状态**（权威 = 冻结库 `size = 518242304 B` / `mtime = 2026-09-13T15:13:53Z`）· **子集 / 离线端不得「复现」R**（那是**诊断量不是指标**）· **唯一可移植口径 = 口径 A（101,512）**。
  - **`DEC-031`（云安全，★ 红线级）**：**同步协议必须引入用户维度 —— v0.11.0 发布阻断项**。依据：`apps/sync-server/src/index.ts:175` 的 `SELECT * FROM book ORDER BY updated_at DESC` **无用户维度**、`:83-87 authOk` 仅比对**单个可选**环境变量 `ZIDIANKAIFA_SYNC_TOKEN` ⇒ 「**单 token = 全库可见可改**」（**本地单人自用无害，公网多用户即数据泄露**）⇒ 要求 ① `book` 表须有**用户列** ② **逐路由**按用户过滤 ③ token **用户级且可撤销**；**未达成不得发布**（**AC-27** / **D34**）。
  - **实测真值 ①（推翻「俄语侧也有词频」的直觉）**：`words` **770,611** 行、`bnc`/`frq` **100% 非空**；**`words_i18n` 无任何词频列**；**`words` ∩ `words_i18n` = 0（0.0%）**、`words` 内西里尔字符 = 0 ⇒ **两张词表完全不相交** ⇒ **AC-24 的「高频」判据必须分语言**（英语用 `bnc`/`frq`；**俄语用考试标签 / 词长**）。
  - **实测真值 ②（推翻「离线查词很贵」的直觉）**：英语 20k = **4.53 MiB → gzip 2.21 MiB（237 B/词）** · 俄语 20k = **28.41 MiB → gzip 3.30 MiB（8.62×，1,490 B/词）** · **两侧合计 32.94 MiB → gzip 5.51 MiB** · 俄语全量 101,512 = **132.75 MiB → gzip 13.96 MiB** · **APK 外推**（现 debug 包 **4,201,212 B** = 4.01 MiB）⇒ ＋两侧子集 = **9.52 MiB**、＋俄语全量 = **17.97 MiB**。⚠ **两条局限照实登记**：(a) **1,490 B/词（俄）vs 237 B/词（英）不可跨语言比较**（差在**行结构**：俄语行含 `forms`，铁律 7）；(b) 探针**未含** `morphemes`(918)/`roots`(492)/`affixes`(416)/`word_etymology`(186,240) ⇒ **不得外推为「离线端可做词根拆解」**。
  - **★ 方案不变**：用户已拍板「**内置高频词子集**」⇒ **全量离线列 v0.11.x 候选**（真值只推翻「贵」的直觉，**不改本轮范围**）。
- **★ B 组风险意见的采纳情况（T69）**：**B-1 部分采纳**（不推翻已拍板方案；替代路线写入 Roadmap 候选，**触发条件 = 熔断被真实触发 ≥1 次 或 rows read > 日额度 50%**）· **B-2 采纳且写进 AC-21**（时间基准 = **服务端接收时间** ＋ 两条新断言）· **B-3/B-4/B-5 采纳**（分别落 D27 / D26 / AC-23）· **B-6 判断被确认为正确**（**T65 整体作废**；正确构成式 = **`15 = 主 agent 1 ＋ 受保护业务常驻 3 ＋ 结构席位 2 ＋ 临时 9`**）
- **状态**：🟡 **已立项，标准已冻结**（**AC-20…AC-27 ＋ D23…D34 ＋ 九项裁定全部定案**，2026-09-16 T69），**待实施**（未 bump / 未开工）
- **★ 顺延留痕（依 `DEC-014`）**：v0.10.0 结束时规划的「正确性梯队（`а-` 前缀 / D2 通用空洞上限 / `термостат` 取舍 / `roots_ru.json` 四条 `origin` 订正）」**经用户本轮拍板顺延至 v0.12.0** ⇒ `REQ.md` §7 **原文保留、不删不改**（仅由 §10 章首声明顺延）
- **★ 本 agent 的五条独立风险（供主管优先处理，详见 `REQ.md` §10.8-B / `DEC-028`）**：① **把 494 MB 词典本体放进云是「为了互通而互通」**（互通真实需求是 KB～MB 级用户数据；词典是只读静态数据 ⇒ 建议「随包分发 / 按需下载离线包」，云只承载用户数据，rows read 风险归零）· ② **同步的真正难点是时间基准**（客户端时钟偏移会让旧编辑覆盖新编辑 ⇒ 需明确基准 ＋ 断言「墓碑不复活」）· ③ **云端备份是新的空白**（现有 `VACUUM INTO`/`.bak-<ISO>`/出口后置校验全是**本地文件语义**，libSQL 远端无本地文件；free plan **PITR 仅 1 天**）· ④ **schema 迁移机制必须重建**（云端方言不能假定与 `node:sqlite` 一致）· ⑤ **隐私面被低估**（搜索历史 = 用户查过哪些词 ⇒ 建议默认本地 ＋ 显式开启云同步）
- **★ 额度事实（来源：`https://turso.tech/pricing`，抓取时点 = 本轮）**：Free $0/月 = `Databases 100 · Storage 5GB · Rows Read 500M/月 · Rows Written 10M/月 · Syncs 3GB/月 · PITR 1 day`；**任一指标触顶 ⇒ 整库 blocked**（**无「超额继续」**）⇒ **494.2 MiB 存入 5 GB 约 10 倍余量，真正的瓶颈是 rows read** ⇒ **熔断（AC-26）是必需品而非优化项**

> **当前无**其他**待发布版本** —— 最新已发布 = **v0.10.0**（结论性条目见下一节「已发布版本」）。

### ⤵ v0.10.0 开发过程留痕（立项 T35 → 发布 T53）

> 本节保留该迭代从立项到发布的**逐轮留痕**（内容未改，仅标题改写）；**发布事实 / 验收结果 / 关键数字 / 遗留**一律以「已发布版本」节的 **v0.10.0** 条目为准，两边不一致时以该节为准。
- **日期**：立项 2026-09-16（台账口径）· **发布 `2026-09-13T17:53:56Z`**（Release 实测；本地时间 2026-09-14）
- **对应需求**：`REQ-V10-001`（见 `.board/REQ.md` **§8**）
- **来源**：**用户三项架构拍板**（落盘 **`DEC-021`**）—— ① 语言分离深度 = **彻底迁移 `(word, lang)` 复合主键**；② 词根分类 = **独立顶层第 5 面板「🌱 词根分类」+ 内部英/俄子页签**；③ 生词本 = **一个「📚 生词本」入口 + 内部英/俄子页签**
- **验收标准**：**AC-12…AC-19**（`REQ.md` §8.2）；DoD **D13…D22**（§8.3）
- **优先级（冻结）**：**AC-18 > AC-15 > AC-12 > AC-13 > AC-14 > AC-16 > AC-17 > AC-19**（护栏与数据安全优先于新功能）
- **基线（进入本迭代时的实测值）**：门禁 **506 / 0**（core 484 + desktop 22）· 词素 `morphemes` **918**（英 468 / 俄 450）· `roots` **492** / `affixes` **416** · 尺子 R **241/791 = 30.47%** / A **33,495 = 32.99%** / B **308/800 = 38.5%** · `book` 表 **3 行且全为墓碑**（`pk` 仅 `word`）
- **达成态预期**：门禁 = **506 + N**（新增 `packages/core/test/book_lang.mjs` 四块断言接入 `pnpm test`；**不得**继续写死 506）；`PRAGMA table_info(book)` 中 `word`/`lang` **两列 `pk > 0`**；两套存储（SQLite ＋ `localStorage`）均按语言隔离
- **不做（已冻结 10 条）**：新增第 6 个面板 · 把词根分类并入 `/lexicon` · 处理 `lang='ru'` 含拉丁的 **8 条** · **改 `sync-server` 协议** · 从词库反推语言 · 改 `breakdownWord`/阈值/位置规则 · 改归类算法 · 动既有断言期望值 · 同形词去重（实测 **0**）· 背单词 B2 / APK B3
- **状态**：🟡 **标准已冻结，待实施**（未 bump / 未 commit）
- **★ 口径订正留痕**：AC-18 由「506 / 0」改为「既有 506 不回归 ＋ 新增 N 项」——因 AC-12/13/14 要求可机械判定，必然新增断言文件（已列 `REQ.md` §8.7 需主管确认项第 1 条）
- **T35 补强（主管裁决落定后）**：**AC-12 新增第 8 条「迁移前备份 `<db>.bak-<ISO>`」**（与事务并列的**两道独立防线**，备份失败则不迁移）· **AC-16⑦** UI 必须显式传 `lang`（因 `bookList()` 缺省不过滤）· **AC-17⑥** 分层纪律（语言过滤在 `bookGroups(lang)` 层，**不在**词素库层）· **§8.7 四项全部裁决落定**（新增测试**接入** `pnpm test`、门禁 = **506 + N**、测试**作者 = 测试 agent**、**加链 = 主管**）· **新立 `DEC-022`**（**临时脚本 `_tmp*` 不得作为证据来源**）· **C6 闭环**：`roots_ru.json` 10,492 行 diff 经主管逐条取证实为**行尾翻转噪声**（真实新增 9 / 删除 0 / 字段改动 0、首条逐字节相同），已由 `.gitattributes`（`*.json text eol=lf`）结构性修复，原「整体重新序列化」判定按 `DEC-014` 原则**保留留痕**
- **T38 行号订正 ＋ 需求审计**：通篇复核 §8 的 `文件:行号` 并重述（`types.ts:239→:245`、`schema.ts:85-96→:111-123`、`sync-server:182→:180-181`、`index.ts` 一批符号 **+110~+138**）· 新增 **§8.8 行号对照表** 与 **§8.6 红线第 8 条 ＋ `DEC-023`**（行号**指示性**、判定以符号名为准）· 新增 **§9 需求审计报告**：**AC-12 / AC-13（SQLite 层）/ AC-14 已实现**（AC-12 实现质量高于 AC 要求——含备份、行数守卫、`DROP TABLE` 后重建索引）· **AC-15 无人验证** · **AC-16 / AC-17 / AC-18 / AC-19 未开始**（四 `package.json` 与三处文本仍为 **0.9.0**）· `localStorage` 缺口实测 **4 处**（主管列 3 处，新增 `lookup` 的 `inBook` 叠加）· 新发现 2 项**可判定性/语义**问题（缺省 `lang` 的删除语义未定义、「只迁移一次」不可判定）
- **T40 状态收口 ＋ 裁决十九（主管实测复核后）**：**AC-15 由「无人验证」→ ✅ 主管实跑 `pnpm --filter @zidiankaifa/desktop test` = 22/0，且 `pnpm --filter @zidiankaifa/core test` = 484/0 ⇒ 既有 506 全部不回归**；**AC-16 / AC-17 由「未开始」→ ✅ 已实现**（`BookPanel.tsx` 语言子页签与独立计数、`RootClassPanel.tsx` 152 行 ＋ `App.tsx` 第 5 面板、契约四处贯通）；`localStorage` 四处缺口**已全修**（含 `inBook` 按语言判定）· **新立裁决十九 → `DEC-024`**：AC-13 缺省 `lang` 删除语义定案为「**核心保留可选（向后兼容）＋ UI/IPC 一律显式传 ＋ 新增红线第 9 条**」，并**登记唯一例外** `apps/web/src/App.tsx:78`（跨语言状态判定，已书面落 `scripts/check_v10_ui_contract.mjs:16-22`，**可机械复核**）· **作废一处审计结论**（`api.ts` 未引用 `isCyrillic` → 实测 **3 处命中**，D18 已实现；成因是**时点差**，原判定按 `DEC-014` 保留留痕）· **新证据（正式路径）**：`scripts/probe_sup_book_migration.mjs`（主管，**22/0**）＋ `scripts/check_v10_ui_contract.mjs`（开发 agent，**53/0**，六块 A–F）
- **⚠ 剩余（未闭项，不改变上方「已实现」判定）**：**AC-18 断言仍未落地** —— `packages/core/test/book_lang.mjs` **不存在**（作者 = 测试 agent；落地后由**主管**加链，契约脚本块 F 已对此做条件式保护）· `apps/desktop/src/main.mjs:121` `book:update` 未校验 `item.lang`（T39 在办）· **契约不一致已登记**：核心 `bookUpdate(...): BookItem`（`:1171`，`Partial<BookItem> & { word: string }`）vs `types.ts:239` `bookUpdate(item: BookItem): Promise<void>` · **AC-19 零 bump**（四个 `package.json` 与三处文本仍 `0.9.0`）
- **★ T41 WAL 一致性快照 ＋ T43 门禁闭合（本节**订正**上一条的两项）**：
  - **AC-12 新增第 9 条「一致性快照」（裁决二十 → 新立 `DEC-025`）**：`packages/core/src/db/schema.ts:29` = `PRAGMA journal_mode = WAL;` ⇒ 备份前**必须** `PRAGMA wal_checkpoint(TRUNCATE)`（`packages/core/src/db/index.ts:189`）把 WAL 并回主库，**再** `fs.copyFileSync`（`:201`，备份名 `:180 const bak`）；**`busy !== 0` ⇒ 放弃迁移**；**备份与主库逐字节相等**。**原第 8 条的复制语义按 `DEC-014` 保留留痕并标注「T41 改写」**。**受控实验**（主库 12,288 B / `-wal` 8,272 B / 写连接打开 / 表内 4 行）：只复制主库 ⇒ 副本 **3 行**（**丢最新已提交数据** ❌）· 先 checkpoint 再复制 ⇒ **4 行齐全且逐字节相等** ✔ · `VACUUM INTO` ⇒ 4 行齐全但**字节不等**（**方案 B 否决**：同构性丢失 ＋ 破坏逐字节语义）。
  - **★ 主管事故自述（留痕）**：第一版实验结论**与本裁决相反**（一度判「原实现没问题」），根因 = `copy` 前调用了 `db.close()` ⇒ 触发 clean shutdown 自动 checkpoint，**掩盖了 WAL 未并回**。⇒ 纪律：WAL 实验**必须保持写连接打开**；**前置条件不成立的结论不得作为裁决依据**。
  - **AC-18 已闭合（订正上一条「断言仍未落地」）**：`packages/core/test/book_lang.mjs` **已落地**（作者 = 测试 agent）并接入 `packages/core/package.json:25` 链尾 ⇒ **核心 484 → 582**（**N1 = 98**）、desktop **22** ⇒ **跨命令合计 604**。**判定纪律**：两条命令**分别判定**（core 582 / desktop 22），**不得跨命令合并要求 604**。**加链由主管执行**（与 §8.7 裁决第 1 条一致）。
  - **§8.8 行号对照表第三次刷新**：`packages/core/src/db/index.ts` **1215 → 1249 → 1268 行 / 51,023 B**（T41 的 19 行插在 `:182` 段内 ⇒ **其后所有符号 +19**；**已逐符号复测**：`upsertBook :1086` / `ON CONFLICT :1091` / `bookGet :1115` / `bookUpdate :1190` / `syncMerge :1216` / `groupBookByMorpheme :1256`）。**本 agent 实测订正主管 T43 表 2 处**：① `upsertBook`/`ON CONFLICT` 主管记「不变 `:1067`/`:1072`」，**实为 `:1086`/`:1091`**；② 备份段主管记 `:173`/`:201`/`:203`，**实为注释 `:182-188` / `bak :180` / `checkpoint :189` / `copyFileSync :201`**。
  - **新增 `REQ.md` §8.9**：`roots_ru.json` **四条 `origin` 文本订正登记**（`домин-` 逐字不符 ＋ 竞争分析 · `пад-` `*pasti`→`*padati` · `-ной` 改引 `боль|-н|+ой` · `однако-` 补共时/历时区分）—— **只登记不改 JSON，待主管落库**；**均不撤词素**。
  - **★ 新发现（本 agent）**：`packages/core/src/db/index.ts:701` 存在**第二处 `inBook` 内联查询且硬编码 `lang = 'en'`**（对照 `:642` 的正确写法）⇒ 若该路径可达，「俄语词是否已收藏」会按**英语条**判定；**待测试 agent 用夹具确认可达性**（若可达按正确性缺陷优先处理）。
  - **⚠ 仍未闭**：`apps/desktop/src/main.mjs:121` `book:update` 未校验 `item.lang`（T39 在办）· 契约不一致（核心 `bookUpdate(…, item: Partial<BookItem> & { word: string })` vs `types.ts:239`）· **AC-19 零 bump**（仍 `0.9.0`）
- **★★ T45 / T47 —— 裁决二十一：AC-12⑨ 备份改 `VACUUM INTO` ＋ 迁移后置校验与有界重试（落盘 `DEC-026`）**：
  - **改动（主管 T45 实施）**：备份由「先 `PRAGMA wal_checkpoint(TRUNCATE)` 再 `fs.copyFileSync`」（T41 方案 A）改为 **`VACUUM INTO`**（`packages/core/src/db/index.ts:231`）；新增 **迁移后置校验**（`:306 if (!isBookComposite(db))` ⇒ `:307 throw`：迁移流程走完后仍非复合主键 ⇒ **抛错**，消息含 `复合主键`）与 **有界重试**（残留 `book_new` ⇒ `:285` 清除后 `:288` 重试一次；`isRetry = true` 分支 `:282` **只告警、不再递归**）；抽出 `bookPkCols(db)`（`:159`）/ `isBookComposite(db)`（`:178`）供**前置判据与后置校验共用**。**旧锚点 `wal_checkpoint` 与 `copyFileSync` 已从实现中删除。**
  - **原因**：方案 A **自己引入了一条降级路径** —— checkpoint 遇并发读者/写者 ⇒ `busy !== 0` ⇒ 放弃迁移 ⇒ 库仍是 `PRIMARY KEY (word)`，而 `upsertBook` 用 `ON CONFLICT(word, lang)` ⇒ 实测 `bookAdd`/`bookRemove`/`bookUpdate` **4/4 全部抛** `ON CONFLICT clause does not match any PRIMARY KEY or UNIQUE constraint`、而 `bookList`/`bookListAll` **正常** ⇒ 生词本表现为「**能打开、列表正常、但一条也加不进去**」的**静默故障**（叠加 en/ru 同形词互相覆盖的静默语义错误）。`VACUUM INTO` 在单个读事务内完成、**无 busy 分支** ⇒ 从根上消除该触发条件。**提出方 = 需求管理 agent（T43），T45 采纳；实测后果比其推定（「写入失败」）更严重 —— 它是静默的。**
  - **备份断言变更**：由「与主库**逐字节相等**」改为「**逻辑内容等价**（行数 ＋ 逐字段 ＋ `integrity_check = ok`）」**＋ 一条反向断言**「与主库**逐字节不相等**」（= 守卫「实现未被换回 `copyFileSync`」）。实测：备份头偏移 18/19 = `[1,1]`（`journal_mode=delete`），主库 = `[2,2]`（`wal`）。
  - **门禁现状（**行为变更造成的预期回归**）**：`packages/core/test/book_lang.mjs` ⇒ **112 通过 / 9 失败（共 121 条断言）exit 1**，失败项 = **A26 · A28 · E6 · E11 · E12 · E13 · E14 · E15 · E20**（**9 条全部为预期回归，无未预期缺陷** —— 成因是断言**按 T41 实现正确编码**，而 T45 更换了实现）⇒ **正在由测试 agent 的 T46 调整**（A26/A28 口径反转；E6/E20 反转为「逐字节不相等」；E15 反转为「同场景下迁移仍成功」；E11–E14 补丁靶点改为 `VACUUM INTO` ⇒ `copyFileSync`）；**desktop 22 / 0 exit 0**（T47 由本 agent 独立复跑，结果一致）。⇒ **「core 582」暂不成立**，待 T46 改毕后由主管复测重述（**本次不擅自改写 D21 数字**，避免第二次口径漂移）。
  - **行号第四次漂移**：`packages/core/src/db/index.ts` **1268 → 1331 行 / 55,263 B**（回落规则：**函数之后 +63 / 函数体内 +30**；12 处符号实测吻合）；`REQ.md` §8.8 已第四次刷新。
  - **★ 需求管理 agent 的四条异议（**未定案**，登记于 `REQ.md` §8.2 AC-12⑨「T47 提请」）**：① `migrateBookCompositeKey` 内两条 `try` 内 `return`（`:207` 取不到主库文件路径 / `:236` **备份失败**）**绕过** `:306` 后置校验 ⇒「从根上不存在迁移被跳过」**不成立**，触发条件只是从「checkpoint busy」换成「**备份写不出**」（建议把校验**上移到 `openDatabase()`**，一处覆盖全部早期返回）；② 并发写者时 `openDatabase` 在**第一条语句** `:129 db.exec(SCHEMA_SQL)` 抛 `database is locked`（栈 `dist/db/index.js:15:8`，**不含**「复合主键」）⇒ 机械判据须分两类并限定夹具；③ 桌面端 `apps/desktop/src/main.mjs:304-305` **无 `try/catch`、无 `dialog`/`showErrorBox`**（`grep apps/desktop/src` ⇒ **0 命中**），`createWindow()` 在其后 `:317` ⇒ 抛错时**不弹窗、不建窗**，用户看到「**双击图标无反应**」（核心注释 `index.ts:303-304` 声称「弹窗告知」**与现状不符**）；④ 重试产生 **2** 个备份（桌面端 494 MB 库 ⇒ ~1 GB），建议沿用首次备份或登记该代价。

---

## 已发布版本

### v0.10.0 — 生词本语言分离 ＋ 词根分类独立模块（**2026-09-13T17:53:56Z 发布**，迭代 3）
- **发布事实**：commit **`041c6e7`**（**57 文件**）· tag **`v0.10.0`**（附注对象 ⇒ `041c6e7`）· 仓库共 **15** 个 tag · `git rev-list --left-right --count origin/main...HEAD` = **`0 0`** · 工作区**干净**
  > **时间口径（T54 澄清）**：以 **Release `published_at` = `2026-09-13T17:53:56Z`**（UTC；本地时间 = 2026-09-14）为准 —— 台账里出现的「2026-09-16 立项」是 `.board/` 内部日期，**二者不可互换**。
- **Release**：https://github.com/lzk101/zidiankaifa/releases/tag/v0.10.0 —— `draft=false` / `prerelease=false` / **附件 4 个**（全部 uploaded）
- **附件**：安装版 `zidiankaifa-0.10.0-x64.exe` **175,041,867 B** · 便携版 `zidiankaifa-portable-0.10.0-x64.exe` **174,812,071 B** · `…exe.blockmap` **182,886 B** · `latest.yml` **350 B**
- **主题 / 关键数字**：`book`（生词本）主键 **`word` → `(word, lang)` 复合主键**（**老库自动迁移** ＋ **`VACUUM INTO` 一致性快照** ＋ **迁移后置校验** ＋ **有界重试**）；**新增顶层第 5 面板「🌱 词根分类」**；生词本内 🇬🇧/🇷🇺 子页签与独立计数；**拆解算法一行未动** ⇒ 三把尺子数值与 v0.9.0 **相同**（R **241/791 = 30.47%** · A **33,495 = 32.99%** · B **308/800 = 38.5%**）
- **验收结果**：**AC-12…AC-19 八条全 ✅**（逐条证据见 `.board/REQ.md` **§8.0.1 收口表**）· 门禁 **643 通过 / 0 失败**（**core 621** ＋ desktop 22，v0.9.0 的 506 **零回归**）· `book_lang.mjs` **137**（**块 [A] 42 / [B] 26 / [C] 18 / [D] 25 / [E] 26**，两种 cwd exit 0）· `scripts/check_v10_ui_contract.mjs` **53/0**（**文本级**）· **UI 冒烟 7 项** ＋ **桌面端两条路径冒烟**（含真实老结构库 `data/db/dict.db.bak-v02pipe` 迁移：主键 `["word"]`→`["word","lang"]`、3 行逐字段相等、墓碑 3→3、`words_i18n(ru)` 159,127 不变、恰 1 份快照、exit 0）
- **版本一致性**：4 个 `package.json` ＋ 3 处文本 ＋ **web dist 内嵌**（`我的电子辞典 v0.10.0` ⇒ **B-1 闭环**）＋ **`/health` 实测** 均为 **0.10.0**；**四项 build 全 exit 0**（core / web / sync-server / desktop 打包）
- **★ 未闭 / 遗留（如实登记，不写成 ✅）**：**跨启动备份累积 `[1,2,3]`**（有意取舍 —— 备份是数据安全最后一道防线，需人工介入指引；`docs/交接文档.md` §6-C7）· **`roots_ru.json` 四条 `origin` 订正未落库**（`REQ.md` §8.9 / 交接 §6-C9）· **★ `inBook` 第二处内联查询硬编码 `lang = 'en'`**（需求 agent **T43 发现、v0.10.0 未修**；T54 实测 `packages/core/src/db/index.ts:823`，另一处正确的在同文件 `:764` ⇒ 已登记 **v0.11.0 正确性候选**；交接 §6-C10）· `book_lang.mjs` 清理分支 **`EPERM`**（交接 §6-C8）· **AC-2 仍 🟡**（`термостат` 留红 ⇒ **不得宣称「30 条全清」**）· **UI 渲染层无自动化**（人工冒烟 ＋ 文本契约 **≠** 自动化 E2E）
- **★ 口径订正（T54，本 agent）**：门禁账本由旧口径「**506** = core 484 + desktop 22」更正为 「**643** = core **621** + desktop **22**」 —— `484` 的**分项明细**一度漏记 `ru_morph.mjs` 的 **60** 条（记成 99+67+38+194+26 = 424，与 506 不自洽）；**根因** = 该文件计数行**无「结果：」前缀**，凡按前缀汇总的账本都会漏。**`506` / `484` 自此仅作 v0.9.0 历史值**（本项目**第 5 次**同类计数陷阱）。
- **开发过程留痕**：见上节「**⤵ v0.10.0 开发过程留痕（T35–T53）**」—— 含 WAL 备份缺陷的**六阶段演进**（`copyFileSync` → `wal_checkpoint` → **`VACUUM INTO`** → 后置校验上移 → 有界重试 → 备份复用）、**裁决十九/二十/二十一**（`DEC-024`/`DEC-025`/`DEC-026`）、`index.ts` **四次**行号漂移、以及需求侧提出的**四条异议**（其中「降级路径行为未定义」直接催生了裁决二十一）。

### v0.9.0 — 词素语义核证 · 防假拆解（2026-09-16，迭代 2）
- **发布事实**：commit **`889674f`**（32 文件，+8319 / −5270）· tag **`v0.9.0`**（annotated 对象 `ef56fc6` → commit `889674f`）· 仓库共 **14** 个 tag
- **Release**：https://github.com/lzk101/zidiankaifa/releases/tag/v0.9.0 —— 非草稿 / 非预发布 / **附件 4 个**（全部 uploaded）
- **附件**：安装版 `zidiankaifa-0.9.0-x64.exe` **175,032,818 B**（≈ **166.92 MiB**）· 便携版 `zidiankaifa-portable-0.9.0-x64.exe` **174,803,084 B**（≈ 166.70 MiB）· `…exe.blockmap` **182,907 B** · `latest.yml` **347 B**
- **版本**：0.8.0 → **0.9.0**（4 个 `package.json` + 3 处文本）；**三项 build 全 exit 0**（core / **web：内嵌版本号已修复 ⇒ B-1 闭环** / sync-server）
- **主题**：迭代 2 — 俄语拆解**防假拆解 ＋ 语义纠错补词素**（**V9-1** = D4a 假词根 3 红 + D4b 真词素被吞 3 红；**V9-2** 真前缀 gap 语义核证 24 红）
- **对应需求**：`REQ-V9-001`（见 `.board/REQ.md` §6；用户拍板原文「按裁决十三：**只做 V9-1/V9-2 防假拆解**」＋ T25「**规则 + 语义纠错补词素：目标 30 条全清**」）
- **验收标准**：**AC-1…AC-11**（`.board/REQ.md` §6.2；**T25 定稿**：AC-2 条件式 ＋ **AC-11 词素落库门**；口径订正见 **DEC-018**）
- **关键指标（基线 → 目标）**——**本迭代不设覆盖率目标**（45% 已由用户拍板取消，见 DEC-017）：
  | 指标 | 基线（v0.8.0） | **实测（v0.9.0，T30 收口）** |
  | --- | --- | --- |
  | `ru_morph_defects.mjs`（按块） | **23 通过 / 33 失败**（D2 0/3 · D4a 0/3 · D4b 0/3 · V9-2 12/24） | ✅ **55 通过 / 1 失败**（**`\|H-d\| = 1`** = `термостат`，属 D4b）；块级 **D1 7/0 · D2 3/0 · D3 2/0 · D4 5/1 · D5 2/0 · D6 36/0**。**表述纪律：29 绿 + 1 留红 ⇒ 不得宣称「30 条全清」**；`H-D` 条目独立留红（链外） |
  | 门禁（core 484 + desktop 22） | 506 / 0 | ✅ **506 / 0 不回归**（`daCoreMin` **不需拆分**——假族未消除，见 REQ §6.2 AC-3 填实） |
  | 全库可拆俄语词条 | 33,174 | ✅ **33,495**（≥ 33,162；净 **+321**）⚠ 含「新词素解锁新词」与「同词由不可拆变可拆」两部分 ⇒ **不得整体读作覆盖率质量改善** |
  | 尺子 R 覆盖率 | 238/791 = 30.09% | ✅ **241/791 = 30.47%**（**+3 词 / +0.38pp**）——**这才是可对外引用的覆盖率变化**；**不与 45% 比较**（DEC-017） |
  | `да-` 家族（63 词） | **真族 10**（零容差，名单见 AC-3）· **假族 26** 为消除目标 —— 记为「**范围决策 · 已知延期缺陷（T20 逐词核证假族 26 词；`да-` 首片段组共 63 词，另 27 词因库内无 `chain[].parts` 无法判定）**」，**禁用「边缘可辩护」**与「≥27」含混数字 | ⚠ **真族 10 保持 ✅**（`да-` 组 63→63 未变）／**假族 26 未消除 ⇒ 延期 v0.10.0（§7.1 A-5，需新机制）**；**「63」是守卫地板的混合计数（真族 10 ＋ 假族 26 ＋ 27 词不可判定）⇒ 不得读作「63 词全对」** |
  | 全库空洞≥3（口径A / 口径B） | 20,214 / 18,762 | ⚠ **20,243（+29 ↑）/ 18,799（+37 ↑）**；**率 60.4359%（−0.4974pp ↓）/ 56.1248%（−0.4315pp ↓）** ⇒ 按**裁决二双口径通过**、按**旧判据（绝对值不得上升）不通过** ⇒ **豁免放行**（REQ §6.2 AC-5 留痕 ＋ **DEC-019**）；**率的降幅含分母长大（33,174→33,495）的机械成分** |
  | D1 替代量（`gapMin=2` 退回变体下的形态数） | **342**（生产现值 75；差异 267） | ⚠ **336（−6）**——**门槛「≤ 342」已作废，改「只报不判」**（该量可双向移动，不具单向判据性）；记为「**已测量、未验证**」（V9-8 断言未落地） |
  | 词素库 | `roots_ru.json` **441** · `morphemes` **909** | ✅ **450**（root 171 / prefix 185 / suffix 94）· **918**（ru 450 + en 468）；**落库 9 条** = 真词素 `суч-` `суд-` `домин-` `-ной` `пад-` `тряс-` ＋ **整词兜底** `столп-` `казус-` `однако-`（按 **AC-11(f)** 单列并披露）；**明确不落**：`стат-` / `термостат-` / `дав-`·`дар-`·`дал-` / `одн-` |
- **本迭代允许/不做**：**允许**补词素 —— 但**每条必须过 AC-11 落库门**（**依据 `DEC-004-R1`**：来源可复核、主管逐条复核 diff、用户事后否决权、整词兜底条目单列披露）；**不做**：动任何阈值 / 放松位置规则 / 覆盖率目标 / D2(V9-5) / V9-6(C3) / V9-4(goals) / V9-9(未过审定门) / 不把 `ru_morph_semantic.mjs`(20/21) 接入门禁
- **交付形态预期**：core / sync-server build 可完成；**`apps/web` build 在受限沙箱不可完成**（esbuild 需管道 stdio），若届时仍受限则按 v0.8.0 先例**仅源码发布并在 Release Notes 披露 UI 版本号滞后**
- **状态**：✅ **已发布**（2026-09-16；commit `889674f` / tag `v0.9.0` / Release 4 附件）。路线甲「只改数据不改代码」，`packages/core/src/**` 一行未改；AC 已按实测收口（T30 → DEC-019，后经 T33 依发布事实复核）。
- **发布后复核（T33，需求管理 agent 独立核对发布事实）**：① tag/commit/附件数与 GitHub API 一致；② 三项 build 全 exit 0（web 内嵌版本号已修，**B-1 闭环**）；③ **`loses = 2` 具名落实**（`поднаковальня`/`поднакопить`，旧态含假词根 `дн-`）；④ 尺子 B **本次新测 38.5%（308/800）**（v0.8.0 为 38.0% = 304/800，非「维持」）；⑤ 见 `docs/需求总结.md` §22。
- **本轮三处改判（DEC-019，均已留痕）**：① **AC-5** 由「绝对值不得上升」改为「**绝对值 ＋ 率**」双口径 ⇒ 本迭代 **豁免放行**（绝对值 +29/+37，率 −0.4974pp/−0.4315pp），**须在 Release Notes 披露**；② **AC-6** 由门槛改为**只报不判**（342 → 336 仅作事实记录）；③ **AC-9①** 块级计数**延期 v0.10.0（B-3）**，以本 agent 的**块级解析法**作为等价放行替代（**修订而非「前置已满足」**）
- **诚实交付披露项（本次发布必须写明）**：① **`термостат` 1 条留红**（`|H-d| = 1`，30 条目标未全清）；② **`да-` 假族 26 词未消除**（延期 v0.10.0）；③ **空洞≥3 绝对值上升**（豁免放行）；④ **UI 版本号滞后**（见下）

---

### v0.8.0 — 修复词首 gap=1 假词根误拆（D1）
- **主题**：用户可见症状 **`плескание`（溅水）的词族卡挂到 `лес-`（森林）40 词**；同类 `хлестаться`→`лес-`、`анестезия`→`нес-`、`ателье`→`тел-`，全库 **342 词**呈此形态（**267 误拆 + 75 真前缀**）
- **根因**：`packages/core/src/db/index.ts` 两道守卫恰好都放过 `gap===1`（`parts.length === 1 && parts[0].start === 1` 要求单片段；`parts[0].start >= 2` 要求 ≥2）；**覆盖率阈值完全无效**（`плескание` 覆盖率 0.89 ≫ `BREAKDOWN_MIN_COVERAGE = 0.55`）
- **修复**：`>= 2` → **`>= 1`**（不选「无条件禁止 gap=1」是因为会误杀 `удачный = у- + да- + -ный`）
- **关键数字**：假前缀组 **267 → 0**（四项独立检验确认，非部分修复）；真前缀组 **75 → 75（0 误杀）**；尺子 R **30.85% → 30.09%**（238/791，**−0.76pp**，退的 6 词全是被消除的假拆解 = **预期代价**）；尺子 A **33,441 → 33,174**（−267）；L4 空洞≥3 **134 持平**；测试 **226 → 480**（新增 `ru_morph.mjs` 60 + `ru_morph_d1fix.mjs` 194）
- **同时闭环**：**C1**（补跑 `pnpm --filter @zidiankaifa/sync-server build`，`/health` 返回 `0.8.0`）、**DEC-009**（`measure_ru_breakdown.mjs` 的 import 路径修复）
- **发布形态**：tag `v0.8.0` ⇒ commit `4c89272`；**仅源码、0 附件**——`apps/web/dist` 未重建（受限沙箱禁管道 stdio），**UI 关于页仍显示 v0.7.1**，已在 Release Notes 披露
- **遗留（已披露）**：75 词「真前缀 gap」形态中 `уч-`×11 + `дн-`×1 为**真假词根污染**，升为 v0.9.0 P0（即本文件上方待发布的 V9-2）
- **详情**：`docs/release-v0.8.0.md`、`docs/需求总结.md` §21
- **发布结果**：✅ 已发（**0 附件**）

### v0.7.1 — 修复升级不换库
- **主题**：桌面端 `resolveDbPath()` 原先只在 userData 副本不存在时复制内置库，导致装过旧版后**永久读旧库**（用户实测 v0.7.0 仍在读 v0.2.x 的 509MB 旧库，俄语词源/词素均为 0）
- **修复**：`apps/desktop/src/dbmigrate.mjs` 的 `ensureUserDb()` —— 内置库 `size:mtime` 指纹与 `dict.db.stamp` 比对，不一致即换库 + 迁移 `book` 表（含墓碑）+ 旧库改名 `.bak-<ts>`
- **关键数字**：新增 `dbmigrate` 测试 **22 项**（5 场景）；总测试达 **226**
- **详情**：`docs/release-v0.7.1.md`、`docs/需求总结.md` §20
- **发布结果**：✅ 已发（安装包 166.92MB）　*来源：回填*

### v0.7.0 — 词源关联网络 · 同根词族
- **主题**：词源相关词可点；同根词族；词素库 909 条
- **关键数字**：词素 **909**（英 468 / 俄 441）；新增 `related.mjs` **38 项**测试
- **发布结果**：✅ 已发　*来源：回填*

### v0.6.0 — 词根表 / 词缀表
- **主题**：拆解算法改造为全局最优分段 DP
- **关键数字**：`roots` **476** + `affixes` **403**；`lexicon` 测试 **67 项**
- **发布结果**：✅ 已发　*来源：回填*

### v0.5.0 — 数据质量总收
- **主题**：词源来源修正；覆盖率口径修正
- **关键数字**：词源来源修正 **19,865** 条（实测 `word_etymology_origin_backup` = 20,060 行，见 `.board/REQ.md` §3.2 D-2）；VACUUM **515 → 491MB**；覆盖率口径修正为 **30.8%**
- **发布结果**：✅ 已发　*来源：回填*

### v0.4.4 — 屈折形污染清理
- **关键数字**：清除 **58,195** 条（实测 `words_i18n_inflection_backup` = 57,615 行，差 580，见 D-2）；俄语词条 **159,127 → 101,512**
- **发布结果**：✅ 已发　*来源：回填*

### v0.4.3 — 俄语词素数据驱动扩充
- **关键数字**：词素 **888**（俄 **432**）
- **发布结果**：✅ 已发　*来源：回填*

### v0.4.2 — 更新行为核验与加固
- **主题**：防自动下载（`updater.mjs:66 autoDownload = false`）
- **发布结果**：✅ 已发　*来源：回填*

### v0.4.1 — 查词正确性修复
- **主题**：专名大小写漏失、词形反查劫持
- **关键数字**：词素 **601**
- **发布结果**：✅ 已发　*来源：回填*

### v0.4.0 — 自动更新系统
- **主题**：blockmap 差分更新
- **发布结果**：✅ 已发　*来源：回填*

### v0.3.0 — 俄语词源 · 词根词缀
- **关键数字**：俄语词源 **0 → 30,767**（后经 v0.5.0 口径修正为 31,256）；俄语词素 **145** 条
- **发布结果**：✅ 已发　*来源：回填*

### v0.2.1 — 数据管线补全
- **关键数字**：俄语 forms **48.8% → 62.8%**；中文词源 **9,378** 行（口径＝`source LIKE '%zh%'`，见 DEC-011）
- **发布结果**：✅ 已发　*来源：回填*

### v0.2.0 — 首个发布版
- **内容**：安装包 + 便携版
- **关键数字**：exe **148MB**
- **发布结果**：✅ 已发　*来源：回填*

---

## 流水统计

| 项 | 值 |
| --- | --- |
| 已发布版本数 | **15**（v0.2.0 → **v0.10.0**） |
| 最新版安装包 | **`zidiankaifa-0.10.0-x64.exe` = 175,041,867 B**（v0.10.0 共 **4 附件**；v0.8.0 为仅源码发布、0 附件） |
| 当前 HEAD | **`041c6e7`**（tag `v0.10.0` ⇒ `041c6e7`；上一版 v0.9.0 tag ⇒ `889674f`）；与 `origin/main` 差异 **`0 0`**、工作区干净 |
| 当前测试基线 | **643**（core **621** + desktop 22），v0.10.0 实测 **643 / 0**；另 3 个手动台账：`defects` **55/1**、`semantic` **44/1**（回归项 0）、`goals` **0/2**。**旧口径 `506`（core 484）已降为 v0.9.0 历史值**（其分项一度漏记 `ru_morph.mjs` 的 60 条，见下「口径订正」） |
| 在办迭代 | **无**——迭代 3 / **REQ-V10-001** 已随 **v0.10.0** 交付并发布（AC-12…AC-19 **八条全 ✅**，逐条证据见 `REQ.md` §8.0.1） |
| 已取消的目标 | **尺子 R 覆盖率 > 45%**（用户拍板取消，`DEC-017`；v0.9.0 交付 30.47%、**v0.10.0 未改算法故同值**） |
| 下一迭代 | **v0.11.0** —— **已立项（2026-09-16）**：**`REQ-V11-001`**（`REQ.md` §10）· 决策 **`DEC-028`（方向）/ `DEC-029`（驱动不换）/ `DEC-030`（R 不可移植）/ `DEC-031`（用户维度阻断项）** ⇒ 主题 = **手机端（Capacitor 真 App）＋ 端云互通（Turso）**，验收标准 **AC-20…AC-27**（**AC-27 = 发布阻断项**）＋ DoD **D23…D34** ＋ **九项裁定全部定案**。**★ 原规划的正确性梯队（`а-` 前缀 / D2 通用空洞上限 / `термостат` 取舍 / `origin` 订正）经用户拍板顺延 v0.12.0**（`REQ.md` §7 原文保留）。**词素落库类**迭代仍须过 **`DEC-020`** 准入（其中 ② 经 `DEC-020-R1` 修订为「空洞三条式」） |

**发布卫生欠账**：
- ~~C1 —— bump 0.7.1 时未跑 `sync-server build`，`/health` 仍返回 `version: 0.7.0`~~ → ✅ **已闭环（v0.8.0）**：补跑 build 后 `/health` 返回 `0.8.0`。今后每次发布**必须**跑三个 build（core / web / sync-server）。
- ~~🟡 **v0.8.0 遗留**：`apps/web/dist` 未重建（受限沙箱禁管道 stdio ⇒ esbuild 不可用），**UI 关于页仍显示 v0.7.1**~~ → ✅ **已闭环（v0.9.0）**：文件策略放宽为 `danger-full-access` 后 **web build 与 desktop build 均一次通过**，`apps/web/dist` 内嵌版本号已更新为 **v0.9.0**。`AGENTS.md` §3 已修正旧结论——**该阻塞是文件策略所致，不是命令本身的缺陷**（`workspace-write` 下仍受阻塞）。
- ~~⚠ **流程观察（T33 新记）**：`packages/data-pipeline/roots_ru.json` 在 v0.9.0 落库时 diff 达 **10,492 行**（远超 9 条新增），系**整体重新序列化**。**AC-11(d)「主管逐条复核 diff」的成本因此显著升高** ⇒ 后续落库须**保持文件原有格式与顺序**，把 diff 收敛为「新增 N 条」。~~ → ✅ **已闭环（2026-09-16 主管逐条取证）**：按 `(kind, morpheme)` 逐条比对两个 git revision ⇒ **真实新增 9 / 删除 0 / 字段改动 0，首个条目逐字节相同** ⇒ 那 10,492 行**全部是行尾翻转噪声**（CRLF/LF 混用）；**原判定「整体重新序列化」已被推翻**（原判定系**从 diff 行数推断**、未逐条比对）；根因 = 仓库此前**没有 `.gitattributes`**，已新建（`*.json text eol=lf`）⇒ **结构性修复**。**原判定按 `DEC-014` 保留留痕**（上文不删）。
- ⚠ **口径订正（T54 新记）**：门禁账本由「**506** = core 484 + desktop 22」更正为 「**643** = core **621** + desktop **22**」—— `484` 的**分项**一度漏记 `ru_morph.mjs` 的 **60** 条（记成 99+67+38+194+26 = **424**，与 506 **不自洽**）；**根因** = 该文件计数行**无「结果：」前缀**，凡按该前缀汇总的账本都会漏。**`506` / `484` 自此仅作 v0.9.0 历史值**。已同步 `AGENTS.md` §1 · `docs/交接文档.md` §5.3 · `docs/需求总结.md` §23.6 · `.board/REQ.md` §8.0.1。**这是本项目第 5 次同类计数陷阱**。
- ⚠ **仍在的卫生欠账（v0.10.0 之后）**：`scripts/_tmp/` 仍有历史探针待清理（`DEC-022`：**临时脚本不得作为证据来源**）· `.board/EVIDENCE.md` 属测试 agent 独占域 · `packages/core/test/book_lang.mjs` 的清理分支报 `EPERM`（交接 §6-C8）。
