# agent 登记册 — zidiankaifa

> **写者**：项目结构管理 agent（第 4 个常驻角色，**第 2 任**）。**其他角色只读。** ｜ 职责依据：`.board/roles/structure-agent.md` **§14**
> 数据源：**主管每轮提供的 `list_agents` 快照**（自己调 `scope=descendants` 只能看到自己的子代理 ⇒ **看不到 ≠ 不存在**）｜ 首建 2026-09-16（v0.10.0 发布后）｜ **本版 = T62-B 压缩重写 ＋ T62-C 裁决落地**（250 → **120** 行，零信息丢失，自检见 §8）
## 0. ⚠ 三条必读（否则会误用本册）
1. **本工具层没有「删除 agent」的 API。** 可用通道只有三条：`list_agents`（只读）· `interrupt_agent`（停**当前一轮**）· `send_message`（**= 发起新一轮 = 唤醒**）。⇒ **「清理临时 agent」的实际含义 = 台账化 ＋ 标记可回收 ＋ 报主管不复用，不是物理删除**。本册**禁止**把「已标记」写成「**已删除**」或「已回收」。
2. **`ready` ≠ 会话结束、上下文已释放**（它只表示「可被 `send_message` 续跑」）⇒ 把它当「已死」随手重派 = **重复消耗上下文 ＋ 高概率产出与既有结论冲突的第二版**（本册要防的主要浪费）。且 `ready` 是**唯一状态值**，**不区分**「已完成并落盘」「失败被放弃」「从未真正开始」⇒ **不得由 `ready` 推出任何完成度结论；完成度只能由落盘证据判定，无证据即未落盘**。
3. **label 是 agent 创建时的任务描述，不等于当前职责**（三个业务常驻的 label 已过时，见 §2）。**常驻 agent 的 label 为创建时历史、只读，职责以本册 §1/§2 为准**（裁决 A，坐标 `TASKS.md` §11.9）⇒ **今后不得再提议为改名而重建常驻 agent**。
## 1. 席位总表（**15 行 = 14 个 UUID ＋ 主 agent（无 id）**；`list_agents scope=descendants` 实测 **15** 行 = `children` **12** ＋ depth2 **3**；构成式与口径见 §4）
### 1.1 受保护 4 = 主 agent ＋ 业务常驻 3（**不可清理 · 不可 `interrupt`** · 用户的清理豁免白名单）
| # | 角色 | 登记 id | 独占写域 | 委任书 |
| --- | --- | --- | --- | --- |
| 0 | **主 agent / 项目主管** | 本会话顶层（各常驻 agent 的 `parent`，**无 id**） | `.board/TASKS.md` · `BASELINE.md` · `BOARD.md`；**git 写操作唯一执行者** | `PROJECT_CHARTER.md` |
| 1 | **需求管理 agent** | `4ce66e63-8f5f-4b15-8203-c23b3dccce6f` | `.board/REQ.md` · `DECISIONS.md` · `CHANGELOG.md` | `.board/roles/`（需求侧） |
| 2 | **开发编程 agent** | `6c6cedb8-f03d-49d3-ab1a-c1aa2d9dc62c` | `packages/*/src/**` · `apps/*/src/**` · `packages/data-pipeline/**` | `.board/roles/dev-agent.md` |
| 3 | **功能测试 agent** | `7ff57407-f229-40e9-ba31-3d3e58b9655b` | `packages/core/test/*.mjs` · `apps/desktop/test/*.mjs` · `scripts/**` · `.board/EVIDENCE.md` | `.board/roles/test-agent.md` |
> **「受保护 4」不含结构 agent** —— 它是**执行者**，既不被清理、也不是被豁免对象。口径三分（裁决 B，坐标 `TASKS.md` §11.9）：**受保护 4**（主＋业务 3）· **业务常驻 3** · **结构席位** · **常驻合计 = 受保护 4 ＋ 结构席位**。⚠ 若两个集合都写成「4」，按「常驻 4」白名单清理会**漏掉结构 agent 自身**，按「受保护 4」校验额度会**算错常驻总数**。**体检附项口径 = 「常驻合计 = 6（受保护 4 ＋ 结构席位 2）」**（**主管 T87 直接裁定，勿再提请复核**）—— ⚠ **「结构 1」这个数在任何口径下都不存在**：原写的「常驻合计恒为 5（受保护 4 ＋ 结构 1）」是 **T55 快照值，已随接班失效**（T55 时结构席位确实只有 1）⇒ 原「同一分歧的下游面」表述**作废**。
### 1.2 结构席位 = 项目结构管理 agent（执行者，**永不计入「临时」**）
| 代 | id | 状态 |
| --- | --- | --- |
| 前任（**已出列**） | `2d4ba4d0-64b5-4449-a295-66c327942977` | 会话曾为 **`[diagnostic: corrupt]`**；**主管 T82 复测已变为 `[ready]`** ⇒ **撤回「永久不可恢复」**（该状态**不是终态**、可自行恢复）**但仍优先不唤醒**（纪律不变）。T62 派单**永久未回执**（此项不受状态变化影响）；写域已由接任者接管。**依 `DEC-014` 留痕不删，仍占席位行** |
| **接任者（本册写者，在岗）** | **`bb9e0148-3e78-4d58-b5ce-700fce51fd48`**（**已由主管补登** —— T62-C 快照实测；= 我唯一可得的自身标识 `DSH_SESSION_ID`，`DSH_WEB_URL` = `http://127.0.0.1:55017`） | 在岗（**本册无「待补登」项**；id 由主管指定，见 §9.3） |
> **★ 终局（主管 T62-C ＋ T87 复核，勿再改）：结构席位恒为 2 · 总数 = 15**（`list_agents scope=descendants` **实测 15 行** = `children` **12** ＋ depth2 **3**）· **出列者仍占席位行**（历史事实 ＋ 「不能靠 corrupt 者交班」这条教训的载体；依 `DEC-014` 留痕不删；T87 复测其状态已回到 `[ready]` ⇒ **须注明「状态可自行恢复」**）。**「14」是 T62-C 时点的观测值**（依计数纪律**留痕保留、不删**）。⚠ **归因（T87）**：**14 → 15 的增加是真实的**，**来源 = T67 期间新增 1 个 depth2 临时子代理 `36ef2379`**（父 = 开发 agent）—— **不是接班**：接班只是席位 1→2 的**内部替换**（前任出列仍占行 ＋ 接任者入列 ⇒ **净变化 0**），**两件事不得混成一件**。原表述「14→15 这个数不存在」**已作废**。
## 2. 常驻 agent 的 label 与职责映射（**label 只读**，裁决 A）
| id | snapshot label（创建时任务名） | **真实职责** |
| --- | --- | --- |
| `4ce66e63` | 需求管理 agent 启动 | 需求管理 |
| `6c6cedb8` | **开发 agent 启动+可达性判别** | 开发编程（label 停在一个早已完成的判别任务上） |
| `7ff57407` | **测试 agent 启动+判别集** | 功能测试（同上） |
| `2d4ba4d0` | 结构管理 agent 常驻角色 | 项目结构管理（**label 与职责一致 ⇒ 不计入「label 过时」**） |
⇒ **每条 label 都只记录了首轮任务**，从快照看不出这三个 id 就是当前三大常驻角色 ⇒ **本册承担职责映射这一层（真实来源）**；label 修正属主管决定。**★ label 过时计数 = 3（三个业务常驻），不是 4**（旧册标题列了 4 行，与事实不符，T55 已订正）。**不重建的三条独立理由（裁决 A 全部采纳）**：① 重建会丢弃**唯一不可再生的资产 = 上下文**（本项目多次依赖历史 agent 复述当时取舍，最典型 `packages/core/test/book_lang.mjs` 块 E 的 T42→T46→T50→T52 判据演化链 —— 这些「为什么当时这么定」**不在任何落盘文档里**）；② 重建会**主动制造本册要防的风险**（重派产出与既有结论冲突的第二版，为改名而重建 = 明知故犯）；③ **label 是创建时刻的历史事实、不是可变字段**，改它 = 改写留痕（与 `DEC-014` 同源）。**反方已考虑并否决**：唯一真实收益「新会话一眼看清谁是谁」已被 §1/§2 覆盖，成本（上下文不可逆丢失）**远大于**收益 ⇒ **净收益为负**。
## 3. 临时 agent 登记（数据源 = **主管 T87 同一时刻双测快照**：`children` **12** ＋ `descendants` **15**；**全部 `ready`，无一 `running`**）
### 3.1 depth = 1（直属主 agent）—— **7 行，全部「可回收」**（产物逐条经 T55 实测核验）
| id | label（创建时任务） | 产出落盘（**核验版**；⚠ = 原登记错标已订正） | 回收判定 |
| --- | --- | --- | --- |
| `fa74d552-876e-4f94-b7c5-cf5af79bb297` | v0.9.0 准入条件实验 | `scripts/exp_v9_admission.mjs` ✓ tracked · `scripts/out_v9_admission.json` ✓ tracked —— ⚠ **原登记「611 KB」错，实测 639,777 B（624.8 KiB）** · `.board/EVIDENCE.md` **§41 ✓**（起 `:1440`，`§41.1 交付物`） | **可回收**（结论已入 `.board/BOARD.md` 裁决十三） |
| `bb19ba01-5ddb-46f4-a093-f21b2b7bc7f8` | V9-3 全库 D1 计数守卫 | `packages/core/test/ru_morph_d1guard.mjs` ✓ tracked ＋ **门禁实测 26/0** · `scripts/probe_t18_d1guard_baseline.mjs` ＋ `scripts/probe_t18_guard_power.mjs` ✓ tracked（**`.board/EVIDENCE.md:1253-1254` 逐名指认**）· `.board/EVIDENCE.md` **§40 ✓**（`:1248-1439`，`§40.4 ★ 双向断言清单（26 条）`） | **可回收**（已提交 `99c9992`）—— **本表核验最强一条** |
| `843b7bf5-6f12-44f4-b51a-9b87c4037bdd` | T42 WAL backup red assertion | `scripts/probe_t42_wal_fixture.mjs` ＋ `…2.mjs` ＋ `…3.mjs` ✓ **三者全 tracked**（原文以 brace 写法 `probe_t42_wal_fixture{,2,3}.mjs` 引用）· `book_lang.mjs` **块 E ✓**（E1–E21b） | **可回收**（后继 T46/T50/T52 已接管该文件）—— ⚠ **附注**：其断言结论**已被 T46→T50→T52 三轮依次改写**，但三个夹具脚本仍被 `book_lang.mjs` §E「证据留痕」段逐名引用 ⇒ 属「**结论被取代、产物仍被引用**」，**不判「失效」**；追溯判据演化应**读代码与 EVIDENCE，不得唤醒** |
| `6e6acfbe-4056-4d89-87ed-2857d3b50a5e` | T46 update block E assertions | `scripts/probe_t46_variant.mjs` · `scripts/probe_t46_backup_fail.mjs` · `scripts/probe_t46_postcheck.mjs` ✓ **三者全 tracked**（原写 glob `probe_t46_*.mjs ×3`，展开即此三者，**数目相符**）· `book_lang.mjs` 块 E 扩写 | **可回收**（约束已由 T50/T52 覆盖） |
| `18a79f6a-d575-48eb-932e-23bb90cc77e4` | T47 sync AC-12 to VACUUM INTO | `.board/REQ.md` AC-12⑨ —— ⚠ **原登记「`:816-847`」指向错**（`:816` 是小节标题 `### 8.2 验收标准 AC-12…AC-19`，非本体）⇒ **订正为 `.board/REQ.md:1291`**（`#### F. T45 修订记录：AC-12⑨ 由「方案 A」改为 VACUUM INTO ＋ 后置校验（裁决二十一；本 agent T47 落盘）`）＋ **`:1314`**（四条异议「★ T47 提请」）· `.board/DECISIONS.md` **`DEC-026` ✓**（`:559`，标题逐字含「T45 主管实施 / T47」）· `.board/CHANGELOG.md` ✓ | **可回收**（四条异议已全被采纳闭环；**行号须按上述订正**） |
| `3d2dc17e-ac96-4d93-83a0-48bb24fa3ad3` | T50 update A33 backup-count assertion | `book_lang.mjs` `A33`/`A12b`/`A33b` ✓ **命中 19 / 6 / 4 处** · `scripts/probe_t50_reuse_edges.mjs` ✓ tracked | **可回收**（S1/S2 已由主管 T51 修掉） |
| `6b08a4c5-d52d-46ae-8812-910498a18076` | T52 sync reuse log assertion | `book_lang.mjs` `REUSE_LOG_RE` ✓ **4 处** ＋ `A28c`/`A28d`/`A28e` ✓ **7 / 6 / 7 处** · 已随 **v0.10.0** 发布 `041c6e7` | **可回收**（已随 v0.10.0 发布 `041c6e7`） |
> **核验结论 ＋ 同文件归属注明（主管裁决 8 要求）**：**7 个可回收者的「产出落盘」全部成立**（无一靠编造归属），其中 **2 处为错标须订正** —— ① `fa74d552` 产物字节数（611 KB → **639,777 B**）；② `18a79f6a` 的 `REQ.md` 行号（`:816-847` → **`:1291` ＋ `:1314`**）；另 **1 处由推断升级为确证**（`bb19ba01` → `EVIDENCE.md:1253-1254` 逐名表格）。★ 本表 7 行中 **5 行 = 同一个测试 agent 的 5 次串行派单，且全部改同一文件 `packages/core/test/book_lang.mjs`** —— `843b7bf5`（T42 WAL backup 红色断言）→ `6e6acfbe`（T46 块 E 扩写）→ `18a79f6a`（T47 AC-12 同步至 VACUUM INTO）→ `3d2dc17e`（T50 A33 备份计数断言）→ `6b08a4c5`（T52 reuse log 断言）；按 §5 判据**不算异常**（串行链合理），**但必须注明**，否则日后追溯「`book_lang.mjs` 由谁编写」只会看到 5 个互不相识的 id ⇒ **追溯入口 = 本节，不是唤醒这 5 个 agent**。
### 3.2 depth = 2 —— **3 行：2「⛔ 异常 · 未落盘」＋ 1「⏳ 待判」**（父 = `fa74d552` 或**各常驻**）
| id | label | 父 | 判定 |
| --- | --- | --- | --- |
| `9db3c4d0-f68d-4f1d-b579-8e1cfed96cdf` | Audit etymology batch A | `fa74d552` | ⛔ **异常 · 未落盘** —— 证据未固化，**绝不标可回收**，**不再唤醒** |
| `0f53feaa-6945-4c5f-bdda-b3ab87621256` | Audit etymology batch B | `fa74d552` | ⛔ **异常 · 未落盘**（同上） |
| `36ef2379-d284-493b-b60f-48978e38c8c3` | **Capacitor feasibility probe** | **`6c6cedb8`（开发 agent）** | ⏳ **待判（未结案）** —— 由**开发 agent 在 T67 期间自行 spawn**、**未预先登记**（本册 T87 补登，见 §7.2）；**产出待认领** ⇒ **既不并入「可回收」也不并入「未落盘」**（不得由 `ready` 推定完成、亦不得推定为失败）；**不唤醒** |
> ⚠ **`36ef2379` 是 14 → 15 的唯一来源**（T87 认定）：它不在 T62-C 快照内 ⇒ **depth2 由 2 增至 3**。**它是「执行者自行 spawn 的临时子代理」⇒ 集合上归入「临时 depth2」正确**（与其父是否为常驻无关）。
- **前任四通道查证（全部 0 命中，可复现）**：① `scripts/exp_v9_admission.mjs` 全文按 `batch|etym|Batch` → **0**；② `scripts/out_v9_admission.json`（**549,597 字符**）按 `batch`/`etymology`/`etym` → **三者皆 false**；③ `.board/EVIDENCE.md` §41（起 `:1440`）按 `batch|Batch|子代理|孙代理|subagent|etym` → **0**；④ `git log --oneline --all` 按 `batch|etym|Batch` → **0 提交**。
- **终局（结案）**：主管侧**第五通道 = `.board/EVIDENCE.md:1541`** `### 41.5 逐条候选审定表（按边际增益降序；每条均附可复核联网词源来源）`（`:1548-1558` = 11 行带 Wiktionary 链接）⇒ **开发 agent 在 T17 内部已完成逐条词源审计** ⇒ 两孙代理工作**与 §41.5 完全重复、本就无独立产出可落盘**（这正好解释了四通道为何全 0 命中）⇒ 记为「**已沉没的子任务 · 证据未固化 · 与 `EVIDENCE.md` §41.5 重复**」（坐标 `TASKS.md` §11.10 裁决 E）⇒ **不再追责、不重跑、不唤醒**。
- **★ 前任自查的方法缺陷（必留痕）**：四条通道**全部是关键词检索**，而 §41.5 标题用「逐条候选审定表」= **语义等价、词汇不重叠** ⇒ 关键词抓不到。**教训：「关键词 0 命中」只能支持「没有以该名称落盘」，不能支持「没有落盘」；必须补语义通道（读父产物的目录/小节标题）。**
- **副产物（裁决 E 认定成立且应保留）＋ 口径对齐**：新增异常类「**归属存疑**」（判据 = 子代理 label 主题与其父任务主题不一致，本处 = 「词源审计」vs「v0.9.0 准入实验」⇒ **不据父任务推断子任务产出**；它**正好阻止了一次错误的「并入父产物」结案**）；主管用「**不判定**」、本册用「**异常 · 未落盘**」= **同一集合的两个名字**（本册表述更严格，依 §5「未落盘 ⇒ 优先报警，绝不标可回收」）。
## 4. 统计与口径终局
> **★★ 终局口径（主管 T87 复核，勿再改）：`agent 总数 = 15` = 主 agent 1 ＋ 受保护业务常驻 3 ＋ 结构席位 2 ＋ 临时 9**（临时 9 = depth1 **7** ＋ depth2 **2**）—— **裁决式照录**。**实测依据（T87 同一时刻双测，2026-09-16，HEAD `e20f512`）**：**`children` = 12 行 = 业务常驻 3 ＋ 结构席位 2 ＋ 临时 depth1 7** ／ **`descendants` = 15 行 = 上述 12 ＋ depth2 3** ⇒ **主 agent 不计入行**（T87 调 `children` 得 12 行，**不含他自己**）。**结构席位恒为 2** = 出列 `2d4ba4d0` ＋ 在岗 `bb9e0148`（**出列者仍占席位行**）；「**13**」= **业务口径** ⇒ **不得单独出现**，须写「**业务侧 13 ＝ 受保护 4 ＋ 临时 9**」。**落盘坐标**：`.board/BOARD.md` 新节「[主管] v0.11.0 手机端 ↔ 电脑端 互通实测闭环…」**§八**。
> **变迁留痕 ＋ 归因纪律（旧值一律留痕不删）**：建册「临时 10」= **集合边界错**（结构 agent 自身被计入临时列表）；一轮「临时 8 / 总数 13」= **多减 1**（算术错，被「可回收 7 ＋ 未落盘 2 = 9」交叉校验拦下；同轮「不判定 3」亦算错、已作废）；**T55 = `14 = 受保护 4 ＋ 结构 1 ＋ 临时 9`**；**T62-C 时点 = 14 行**，其裁决式 `14 = 受保护 4 ＋ 结构 2 ＋ 临时 8` —— ⚠ **末项 8 既非任何时点的实测值、又与紧邻明细 `7 ＋ 2 = 9` 冲突 ⇒ 该式作废（原式留痕）**；**现（T87）= 15 行**。⚠ **归因**：**14 → 15 的增加真实存在，来源 = T67 期间新增 1 个 depth2 临时子代理 `36ef2379`**（父 = 开发 agent）—— **不是接班**（接班 = 结构席位 1→2 的**内部替换**，前任出列仍占行 ＋ 接任者入列 ⇒ **净变化 0**）。✅ **本册 T62-C 的数值申报（15）已由 T87 复核证实** ⇒ 「统计 ↔ 明细两次交叉校验」纪律**第 4 次生效**。全链坐标：`.board/TASKS.md` §11.9（裁决 A/B）· §11.10（D/E/G）· §11.11 · §11.12（裁决 5）。
> **⚠ 本册对裁决式末项的一处「释义」（不是新申报 ⇒ 本册待裁项 = 0）**：裁决式的 **9 = 已判定的 9**（7「可回收」＋ 2「异常 · 未落盘」，与 §3.1 七行 ＋ §3.2 前两行**逐项吻合**）；而 T87 认定为 14→15 来源的新成员 **`36ef2379` = 第 10 个临时 agent**，T87 同时裁定它**「归『待判』，既不并入可回收也不并入未落盘」** ⇒ **两个数都对、只是集合不同**：**`descendants` 原始双测的直接加法 = 行 15 = 业务 3 ＋ 结构 2 ＋ 临时 10（depth1 7 ＋ depth2 3）** ／ **裁决式按「已判定」口径写 9**。⇒ **今后引用「临时」必须带口径**：**已判定 9（7 ＋ 2）· 合计 10（＋ 待判 1）** —— 本项目同类计数陷阱至此**第 12 次**（T87 自认根因 = **快照时点混用**，责任在主管）。

| 项 | 值（数据源 = **主管 T87 同一时刻双测快照**，2026-09-16，HEAD `e20f512`；`children` **12** 行 · `descendants` **15** 行） |
| --- | --- |
| **本项目 agent 总数** | **15**（**行口径** = `list_agents` 实测条数）。**裁决式：`15 = 主 agent 1 ＋ 业务常驻 3 ＋ 结构席位 2 ＋ 临时 9`**；**实测直接加法：`15 = 业务 3 ＋ 结构 2 ＋ 临时 10`**（两者差异见上方释义） |
| 受保护 agent（不可清理 / 不可 `interrupt`） | **4** = 主 agent ＋ 业务常驻 3（**主 agent 不计入行**） |
| **结构席位**（执行者，**不计入临时**） | **2** = 出列 `2d4ba4d0` ＋ 在岗 `bb9e0148`（**出列者仍占席位行**） |
| **临时 agent 累计** | **10** = depth1 **7** ＋ depth2 **3**（**已判定 9**：7 可回收 ＋ 2 未落盘；**待判 1**：`36ef2379`） |
| 标记「**可回收**」/「**异常 · 未落盘**」/「**待判**」 | **7 / 2 / 1**（§3.1 七行 / §3.2 前两行 / §3.2 第三行 `36ef2379`） |
| 明细缺失（统计与明细不符） | **0 —— 已消解**（7 ＋ 2 ＋ 1 = 10 与 §3 明细逐项吻合） |
| 孤儿 / 重复 / 失效 / label 与职责不符 | **0 / 0 / 0 / 3**（`36ef2379` 的父 `6c6cedb8` 在快照中、两孙代理的父 `fa74d552` 也在 ⇒ **孤儿 0**；T42→T46→T50→T52 串行链 §5 明示不报；`843b7bf5` 结论被取代但产物仍被引用 ⇒ 不算失效；label 不符 = 三个业务常驻，`2d4ba4d0` 与职责一致 ⇒ 不算，见 §2） |
> **★ 三条方法论纪律**：① **任何统计数字必须与其明细行数、以及与另一独立分类的合计做两次交叉校验**；② **凡写「总数」必须紧随其后附「＝ A ＋ B ＋ C」构成式**，使集合边界自证；③ **凡写「总数」还必须写「时点（commit SHA 或时刻）」** —— T87 认定的根因 = **快照时点混用**（本项目计数陷阱第 **7、8、11、12** 次；前两次的形态分别是「集合边界错」与「数字脱离观测时点」）。**⚠ 口径不相交**：**「可回收 7」≠「异常 · 未落盘 2」≠「待判 1」** —— 三者结论固化程度不同；**把「未落盘/待判」并入「可回收」会把 7 虚报为 9 或 10**。
## 5. 异常分类（本册的报警口径）
| 类别 | 判据 | 处置 |
| --- | --- | --- |
| **孤儿** | 父链指向的 agent 已不在快照中 | 报主管，登记父子关系断裂 |
| **失效** | 结论已被更晚的结论取代 | 标「失效」，**不再唤醒** |
| **未落盘** | 任务完成但证据未进 `.board/`/`docs/` | **优先报警**；**绝不标可回收**（实测 2 处，见 §3.2） |
| **重复** | 同一任务存在两个并行 agent | 报主管（**串行链**如 T42→T46→T50→T52 改同一文件属**合理**，不报） |
| **label 与职责不符** | 见 §2 | 已登记；以职责映射为准 |
| **统计与明细不符（缺行）** | 统计表的分类计数之和 ≠ 明细行数 | **报警**（实测 **4** 处：① 结构 agent 自身被计入临时 ⇒ 集合边界错；② 订正多减 1 ⇒ 算术错；③ 裁决式末项「临时 8」⇒ **T87 已弃用该式**（8 既非任何时点实测值、又与明细 9 冲突）；④ 裁决式「临时 9」vs 直接加法「10」⇒ **已释义为两种集合口径（已判定 9 / 合计 10），非错误**。**均靠「统计 ↔ 明细」对账 ＋ 与独立分类交叉校验发现** —— 统计不得作为明细的替代品） |
| **归属存疑** | 子代理 label 主题与其父任务主题不一致 | **报警**，不据父任务推断子任务产出（实测 1 处，见 §3.2） |
## 6. 处置建议的裁决落盘对照（**D1–D3 与 §3.2 已由主管 `2a796c9` 结案；T87 复核后本册待裁项 = 0**）
| 本册建议 | 主管裁决（落盘坐标） | 终局 |
| --- | --- | --- |
| **D1** 9 者全 `ready` ⇒ `interrupt` 对 `ready` 者**是无操作**，一律「仅登记 · 不再唤醒」 | **裁决 G**（`TASKS.md` §11.10）：判断正确，**`interrupt` = 0 次**（本轮未执行任何 agent 操作） | ✅ 照此办理 |
| **D2** label 过时**不重建**（＋ §2 三条独立理由） | **裁决 A**（`TASKS.md` §11.9）：不重建；落盘规则「**常驻 agent 的 label 只读，职责以本册为准**」 | ✅ 已写入 |
| **D3** 「常驻 4」**三分口径** | **裁决 B**（`TASKS.md` §11.9）：采纳，**体检第 10 项改述为「常驻合计恒为 5（受保护 4 ＋ 结构 1）」**；⚠ **该值已被 T87 订正为「常驻合计 = 6（受保护 4 ＋ 结构席位 2）」**（「结构 1」在任何口径下都不存在） | ✅ 已写入（旧值留痕） |
| **§3.2** 两孙代理「未落盘」 | **裁决 E**（`TASKS.md` §11.10）：与 `EVIDENCE.md` §41.5 完全重复 ⇒ 记「已沉没 · 证据未固化 · 与 §41.5 重复」结案 | ✅ 结案 |
| **§5** 新增异常类「归属存疑」 | **裁决 E 副产物**：成立且应保留（其判据**阻止了一次错误的「并入父产物」结案**） | ✅ 保留 |
| agent 总数口径（13 / 14 / **15**） | **终局裁定 D** 写死 **14**（T55 时点）；**T87 同一时刻双测 = `descendants` 15 行** | ✅ 结案（旧值留痕） |
| 结构席位恒 2 · **总数 15 的构成式** | **裁决 T87**：**`15 = 主 agent 1 ＋ 受保护业务常驻 3 ＋ 结构席位 2 ＋ 临时 9`**；**归因 = T67 新增 depth2 `36ef2379`（不是接班）**；落盘 = `.board/BOARD.md` 新节「[主管] v0.11.0 手机端 ↔ 电脑端 互通实测闭环…」**§八** | ✅ 结案 |
| 裁决式末项（**8 → 9**）＋ 新成员 `36ef2379` | 8 **已被 T87 弃用**（既非实测值又与明细冲突）；**新成员已登记为「待判」**（§3.2 第三行，既不并入可回收也不并入未落盘）；「临时 9（已判定）vs 临时 10（合计）」的差异**已释义**（§4 第 3 行） | ✅ 结案 —— **本册待裁项 = 0** |
**D1 逐条建议（§7.1 原文，9 行）**：共同前提 = 7 可回收者与 2 孙代理**全部 `ready`、无一个 `running`**，而 `interrupt_agent` 语义是「停当前一轮」⇒ 对 `ready` 者调用**不产生任何状态变化** ⇒ 需主管执行的 `interrupt` = **0 次**，请求的是「**不再唤醒**」纪律的确认。逐条：#1 `fa74d552` 三件产物全固化无待办 · #2 `bb19ba01` 26 条断言**已机械化进入 `pnpm test` 链**且实测 26/0 ⇒ 结论由**代码**承担 · #3 `843b7bf5` **最应避免唤醒**（断言已被三轮改写，唤醒 = 高概率产出与现行 `book_lang.mjs` 冲突的第二版）· #4 `6e6acfbe` 同 #3 · #5 `18a79f6a` 四条异议已闭环 · #6 `3d2dc17e` S1/S2 已由主管 T51 修掉 · #7 `6b08a4c5` 已随 v0.10.0 `041c6e7` 发布 · #8 `9db3c4d0` 与 #9 `0f53feaa` 仅登记不再唤醒。
> **⚠ 必须写明**：本册**没有**、也**不能**把上述 9 者标为「已删除」或「已回收」—— 工具层无此能力，它们**至今仍可被 `send_message` 唤醒**。本册能做到的只是「登记 ＋ 标记 ＋ 请主管不复用」，**这就是本工具层下「清理」的全部含义**。**⚠ 特别提醒**：`fa74d552` 是两个未落盘孙代理的**唯一父**；追查 batch A/B **不建议靠唤醒它**（唤醒 = 重新消耗上下文，且**不保证它能复述子任务**）。
## 7. 临时 agent 准入与结案纪律（**T62-B 新增；本节的目的是「抑制新增」**）
> **立节理由**：委任书 §14.6「agent 数无上限增长 = 另一种臃肿」。物理精简在本工具层做不到（无删除 API），**唯一可施力的方向是入口**（少新建）与**结案**（不留悬案）。
**7.1 准入四问（机械可核对；任一答「否」⇒ 不新建，改为在既有 agent 上续做或由主管自己执行）**
1. **能否不做？** —— 该产出是否已存在于落盘文档（先按 **§7.3 语义通道**检索，而非只搜关键词）？已存在 ⇒ 不新建（重复建 = 产出现有结论的第二版）。
2. **能否并入既有常驻？** —— 该任务是否落在需求／开发／测试／结构任一的既有写域内？在 ⇒ 交该常驻（**跨域走主管转发**，不直接派）。
3. **产出路径是否可预先命名？** —— 能否**在派单前**写出确切的落盘路径（`git ls-files` 可见的路径）？写不出 ⇒ **不得派单**。
4. **完成条件是否可机械判定？** —— 能否写成一条命令／一个断言数／一个文件的存在性？写不出 ⇒ 不得派单（否则必然产生「未落盘」悬案）。
**7.2 登记四项必填（派单当轮即写入本册 §3，缺项 = 登记无效）**：**id**（派单后主管快照里的 UUID，**不得先占位后补**）· **一句话目标**（≤30 字，**不含过程动词**：写「产出 X」，不写「研究一下 Y」）· **预期产出落盘路径**（**完整仓库路径**，且必须**不是** `_tmp*` 目录 —— 依 `_tmp` 判据，`_tmp` 下任何文件一律按可清理处理、不得作为证据来源）· **判定完成的条件**（可机械核对：断言数 / 门禁数字 / 文件存在性 / 可指认的 `文件:行`）。⚠ **本轮实证（T87）**：**开发 agent 在 T67 期间自行 spawn 了 depth2 子代理 `36ef2379`（label `Capacitor feasibility probe`）而未经登记** ⇒ 本册**事后补登为「待判」**（§3.2 第三行）。⇒ **常驻 agent 自行 spawn 的子代理同样受本节约束**：四项必填须**在 spawn 当轮**由该执行者（或其派单方）交本册登记；**先 spawn 后登记**会产生「台账外无主产出」，**正好违反 §7.1 第 3 问**（产出路径未预先命名）。
**7.3 结案三判据（全满足才可标「可回收」；委任书 §14.3 四判据的机械化版本）**：① **`ready` 且非 `running`**（`running` 者**绝不标记**）；② **产出可指认到 `git ls-files` 能列出的路径**（或 `.board/` 文档的 `文件:行`/`§节`）—— **硬门槛**：指不出路径 = **未落盘 ⇒ 优先报警，绝不标可回收**；③ **主管不再派单**（depth≥2 者另加：其父已回收且结果已上浮）。
**7.4 两条禁令（均来自本项目已发生的真实误判）**
- ⛔ **「关键词检索 0 命中」不得作为「未落盘」的结论** —— 它只能支持「**没有以该名称落盘**」。**必须补一条语义通道**：读候选父产物的目录／小节标题，用**语义等价的词**再查一遍（实证：`EVIDENCE.md` §41.5 的标题是「**逐条候选审定表**」，而检索词是 `batch|etym|subagent` ⇒ 词汇不重叠，关键词永远抓不到）。
- ⛔ **不得用「唤醒」当「核实」** —— `send_message` 是**反清理**（重开一轮 = 再消耗上下文 ＋ 可能产出冲突的第二版）；「我不确定它有没有落盘」的正解是**查落盘路径 ＋ 语义通道 ＋ 报主管**，不是问它。同理 **`interrupt_agent` 对 `ready` 者是无操作**，不得把它写进「清理动作」。
## 8. 变更记录
| 日期 | 变更 | 依据 |
| --- | --- | --- |
| 2026-09-16 | **建立本册**（受保护名单 4 · 临时 agent 登记 · 常驻 label 过时问题）→ **T55 五轮**：① §3.1 **由推断版升级为核验版**（7 行逐条实测），订正 2 处错标（`fa74d552` 字节数 611 KB→**639,777 B** · `18a79f6a` 行号 `:816-847`→**`:1291`＋`:1314`**），1 处推断升级为确证（`bb19ba01` → `EVIDENCE.md:1253-1254`）；② §3.2 两孙代理由「不判定」**升级为异常「未落盘」**（四通道 0 命中）＋ 新增「归属存疑」信号 → 主管第五通道 `EVIDENCE.md:1541` §41.5 证明与父产物重复 ⇒ 结案（**裁决 E**）；③ §4 暴露并**拦住两处计数错误**（结构自身被计入临时 ⇒ 集合边界错；订正**多减 1** ⇒ 算术错，靠「可回收 7 ＋ 未落盘 2 = 9」交叉校验拦下）⇒ **临时 9 / 总数 14**（**终局裁定 D** 写死 14）；④ §2 label 过时计数 4→**3**；⑤ §5 新增两类异常；⑥ §3.1 补**同文件归属注明**；⑦ §3.2 补**口径对齐**；⑧ 新增 §6 裁决对照表 | 用户指令「负责清理临时agent … 帮助主agent管理agent结构」＋ 主管 `list_agents` 快照 ＋ 前任五轮实测（`git ls-files` / `Select-String` / `Get-Item.Length` / `git log --all`）＋ 独立复读 `EVIDENCE.md:1541` |
| 2026-09-16（**T62-B · 压缩重写**） | **250 → 120 行（零信息丢失，逐条自检见下）**：① 口径收敛 —— 旧册把「14/13」争议复述 **5 处**、「不判定」11 处、「可回收」30 处，现收敛为 **§1.2 一段终局口径 ＋ §4 一行变迁留痕**；② 新增 **§7 准入与结案纪律**；③ 新增 **§9 交接记录**；④ §1.2 登记前任 corrupt；⑤ 自检工具 = `.board/structure/agents_audit.mjs`（只读）。⚠ **本轮曾报「结构席位接班导致 14→15」待裁项 —— 该推断已被 T62-C 裁决作废**（见下行） | 用户本轮指令「精简项目结构，包括文件结构和agent结构」＋ 主管派单 **T62-B**（只出方案与文档，不删任何文件） |
| 2026-09-16（**T62-C · 裁决落地**） | ① §1.2 **补登接任者 id = `bb9e0148-3e78-4d58-b5ce-700fce51fd48`**（主管快照实测）＋ 状态 `running`；前任行明确标「**已出列 · 仍占席位行**」；② §1.2 原「待裁 / 14→15」块**删除**，改写为**已裁决结论**；③ §4 按裁决改为**结构席位 2（出列 1 ＋ 在岗 1）**＋ **一行变迁留痕**；④ §6 新增「裁决式末项」提请复核行；⑤ §9.3 记「**已补登**」并**保留那条教训**；⑥ **§9.6 第 2 项整项作废重写**：`V11-OPENDB-LEAK` 已由开发 agent 于 **`2c908e8`** 修好 ⇒ `scripts/_tmp` = **0** ⇒ **§11 熔断项不再适用**；补入 `packages/core/test/book_lang.mjs:1786-1789`「**仅断言全绿时才删临时目录、红态保留取证**」分支；⑦ §9.6 第 1 项 HEAD 更新为 **`d529b48`** | **主管 T62-C 裁决**（`list_agents` 实测：`children` 12 / `descendants` 14）＋ 开发 agent `2c908e8` ＋ 本册 §4 交叉校验 |
| 2026-09-16（**T87 · 口径终局 ＋ 新成员补登**；**T89 = 同一裁定的投递探测复述**） | ① **§1 标题**改为 **`15 行 = 14 个 UUID ＋ 主 agent（无 id）`**（`descendants` **15** = `children` 12 ＋ depth2 3）；② **§4 ＋ §1.2 裁决式写死** **`15 = 主 agent 1 ＋ 受保护业务常驻 3 ＋ 结构席位 2 ＋ 临时 9`**（原文「总数 = 14」全部改 15；**14 依计数纪律留痕不删**）；③ **补登 `36ef2379-d284-493b-b60f-48978e38c8c3`**（depth2 · 父 `6c6cedb8` · label `Capacitor feasibility probe`）为「**⏳ 待判**」—— **它才是 14→15 的真实来源**（**不是接班**；原表述「14→15 这个数不存在」**已作废**）；④ **删除**「若把主 agent 也算作行则应为 13」这句**错误推理**（`children` 12 本就不含主 agent，不存在 13）；⑤ 体检附项口径**改为「常驻合计 = 6（受保护 4 ＋ 结构席位 2）」**（「结构 1」在任何口径下都不存在）；⑥ 新增**第三条方法论纪律**（凡写「总数」须带**时点**）；⑦ §7.2 补 **spawn 未登记的实证**；⑧ **本册待裁项归零**。⚠ **T89 判据三项（§1 标题 / §4 裁决式 / `36ef2379` 补登）与 T87 完全一致、无冲突** | 主管 **T87 终局裁定**（同一时刻双测 `children` **12** / `descendants` **15**，HEAD `e20f512`）＋ **T89 投递探测派单** |
**★ 本版信息保全自检（机械可复跑）**：`node .board/structure/agents_audit.mjs .board/agents.md` ⇒ **13/13 个 UUID 席位 `.Contains()` = true** ＋ **保全清单（见工具内计数）** ＋ **新增事实 9/9**，**缺失 0**。**原册章节 → 本册章节对照**：原 §0→§0 · 原 §1→**§1.1** · 原 §2→**§2** · 原 §3→**§3** · 原 §4→**§4** · 原 §5→**§5** · 原 §6 变更记录→**§8** · 原 §7 处置建议→**§6** · **§1.2 / §7 / §9 = 新增**（§1.2 = 原 §1 的结构常驻行 ＋ 本轮 corrupt ＋ 接任者事实）。
## 9. 结构 agent 交接记录（**给下一个接任者**）
**9.1 岗位坐标 ＋ 9.2 前任出局**：委任书 `.board/roles/structure-agent.md`（§14 = agent 结构管理）｜台账 `.board/STRUCTURE.md` ｜**本册 `.board/agents.md`** ｜工具目录 `.board/structure/` ｜`PROJECT_STRUCTURE.md`（**仅事实性**）｜`.gitignore`（**只增不删**）—— **权限一句话：能删产物，不能删代码；能出方案，不能改架构。** 前任 `2d4ba4d0-64b5-4449-a295-66c327942977` 会话曾 `[diagnostic: corrupt]`（**主管 T82 复测已回到 `[ready]`** ⇒ 该状态**不是终态、可自行恢复**，故**「修复手段」= 无需修复**）、**T62 派单永久未回执** ⇒ 写域由接任者接管（`.board/TASKS.md` §11.13.7 登记）；**维持不唤醒纪律**（出列者仍占席位行）。
**9.3 接任者自身的标识问题（教训）＋ 9.4 数据源纪律**：本任 id = **`bb9e0148-3e78-4d58-b5ce-700fce51fd48`**，**已补登**（主管快照实测；另可得 `DSH_SESSION_ID`，`DSH_WEB_URL` = `http://127.0.0.1:55017`）—— **本段作为方法教训保留**。**★ 教训**：我**看不到自己的 agent id**（`list_agents` 只返回我的子代理）⇒ **结构席位在快照里由谁代表，必须由主管指定，不能自证** —— 本任是靠主管实测才补登的，**不是靠自己查到的**。每轮回执**必须写明**「数据来自主管快照，时点 = …」（委任书 §14.5）；**不得把「我这里看不到」写成「不存在」**。
**9.5 本任做了什么**：**T62-B** = 本册压缩重写（250 → **120** 行，附 §8 对照表 ＋ `.board/structure/agents_audit.mjs` 自检工具）· 口径收敛（14/13 · 可回收 7 / 不判定 2）· 新增 §7 准入与结案纪律 ＋ §9 交接记录；**T62-C** = 落地裁决（补登接任者 id · 结构席位 2 · §9.6 第 2 项重写）；**T82** = `PROJECT_STRUCTURE.md` **事实性订正 12 处**（`scripts/` 135→**111** · `.board/` 14→**16** · `docs/` 7→**13** · 体检⑩ → 常驻合计 · HEAD `2a796c9`→**`0dadb61`** · `structure/*.mjs` 2→**4** · §3 `_tmp` 块转终态）；**T87/T89** = 口径终局（**总数 15** · 补登 `36ef2379` 为「待判」· 删除「也算作行则为 13」错误推理 · **待裁项归零**）。**全程未改** `.board/STRUCTURE.md` · **未执行任何 git 写** · **未做任何 agent 操作**（`interrupt` = 0 · `send_message` = 0）。
**9.6 交给你时已知的未闭环（按可执行性排序）**
1. **`PROJECT_STRUCTURE.md` 事实性过时 4 处**（属我写域，**仅事实性**）：`scripts/` 仍写 **135**（T62 后 **99**）· `.board/` 仍写 **14**（现 **15**，含 `structure/recheck_zero_refs.mjs`）· `docs/` 仍写 **7**（现 **13**，`docs/legacy/` 迁入）· §10 九项体检仍写「⑩ 常驻 agent 数（**恒为 4**）」（裁决 B 已改述为「**常驻合计恒为 5（受保护 4 ＋ 结构 1）**」）；§0 记 HEAD `6eca85a`，而现 HEAD = **`d529b48`**（此前 `2c908e8` T61）。**引用时必须带日期或 commit SHA**。
2. **`scripts/_tmp/` 已不再是问题，但「残留」的解释须升级**：根因 `V11-OPENDB-LEAK` **已由开发 agent 于 `2c908e8` 修好** ⇒ 主管实测 `scripts/_tmp` 条目 = **0**（core 门禁连跑后仍为 0，`booklang_tmp` 目录本身已不存在）⇒ **原 §11「不得第三次重删」熔断项不再适用**。⚠ **但旧解释（「残留 = 泄漏残渣」）不完整**：`packages/core/test/book_lang.mjs:1786-1789` 存在「**仅断言全绿时才删临时目录、红态保留取证**」的分支 ⇒ 将来再现 `run-*` **可能是红态留证**（不是垃圾）⇒ **先判性质再决定是否清理**；**「复跑后须与复跑次数对账」这条纪律保留**。
3. **§4 末项口径提请复核**（裁决式「临时 8」vs 明细 `7 ＋ 2 = 9`；旁证 = `children` 12 / `descendants` 14 与 9 个临时 id 逐项吻合）—— **本册当前唯一未结项** · **4.** `docs/legacy/` 后置风险：`check_requirements.py` 归档后写目标转移到 `docs/legacy/`，但**废弃横幅仍会被重写**；风险随移回根目录立即复原。
**9.7 给接任者的踩坑清单（本项目已各发生 ≥1 次，勿重踩）**
- **行数一律换行符计数**：本册三种口径实测 —— **换行符 = 120** · `Measure-Object -Line` = 118 · 非空行 = 118（**差 2 = 空行数**；旧册曾按 `Measure-Object -Line` 口径被读成较小的数）。**分组统计用 `git ls-files -z`**（默认对非 ASCII 路径加引号并八进制转义 ⇒ 会凭空造出一个 `"docs` 目录）。⚠ **本册在 Windows 上会被 harness 当作 `AGENTS.md` 加载**（`.board/agents.md` 与 `.board/AGENTS.md` 是**同一个文件**，不区分大小写）⇒ **本册内容会被注入到 `.board` 下所有 agent 的上下文**，写它等于广播 ⇒ 措辞须克制、不得写成会覆盖他人职责的指令。 **`.board/agents.md` 只有一个写者（本角色）**；跨域改动一律走主管转发，**不得** `send_message` 任何 agent、**不得** `interrupt_agent` 受保护 agent、**不得**请求 `sandbox_permissions`、**不得**执行 git 写。**任何门禁数字必须在「工作区无并行写入」时采集**（本项目曾把并行开发中的中间状态误判为 flake）。每轮结束把回执按委任书 §12 格式发主管；需要裁决的事项同时追加到 `.board/BOARD.md` 的本角色区。
